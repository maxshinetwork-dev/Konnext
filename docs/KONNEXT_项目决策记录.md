# KONNEXT 办公管理平台 · 项目决策记录

> **这份文档是跨对话的项目记忆。** 新对话读完这一份就能接上全部上下文，无需重述。
> 只写**最终结论**和**为什么这么定**，不写演进过程。
> **当前版本：`contract_v0_31.sql`** —— 60 表 / 85 视图 / 140 条 RLS 策略 / 73 条断言
> 回归套件 `tests/`：26 个文件，`bash run_all.sh <ddl>` 一键跑全套，**130 次拦截全过**
> 语言：全程中文（UI、DDL 注释、文档）

---

## 1. 项目背景

KONNEXT 是澳大利亚（悉尼 / NSW）的智能家居集成公司，服务华语客户。要做**多部门协作的办公管理平台**，把售前、工程、采购、库管、财务、运维的工作流数字化，做到可验证、可校验、可修改。

**用户的两个核心担心**（贯穿所有设计决策）：

1. **架构过载** —— AI 设计太复杂，以后改不动
2. **逻辑错误长期不被发现**，导致错误决策

> 第 2 条是整个方案最重要的驱动力。实践中真正危险的 bug 全是"**不报错、只是数字悄悄错了**"那一类——
> 退库不进库存、未退料不进发票、视图绕过 RLS、SECURITY DEFINER 提权。
> **门禁测试一个都没抓到**，所以才有了 73 条不变量断言。

---

## 2. 十条架构原则（不可动摇）

1. **一个共享业务对象(project) + 只增不改的事实流 + 显式门禁 + 派生视图**。部门拥有的是"字段和门禁"，不是各自的数据库
2. **只增不改(append-only)**：原始输入不 UPDATE，改 = 插新行
3. **当前状态尽量派生**（视图现算），不存
4. **字段级所有权**：每个数据只有一个写入方 → 从根上杜绝覆盖冲突
5. **定格的落库，会变的现算**
6. **门禁由 DB 触发器强制**，UI 只是第二道。"没被拦住的门禁等于不存在"——再加半句：**"没被测到的门禁等于没有保障"**
7. **外部系统只读单向镜像**；**外部依赖绝不进门禁**（Google 挂了不能卡流程）
8. **通用能力抽离复用**（考勤、通知、清单），不做成某模块专属
9. **克制意识**：能用 SQL/公式就不用 LLM；AI 写实现、人定契约
10. **例外用「手动标记 + 填原因 + 留痕」过，不取消卡点**

### 三个判断口诀

**平铺还是建表**
- 固定几个、几乎不变 → 平铺进主表（联系人×3、楼层）
- 数量不定、种类可能扩展 → 单独建表（家庭成员、参建方、方案）

**分表还是共用**
- **规矩不同** → 分表（SM 要排队要签字，安装不排队不签字）
- **只是填的内容不同** → 共用一张表 + 类型字段（四类任务的清单）

**能自动扫全库的地方，绝不手列清单**
- 手列的清单一定会漏，而且漏了不报错（曾漏 13 个视图、漏售前身份）
- 项目标识追加、`security_invoker` 设置都改成程序化扫全库 + 断言守着

---

## 3. 技术选型与部署架构

```
浏览器（React + TypeScript + Vite）
    │  只调 API，不知道数据库存在
    ▼
后端 API（Next.js API Routes）
    │  ★ 在这里 SET app.account_id ← 取自服务器验证过的会话
    │    前端请求里带的任何身份信息一律忽略
    ▼
Supabase（PostgreSQL 15+）
    132 条 RLS 策略 · 73 条断言 · 全部业务门禁
```

**为什么必须有中间那层**：RLS 靠 `SET app.account_id` 判断你是谁。前端直连的话，**这句话由浏览器说了算**——改一行 JS 就能声称是核心管理员，全部权限瞬间失效。

**★ 必须 PostgreSQL 15+**：视图的 `security_invoker` 选项要它。

**绝对不能做的三件事**（写进开发规范）：
1. ❌ 前端直连 Supabase
2. ❌ Service Key / 连接串下发到浏览器
3. ❌ 前端直接查基表——**基表没有列级遮罩，只查视图**

**为什么选 TypeScript**：84 个视图的列名与可空性可从数据库生成成 TS 类型，改了 DDL 前端立刻编译报错。没类型的话，`sell_price_aud` 被遮罩成 null 而前端拿它做乘法，页面显示 NaN 且不报错。

### 外部依赖

| 依赖 | 状态 | 要点 |
|---|---|---|
| **Twilio 短信** | ★最要紧，未接 | **必须买澳洲手机号，不能用字母发送者 ID**（后者收不到 YES 回复）。2026-07-01 起澳洲 Sender ID 需 ACMA 登记，手机号可绕开。无密码登录**完全依赖它** |
| Google Distance Matrix | 未接，不阻塞 | `avoid_tolls=true`，**motorway 照走**（悉尼避 M2/M7 绕远反而失真）。查不到就是空，不进门禁 |
| 报价系统 | **不对接** | 报价同事直接在系统里手填，`quote_source` 保持 `'manual'` |

---

## 4. 组织与权限

### 六个部门
售前 · 工程管理 · 采购 · 库管 · 财务 · 工程运维
（采购与库管是**两个部门**，可由同一人兼任 → 该账号同时挂两个部门）

### 账号三级结构

| 级 | 谁 | 账号由谁建 | 用什么 |
|---|---|---|---|
| **1** | 决策管理员，上限 **3** 个，其中 **1 个核心管理员** | 核心管理员自建；第一个账号自举 | 后台 + 决策页 |
| **2** | 部门负责人 | **只能由决策管理员建** | 后台（本部门范围） |
| **3** | 施工人员（含外包） | **只能由「工程管理」或管理员建** | **只有 App，不能进后台；只有上报权限** |

- **管理员不挂部门**（全局）；**施工人员也不挂部门**（只上报）
- **外包**：不计等级；账号由管理人员**代建**，建好后按第三级用
- **电工 / Builder / 客户：不给账号**，全部走短信与邮件

**登录与找回**
- ★ **全系统无密码**——表里不存密码，手机短信验证码（`auth_otp` 只存散列，一次性、有时效）
- 管理员额外**邮箱 + 手机双验证**，且必须绑手机、必须填邮箱（无邮箱直接拒建）
- **只有管理员可自主找回**；其余一律管理员重置，**全程留痕**（谁给谁改的、原值新值、原因）

**核心管理员**：全系统唯一 · 只有他能建/停用其他管理员 · **不能停用自己**（须先移交）

### 权限规则：读全部、写本部门

| 级别 | 读 | 写 |
|---|---|---|
| 决策管理员 | 全部（含敏感） | 全部 |
| 部门负责人 | **全流程业务数据**（敏感金额除外） | **只能写本部门归属的表**（`table_ownership`） |
| 施工人员 | 不给后台读；只看自己的工时与日结 | 只能写自己那几行 |

> **为什么读全部**：下游能查上游的流水，卡在哪儿一目了然。系统自动推进之外还有人在推。

**部署方式**：应用只用**一个数据库用户**，登录后 `SET app.account_id`；权限完全由**账号 + 部门表**决定。
未登录（account_id 为空）→ 按最低权限处理，**失败即关闭**。

**⚠️ 兼任风险**：`v_dual_role_alert` 点出"同时管采购与库管"的账号——这类账号**可以自己下单、自己确认到货**，「盘点人不能自己批自己」那道门也会失效。**待决策**：是否要求到货确认与盘点审批交他人复核。

### 金额可见性：五类，各看本职

| 类别 | 谁能看 | 守卫函数 |
|---|---|---|
| **薪酬** | 财务＋管理层 | `fn_can_see_salary` |
| **成本与利润率** | 财务＋管理层 | `fn_can_see_margin` |
| **采购价与毛利率** | 采购＋管理层 | `fn_can_see_purchase_price` |
| **客户合同金额** | 财务＋**售前**＋管理层 | `fn_can_see_customer_amount` |
| **库存成本单价** | **库管**＋采购＋财务＋管理层 | `fn_can_see_stock_cost` |

> 售前能看合同金额（签约价是他填的）；库管能看库存成本（出库单价是他填的）。
> **业务流水互相可见 ≠ 钱的数字互相可见。**
> 要放开某一项，在对应函数里加 `OR fn_has_dept('xxx')`。
> `v_money_guard_map` 列出每个带钱的视图由哪个函数看守，改动时照它自查。

### ⚠️ 三个曾踩过的权限坑（都是"不报错只是全放开"型）

1. **`SECURITY DEFINER` 里的 `current_user` 变成函数所有者**。原先写了 `pg_has_role(current_user,'konnext_admin')`——在 SECURITY DEFINER 中 `current_user` 是 postgres（超级用户），于是**每个人都被判成管理员**，库管改签约价一声不响就成功。已去掉所有 `pg_has_role`
2. **权限函数必须 `SECURITY DEFINER`**，否则 `fn_me()` 查 `app_account` → 触发该表 RLS → 策略又调 `fn_me()` → 栈溢出
3. **视图默认按【视图所有者】执行，RLS 整个被绕过**。PostgreSQL 视图默认 `security_invoker=false`。**所有视图必须 `ALTER VIEW ... SET (security_invoker = true)`**，DDL 末尾有自动补齐的 DO 块，断言 `INV-SEC-01` 守着

---

## 5. ★★ 项目归属三态规范（最重要的横切规则）

> **系统内所有流水／报告／报表／通知／任务／成本／收款／付款／报销／收入／支出／利润，
> 必须有具体项目。例外只有两种，且都必须明确标记，绝不许靠留空糊过去。**

| 三态 | 含义 | 要求 |
|---|---|---|
| `project` | 属于具体项目 | **必须填 `project_id`** |
| `company` | 公司级 | **明确标记**；通知/报销还要填 `scope_reason` |
| `untraceable` | 无从追溯 | **明确标记 + 提示人工确认**，系统不猜 |

**`project_scope_registry` 归属登记表**——每张表属于哪一类、怎么追回项目：

| 归属类型 | 表数 | 举例 |
|---|---|---|
| 项目必填 | 19 | 收款/出库/工时/变更/维护/订阅/RMA/交付/SM |
| **项目 或 明确公司级** | 5 | 通知 · 部门交接 · 交接留痕 · 问题上报 · **报销** |
| 经父表可追 | 6 | 收款明细→milestone · 出库明细→出库单 |
| **跨项目·可追** | 5 | 日结 · 每日上报 · 月工资单 · 倒休 · 路程预估 |
| 公司级 | 4+1 | 采购单 · 盘点 · 物料 · 汇率 ·（离职清算=人员级） |
| 人员级 / 配置 / 审计 | 19 | 账号 · 人员 · 阈值 · 假日 · 文档 · 断言 |

**跨项目怎么追**：`v_daily_projects`（某人某天涉及哪些项目 = 打卡 + 四类任务派工，合并去重）。在它之上：
- `v_daily_payroll_detail` —— **挂起的日结带"补录线索"**：「待负责人补录 · 当天派工：0142 / 0180」；
  当天既无打卡也无派工 → 「**★无从追溯：需人工确认**」
- `v_payroll_month_projects` —— 月工资单按周期内各项目**工时占比分摊**
- `v_toil_detail` / `v_travel_estimate_detail` / `v_daily_report_detail` / `v_daily_issue_detail`

**★ 项目标识贯穿全系统**
- `project.display_label` 生成列 = `编号 · 名称或Suburb`
- `v_project_label` 规范视图（`display_label` / `addr_suburb` / `addr_full` / 业主 / Builder / 电工）
- **所有带项目列的视图（33 个）自动追加五列** `pj_code` `pj_label` `pj_suburb` `pj_addr` `pj_owner`
  —— 程序化扫全库，新增视图重跑 DO 块自动补上；断言 `INV-UI-01` 守着**一个都不许漏**
- `v_project_directory` 项目总目录：六个部门的项目列表页**共用这一个**

> 实际工作里没人靠 `KX-2026-0142` 认项目，大家说的是「Cherrybrook 那家」。所以地址必须带出来。

**★ `v_open_tasks` 悬而未决总表**——管理人员打开这一张就够了。汇总 11 类挂起事项，
**前三列固定为 项目编号 / 项目地址 / 项目标识**，无项目者显示「公司级」或「★无从追溯」，绝不留空。

---

## 6. 完整业务流程

### ★ S1~S4 与 SM1~SM4 是两套东西，别混

| 编号 | 是什么 | 内容 |
|---|---|---|
| **S1~S4** | **收款阶段** | S1 定金 10% ｜ S2 物料款 40% ｜ S3 人工款 40% ｜ S4 尾款 10% |
| **SM1~SM4** | **工程现场会议** | SM1 进场 ｜ SM2 布线交流 ｜ SM3 检查布线 ｜ SM4 封板核对 |

> **S 是钱，SM 是会。** 财务说"S2 没到账"和工程说"SM3 已完成"是两件事。
> （决策看板 Demo 里把工程阶段写成 S1~S4 是那边的笔误，以本契约为准。）

### 主链路

```
【售前】七步：①讲功能 ②全功能设计 ③讲解修改 ④出Draft ⑤讲Draft ⑥最终定价·客户签字 ★方案定版 ⑦已收定金
   ↓ 门禁：第六步没签 → 不能置"已签署报价"、不能进施工
【财务】S1 定金 10%   ↓ 门禁：定金没到 → 工程不能进场
【工程】SM1 进场 → SM2 交付Finish给电工·★电工签字 → 〔电工Rough-in布线〕
   → SM3 检查布线·★双向签字·★登记变更   ↓ 门禁：SM3 完成才能发 S2
【财务】S2 物料款 40%   ↓ 门禁：S2 结清才能下单 ／ 才能出库放货
【采购】下单 → 到货入库（公司级）
【库管】★出库 —— 物料在这一刻才落到具体项目 + 提货人 + 短信确认
【工程】预配 → 发货 → 〔封板〕→ SM4 封板核对出线   ↓ 门禁：SM4 完成才能发 S3
【财务】S3 人工款 40%（★与电工补线解耦）
【工程】安装调试（一趟趟去）  ↓ 门禁：必须 SM4 之后
   → ★整体完工 = 剩余清零 + 负责人确认，两个都要
【财务】S4 尾款 10% + 可计费变更 + 未退料
【工程管理】看"工程流水账"人肉确认交付条件（系统不代替判断，但留痕）
【工程】交付 Handover  ↓ 门禁：S4 结清（硬卡无后门）+ 已确认 ready + ★客户签收
   → 完成瞬间：回写交付日 · 状态转 delivered · ★工程利润率定格
   → ★交付过程中签【业主订阅】（归运维）
【运维】技术维护（长期）+ 订阅续费
   → 拒付 → 财务标记 → ★项目级全面停服 → 市场发声明 → 90 天后停用服务器
```

### 项目状态（9 态）+ 卡住天数

`engaging` 接洽中 · `to_contact` 待联系 · `quote_signed` 已签报价 · `deposit_paid` 定金已收 ·
`in_construction` 施工中 · `delivered` 已交付 · `maintaining` 维护中 ·
**`bid_lost` 留标** · **`stalled` 已烂尾**

| 失败态 | 定义 | 门禁 |
|---|---|---|
| **留标** | 参与投标/谈判但未中标，或方案发出后无反馈 | **有任何一笔合同款已结清就不许置留标** |
| **已烂尾** | 已在施工阶段无法继续，**且尚未进入维护** | **已交付的不许置烂尾**；**未签约的也不许**（那叫留标） |

**卡住天数**：`project.status_since` 状态一变自动重置 + `status_stall_threshold` 一状态一行配阈值。
默认：接洽 45 · 待联系 30 · 已签报价 14 · 定金已收 21 · 施工中 120 · 已交付 30 · 终态不设。
`v_project_stall` 算停留天数；`v_risk_board` 汇总留标/烂尾/卡住/订阅逾期。

---

## 7. 各模块最终结论

### 7.1 售前立项
`project`（编号/地址/坐标/联系人×3平铺/佣金/房屋用途类型/施工阶段/楼层×5/屋顶/七步时间戳/签约价/定金/**免责维保月数 0~12**）
+ `household_member` + `project_party` + `solution_option`（有线无线单列 + 协议 + 预算 + **有无 KNX/地暖**）

- **第六步签字 = 方案定版信号**，没它后续全部动不了
- **定金只有财务能写**，售前只读（字段级所有权第一个落地）
- 乐观锁 `version`：冲突提示「项目已被他人修改（期望 1，实际 3），请刷新重试」
- `has_knx` / `has_floor_heating` 决定 **SM2 清单上要不要显示"带 KNX 线/地暖线"**
- 坐标既给地图，也当**考勤围栏圆心**

### 7.2 四类任务（顺序 = 现场先后）

| 顺序 | 任务 | 特有规矩 |
|---|---|---|
| **①** | Site Meeting | SM1→2→3→4 顺序硬卡；SM2 电工签字、SM3 双向签字；**不报那 5 类问题** |
| **②** | 安装调试 | 必须 SM4 之后；**不签字**；超时要报问题 |
| **③** | 交付 | 必须 S4 结清；**客户必须签字**；一项目一次 |
| **④** | 技术维护 | **只受理已交付项目**；默认挂维护单，急事可无单派工+填原因 |

**四类共用**：出发前清单 `job_checklist`（靠 `job_kind` 区分）· 工时考勤 `work_log` · 通知 `notification`

> 任务表分开是因为**规矩不同**；清单合并是因为**规矩相同**（适用项全勾才放行）。

**SM 各自特有**
- SM1：进度照片、记录牌照片、见到了谁、采集分包商进参建方表
- SM2：交付 Finish 给电工 → **电工单向签字**；清单 6 项（KNX/地暖为条件项）
- SM3：以 SM2 的 Finish 为准检查 + **双向签字** + **登记变更**；整改只记"已告知+电工签字"，不追踪落实
- SM4：只带最新图纸；人工核对出线；**缺线只拍照通知，不自己动手**

**老项目后门**：`na_flag` 标不适用 + 填原因 → 后续 SM 放行。走一次记一次。

**安装剩余项五态**：`pending` / `done` / `cancelled`(填原因) / `suspended`(填原因+卡在谁) / `to_variation`(关联变更)
> **"清零"≠"全做完"**——后三种也算清零，但每次都留着名字和理由。
> 负责人点完工时看到的是"3 项：1 项客户取消、2 项等空调队"，签的是明白账。

**交付前的工程流水账**（视图，不新建表）：时间轴维度 + 人员维度（KPI 用）。
**交付条件人肉确认，系统不卡**——"干得够不够好"没法用规则表达。但**谁、何时、看着哪版数据点的 ready 要留痕**。

### 7.3 变更 Variation

**两个产生时点**（2026-08-02 老板拍板，放开原「只在 SM3」限制）：
- **SM3 现场**（origin='sm3'）：当着客户面登记，类型 多加/减少/移位（移位在图上标，不记原坐标），
  登记后自动知会设计、采购、财务 + 留痕。**金额和工时在 SM3 都不碰。**
- **工程追加**（origin='post_sm3'，SM3 之后 ~ 项目交付之前）：工程管理为项目追加自定义变更——
  物料清单**自动传库管安排出库**（出库价自动计入成本）· **预计人工工时由工程管理手动填写**
  （`labor_hours_est`，只进成本核算与排产，不改计费口径——人工仍含在物料售卖价里）·
  金额由财务估价 · **并入 S4 一起结账**；
  **★每笔追加登记即自动给付款人发告知短信**（拍板配套：回 Y 记录确认、不回不挡进度——
  系统只摆事实，留「几月几号已告知」凭证防事后扯皮）；交付后/S4 发票后不许再追加。

**计费规则**
- **加 / 减 → 计费**（客户变更）
- **移位 → 不计费**：移位是电工装错位置，属整改。仍登记留痕，但填金额直接拒
- **人工不单列**：物料售卖价 = 成本 ÷ (1−毛利率)，**毛利里已含安装人工**，变更跟着物料走
- **工时超支不挂客户**：实际 vs 预测的差额是我们效率问题，吃工程利润率

**估价 = 外部系统**：财务 + 库管依据工程与 SM 记录**在外部手工计算**，填回 `settle_amount`。
留痕四件套：`settle_source` / `settle_ext_ref` / `settle_by`+`settle_at` / `settle_note`。
**★ S4 发票已发出后，变更金额不许再改**——要改就 S4 开 v2 重发。

**未退料并入 S4**：S4 开票**前**跑 `fn_settle_unreturned(项目)`，把未退料按售卖价转成正式变更记录。
明细表亮 `need_settle_unreturned` 提醒；**S4 发出后不许再追加**。
> 这是补的一个洞：未退料原先只是视图，没进 `variation` 表，导致明细算 56513.83、发票自动算 54500，**差 2013.83 且不报错**。

**变更争议不追溯**：交给人处理。系统只摆事实（谁手里多少件、金额多少、Builder 何时收到告知）。

> **📌 提案史**：2026-08-02 工程负责人提出 → 同日老板拍板**「放开 + 告知短信」**（上方已并入正文口径）。
> 契约落点 → v0_34：`variation.origin('sm3'|'post_sm3')` CHECK + `labor_hours_est`（工程填）+
> 追加告知记录（notified_at / sms_replied_at 仅记录不做门禁）+ 门禁「交付后不许追加」；
> 原型已演示：S4与Var「工时(工程填)」列 + 「工程追加」标签 + 已传库管提示（KX-2026-0142 行）。

### 7.4 采购与库管（两个部门）

```
【采购】对供应商下单 → 到货入库（公司资产，不属任何项目）
   → 【库管】出库 ★这一刻才落到具体项目 → 提货人短信确认
```

**物料主表 `material`**
- 品类(功能) / 协议 / 物料名(内部型号) / 型号(供应商型号) / 供应商 / 规格颜色
- **每个规格颜色是独立物料**：白面板 M0001、黑面板 M0002 各有各的库存与红线
- **`display_name` = 物料名 + 规格，自动生成**。两行的 `internal_name` 完全相同，报表一扫就看串 → **所有对外呈现一律用 `display_name`**
- **C1 定期备货**：每半月集中采购，设 `reorder_point` 红线
- **C2 按需采购**：本地/极易获取，**严格不设红线**（填了直接拒），SM 确认需求后采购，客户最长等两周
- 三币种采购价 + 运输成本；**利润率口径 = 毛利率**，售卖价 = 成本 ÷ (1−利润率)
  - ⚠️ 利润率必须 < 0.95：填 1 除以零、填 >1 算出负价，且不报错
- 维保期(月)：厂商固定期限，**与工程变更 `variation` 无关，勿混用命名**
- 调价/调利润率**自动留痕**

**增 / 划掉 / 恢复 / 彻底删除**
- **划掉**（软删除）：`active=false`，界面画横线。**必须填原因**
- **恢复**：`archived_*` 不清空——划掉过这件事本身是历史
- **彻底删除**：**只有从未产生任何流水的物料才允许**，且必须先划掉（防手滑），删除动作进审计日志
- 划掉后**不再被任何系统调用**（采购/出库/期初全拦），但**历史记录一字不动**
- **划掉但仍有库存的货不能从报表消失**（`v_archived_with_stock`），否则成账外物资

**汇率 `fx_rate` ★只增不改**：每次抓取插新行，当前取最新 → 历史汇率永久可查。
抓不到时人工填标 `manual` 并写原因；`is_stale` 超 3 天亮灯，**不静默用旧值**。

**★ 库存全部现算，不落库**
```
当前库存 = Σ到货 − Σ出库 + Σ退回(好料·未被拒收) + Σ盘点调整(已审批)
```
> 用户最初想加"库存"一列。**不能加**——存成列就有两个真相，迟早不一致且不报错。

**期初库存移库**（公司不是从零开始的）
- 采购单加类型 `opening`，把仓库现有存货**当成一次到货**录进来 → 公式不用改，来路可查
- **一个物料只能做一次期初**（做两遍库存凭空翻倍，**上线时最容易犯又最难发现的错**）
- 工具：`期初库存_填写模板.csv` + `导入期初库存.py`（默认只试算，`--confirm` 才写；整批一个事务，要么全成要么全不写）

**出库 `stock_out`（★物料落项目的时刻）**
- 强关联三要素：出库时间 + 项目 + 提货人
- **提货人只能二选一**：自己的工程人员 或 **本项目立项时录入的参建方**（拿别项目的电工来提 → 拒）
- 姓名手机号**定格**在出库单上；没手机号直接拒（发不了短信）
- 出库数超过库存 → 拒
- **★ S2 门禁在出库，不在采购下单**：采购是公司级行为，不能因某项目没付钱就不给仓库补面板

**提货短信双向确认**（`sms_2way` / `replied_yes` 占位在此首次启用）
- 出库即自动生成中英对照提货清单短信；**一张出库单一条短信**（加明细时更新内容，不是加一行发一条）
- 已发出的短信不再改动；回 `yes` → 出库单自动置"已接收"

**★ 给 Builder 的物料发放告知**（无 Builder 则发业主）
出库给外部人员时自动发出，写明何时把什么发给了谁、逾期未退将计入 Variation。
**这不是通知，是责任凭证**——没有它，业主凭什么认未退料那笔账。

**退库 / 拒收**
- 好料 `good` → 回可用库存；坏料 `damaged` → 不回库存，**自动转 RMA**
- 退库数不得超过该人在本项目名下未退数量
- **拒收必须三件齐全**：谁拒的 + 原因 + 货现在在谁名下（缺一即拒）。责任仍归对方

**★ 未退料一律先计入客户 Variation**
> 这不是记账偷懒，是**拿客户当审计员**。工地物料流向看不住，靠自查成本高又查不全；
> 变更单一发出去，客户自然来对账，异常就浮出来。客户 argue 即触发追溯。
> 前提：Variation 到 S4 才结算，中间有足够时间 argue。
> **发错料 / 现场条件变了 / 电工装坏了——一律先记客户账**，不在系统里预先分责任。

**RMA 坏货**
- 坏货**必须退回仓库才认**，不退则责任人自担
- 记录：项目/退货人/物料/退回时间/包装是否完整/配件是否缺失/**损坏原因**
- **保修起算 = S2 结清日**；供应商与保修自动带出
- **在保 → 返供应商**，四道跟踪防"退回去就不还了"：寄出→已接货→已返还→修理费/运费。**顺序硬卡**
- **过保 → 报废**；积压可清理，但**必须记录操作人与原因**
- **成本吸收**（登记当时定格）：未交付 → 项目工程成本；已交付 → 长期运维成本

**盘点（账实不符）**
- 不直接改库存，**记一笔盘点调整流水**
- **系统数在盘点那一刻定格**——不存的话事后再出入库就永远还原不出"当时差了多少"
- **盘亏必须填原因**；审批线 A$200，超过必须审批，且**盘点人不能自己批自己**
- **审批前库存不受影响**；审批后自动调整；**已审批不能再改明细**

### 7.5 财务

**全公司一套应收规矩**：`payment_milestone` 加 `kind` = `contract`(S1~S4) / `maintenance` / `subscription`。
收款明细、结清判定、Invoice 版本化、现金/走账、GST 全部共用。
> 不合并的话现金和 GST 要写两遍，**税务口径两套账，风险不小**。

**应收金额：发出即定格**
- 发出前跟着签约价现算浮动（签约价×比例，S4 叠加**可计费**变更）
- **一点"发出"就焊死**——客户手里那张纸和系统永远对得上
- 要改 → `invoice_ver` +1 重开新版

**结清：系统算，财务点**

| 项 | 谁决定 |
|---|---|
| 已收 / 还差 | 系统现算 |
| `partial`（收了不够） | **系统自动判** |
| `settled` | **只能财务点** |
| 足额 | 直接放行 |
| **不足额** | 放行，但**必须填原因**，**批准当时的差额定格落库** |

> 差额定格的理由：以后收到零头，现算值会变 0，当时的批准记录就糊掉了。

**超期：只提示，不作废合同**

| | 从哪算 | 谁看 |
|---|---|---|
| 本期超期 | 最后一版 Invoice 发出 | 催账 |
| **账龄** | **第一版发出** | 管理层，**改版抹不掉** |

标准 30 天。市场 + 财务介入催账。
> 为什么要两个数：改版重发会让超期归零，而催账时改金额重发本是常规操作。**账龄堵住这个口子。**

**拒付：财务手动标 → 项目级全面停服**
不用等 30 天；停整个项目不是单张单；**任何新任务派不出去**；90 天后提醒停用服务器（市场发声明，**人去执行**）。

**收款细节**：一笔款可分多次收、现金走账混合。**GST 单列**（走账默认 10%，现金默认 0，可手改）。
⚠️ 现金/GST 涉及税务合规，系统只如实记录，**报税以会计/税务顾问为准**。

**★ 报销 `expense_claim`**
- 三态归属；公司级/无从追溯**必须填原因**
- 提交必须有收据照片；**不能自己审批自己的报销**
- 已批准的进项目成本（`v_project_expense`）；公司级报销单列（`v_company_expense`）

**S4 结算**：`v_s4_settlement` 总表 + `v_s4_line_items` 逐条明细。
**不计费的行也要显示**（灰色 ✗）——客户问起来有交代，不显示反而像漏了。
每条可追到来源、登记人、外部单号、**计算依据原文**。

### 7.6 运维与维保

**★ 维护单只受理【已交付】项目**——未交付一律不予处理，**建单和派工两道门都卡**。
交付前客户发现的问题属于安装还没干完 → 走剩余项或变更登记。

**★ 真实流程：先上门，后定价**
```
受理建单(还不知道多少钱) → 派工上门 → 修完【当着客户面确认归因】
→ 【客户签字确认当日服务内容】→ 才可离开
→ 办公室按【工程人员报回的归因 + 时长】定价 → 开票 → 收款结案
```
单据状态：`open → in_progress → invoiced → paid_closed`

**现场三样缺一不可**（否则任务点不了完成，人不能走）：
① 故障归因，当着客户面确认 ② 当日服务内容（客户签的就是这段）③ 客户签字

**人没去过不许开票**——定价要靠现场归因与时长。

**报修受理**：`reported_at`（**允许"未知"**）/ `report_channel` / `taken_by` / `report_note`（客户原话）
> 若空着就默认取建单时间，响应时长永远显示 0 分钟——**看起来好看，但那是假的**。宁可显示"算不出来"。

**口头预估 `estimate_amount`**：工程管理填，**不参与任何计费**，实际以发票为准；填了自动留痕。

**★ 故障归因·四类统一词表**

| 值 | 含义 | 后果 |
|---|---|---|
| `human` | 人为损坏 | 免责维保**不覆盖**；责任人承担，不索赔 |
| `product_defect` | **产品缺陷** | 免责期内全免；**在保 → 返供应商索赔** |
| `wear_out` | 自然老化 | 免责期内全免；过保报废 |
| `force_majeure` | 不可抗力 | 走保险或客户自担 |

> 统一前三处各用一套词表。**"非人为"和"产品缺陷"不是一回事**——面板自己坏了可能是批次缺陷（该索赔），也可能是正常老化。
> 统一后：**现场填一次，RMA 直接继承**。`v_rma_action` 按归因+在保给出处置建议。

**★ 维护有两种上报，别混**

| 上报 | 何时 | 内容 | 不填后果 |
|---|---|---|---|
| **现场·故障归因** | 收工前，当着客户面 | 上面四类 | ⛔ 走不了 |
| **当日·问题上报** | 当夜，跨项目一条 | 研发/产品/市场/标准化/工程安排 | ⛔ 提交不了 |

归因决定**这单收不收客户钱**；上报决定**管理层看到什么问题**。

**两套维保钟**

| | 免责维保 | 物料保修 |
|---|---|---|
| **起点** | **交付之日** | **S2 结清日** |
| **时长** | 0~12 个月（售前手填） | 各供应商不同（采购单填） |
| **覆盖** | 非人为：**人工+物料全免** | 只管物料 |

**期外收费**：物料**没过保** → 只收人工；**过保** → 人工 + 物料。财务报价。
**★ 不做设备台账**——维保只查该项目 S2 收款日期。
⚠️ 物料保修从 S2 结清就烧，**施工拖越久客户拿到手时保修烧掉越多**。交付当天显示"物料保修剩余 X 个月"。

**免责维保单开 $0 Invoice**：金额 0 照开发票，列明人工多少小时、换了什么件、应付 $0。
**免费不等于白干**——客户看得见值多少钱；对内一年免掉多少单列一栏。

### 7.7 业主订阅（归运维）

**交付过程中签**，业主愿意就进订阅流程。

| 项 | 规则 |
|---|---|
| 档位 | **1~5 档，第 5 档 = 不订阅** |
| 年费 | 运维填；1~4 档必填且 >0；**第 5 档不得有年费与期间** |
| 各档含什么服务 | **系统不管**，全在线下 |
| 门禁 | **只能建在已交付项目上** |
| 收款 | `kind='subscription'` |
| **计入利润率** | **不计入**。另看 `v_subscription_rollup` |

**四色状态**（`color_token`，前端直接用）：生效中 `green` · 即将到期 `amber`（30 天内）· 已逾期 `red` · 未订阅/已退订 `grey`
**已退订的历史已收仍计入累计**，只是不计年化与活跃数。

### 7.8 考勤 · 路上时间 · 午餐

**围栏**：默认 100 米（按项目可调），圆心 = 项目坐标。入场**定位优先、拍照兜底**；拍照的记录在案，频繁拍照 → 标记追溯。
离场：现在定时抽查，出围栏标"疑似离开"；将来原生 App 后台持续定位自动离场（**字段已设计好，升级时结构不变**）。

**路上时间 traffic slot** = 上一站离场 → 下一站入场，逐段编号。
**全自动派生，人不用填**——想少一段就得在打卡上动手脚，那条路已被围栏/拍照/留痕堵死。
起点 = 当日第一站**入场**；末站回程不算。

**★ 付薪按首尾**（澳洲规矩，路上不能克扣）
```
当天付薪时长 = 最后一次离场 − 第一次入场 − 午餐
```

**★ 午餐：自动扣，不打卡** —— 30 分钟无薪；首尾满 5 小时才扣；**锚点 12:00**。
> 不打卡是因为打卡能忘点/晚点/假点；**必须扣**是因为不扣的话，准点上下班 8 小时的人也会被算成超时 30 分钟——**天天触发，门禁作废**。

**成本分摊**：**路上成本摊给下一站项目**；午餐扣在锚点所在那一段。
**项目成本口径必须跟发薪口径一致**，否则利润率失真。

**Google 预估：两版都存死**

| 显示 | 何时查 | 回答什么 |
|---|---|---|
| 计划预估 | 派活时按**计划**出发时间 | 派活估时准不准 |
| 实际预估 | 收工后按**实际**离场时间 | 那个时段本该开多久 |
| 真实耗时 | 打卡现算 | 实际开了多久 |

**必须连"查询时用的出发时间"一起存死**，否则拿今天的路况审昨天的人。
**只给工程管理看，不影响发薪；查不到就空，不进门禁。**

### 7.9 每人每天上报

**挂"人 + 日期"，不挂项目**——一人一天可跑几个项目，上报**横跨项目，每天一条**。

**两条超时线**（任一超 30 分钟即强制）

| 线 | 拿什么比 | 抓的是 |
|---|---|---|
| **计划超时** | 在场总和 vs 当天派活计划总和 | **活比预想的难** |
| **工时超时** | 付薪时长 vs 标准 8 小时 | **加班了** |

> 分开是因为：堵车导致下班晚 → 工时超时（该付加班费，是事实）；活干不完 → 计划超时（要写原因，是问题）。

**五类问题**：研发 / 产品 / 市场 / 标准化 / 工程安排
- 不超时自愿填 · 超时不填不许提交 · **SM 不报**（SM 不存在这 5 类问题）· **填写时间不计工时**
- ★ **必须落到项目，或明确标 `is_company_level`**

### 7.10 工资：三种制度

| | 月薪制 | 时薪制 | **外包 contractor** |
|---|---|---|---|
| **发薪** | 每月固定，**不发加班工资** | 付薪时长 × 时薪 | **固定日薪，去了就一天** |
| **周末/公共假日** | 整天算加班 | 整天算加班 | **没这个概念** |
| **加班** | 累计 → **倒休** | 按倍数付 | **没有加班** |
| **成本时薪** | **人工换算手填**（不填拒绝建人） | 就是发薪时薪 | **系统自动算 = 日薪 ÷ 参考时间** |

**外包也进 `eng_staff`**（这张表实为"人力资源"）——考勤、工时、项目成本全套照用。

**★ 时薪计算参考时间 `ref_minutes`**：这**不是**"承诺工作多久"，是**算成本时薪用的除数**。
例：日薪 510、参考时间 10 小时 → **成本时薪 51**。
⚠️ **实付与摊入会有差额**（发薪按整日薪，项目成本按成本时薪×实际在场）。`v_contractor_cost_gap` 把差额摊出来，**让偏差看得见**。

**日结：当夜 0 点定格**，一人一天一条，行数按实际出工天数走。
**定格即不可改**——补录只能滚到下月补发。**制度、发薪时薪、成本时薪、加班倍数一并定格**（8 月加薪不影响 7 月）。

**没记录的那天**：**不扣钱**（澳洲 Fair Work 不允许惩罚性扣薪，且打卡失败常常不是人的错），但**挂起不发**，须**负责人补录 + 填说明**才放行；同时永久进 KPI。
> 压力传导到管理层，比扣工程人员合法也有效。

**加班判定**：平日超标准工时的部分 + **周六周日整天** + **公共假日整天**。

**加班倍数**：**工程管理手动设置**，两层——全局默认（`eng_setting` 四个键）+ **按人覆盖**（`eng_staff.ot_rate_*`，NULL=跟全局）。
- **不许设成低于 1**；**当天倍数随日结一并定格**；**改倍数就是改钱**，改动全进审计
- ⚠️ 现在全是 1.0（加班不加倍），**按你们实际的 Award / EA 填**

**公共假日按 NSW**，`public_holiday` 已录 2026 全年 **12 天**（核对自 Fair Work 官方名单）。三个易错点：
- ⚠️ **Anzac Day 落周六，NSW 不补假**（补假只有 ACT/WA；部分网站写 NSW 13 天是错的）
- ⚠️ **8 月第一个周一的 Bank Holiday 只适用银行**
- ⚠️ **Easter Saturday 是 NSW 特有**，别照别州抄
- **一年更新一次**；`v_holiday_coverage` 剩不到 90 天预警。**宁可空着也不要凭记忆填错**

**倒休**：**不存余额存流水**（`toil_ledger`，只增不改），余额视图现算。**按出工日归期**，不按记账时间。

**月度**：周期 = **上月 21 日 → 本月 20 日**，21 日封账即发放。**只做日结加总**，不再存一遍明细。

**入职/离职**：`hired_at` 必填、`terminated_at`。**入职前、离职后一律不生成日结**。

**离职清算**：系统算出「未休倒休 × 离职时成本时薪 = 应结多少」并定格；**付不付、付多少由人定，但不一致必须填原因**。清算后加一条冲抵流水冲平（**只增不改，旧记录一条不删**）。
⚠️ 涉及雇佣法，**以雇佣合同与会计/劳资顾问意见为准**。

### 7.11 文档库（★版本化）

三类：`internal_guide` 内部指引 · `external_notice` 对外交付（要发出去要签收）· `client_doc` 客户文档（交付时必须交齐）

`document`（逻辑文档）+ `document_version`（**只增不改**）+ `document_binding`（绑流程节点）+ `document_issue`（发放记录）

- **发放记录锁定的是【版本 id】，不是文档 id**
  > 电工签的是"我收到了第 2 版"。后来改成第 3 版，他没签过第 3 版——**出问题时这个差别要命**
- **已发放的版本不许改文件地址**（发出去的东西不能偷换），要改就发新版
- `v_node_documents`：**流程中调取**——打开 SM2 任务自动看到该带哪些文档（当前版本 + 下载地址）
- 两道门禁：**SM 完成时"必须签收"的文档必须已签收**（SM2 电工签字知悉终于落地）· **交付完成时必交客户文档必须都已发放**

### 7.12 部门交接短信通知（通用机制）

**问题**：卡点建好了，但"该谁动了"没人通知，流程就在那儿干等着。

**做法**：`dept_handoff_rule` 一张规则表定义"什么事发生 → 从哪个部门交到哪个部门 → 对方要做什么 → 多少小时内该响应"。
**以后加节点只加一行数据，不改代码。** 已配 **18 个交接点**，覆盖六部门所有衔接。

- **通用函数 `fn_notify_dept()`**：一处实现，自动查出目标部门所有在职账号发短信
- `dept_handoff` 记交接实例；**`v_pending_handoff`** 待办与催办（谁在等谁、等了几小时、超没超 SLA）
- **`v_dept_workload`** 管理层看板
- 拒付停服 SLA 只有 4 小时，**一条事件同时发给全部门**

### 7.13 成本与两个利润率

**人工成本链**
```
分母(预测工时) ← 报价系统手填 → project.planned_labor_hours
分子(实际成本) ← (在场工时 + 路上时间 − 午餐) × 成本时薪
消耗% = 实际/预测 → 推进中现算，交付定格
```

**★ 利润率口径（定稿）**
```
利润   = 合同额 − 材料 − 人工 + 维护净额
利润率 = 利润 ÷ 收入基数
```
- **材料**按【出库明细】归集（不是采购单）——**库存里没发出去的货不算进项目**
- **人工**按 work_log × 成本时薪
- **维护净额** = 维护收入 − 维护成本
- **★ 订阅收支不计入**
- 四档配色：`healthy ≥55%` / `warning 45–55%` / `breach 0–45%` / `loss <0`

**★ 三处特殊处理**（修掉"未开工显示 100% healthy"这个会误导决策的问题）

| 情形 | 处理 | 档位 |
|---|---|---|
| 未开工（无材料无人工） | **不显示利润率** | `not_started` |
| 留标 | **不显示** | `bid_lost` |
| **已烂尾** | 收入基数换成**实收合同款** | 按实算 |

> 烂尾意味着钱收不全，按合同额算利润率是自欺（会显示 +66%，实际 −237.6%）。

**工程利润率**交付当天定格落库；**长期利润率**视图现算，单列"免责维保共免掉多少"。
**RMA 成本吸收**：未交付 → 项目工程成本；已交付 → 长期运维成本。

---

## 8. 定格 vs 现算（最容易搞错的分界）

### 定格落库（历史事实，以后不能变）

| 数据 | 何时定格 | 为什么 |
|---|---|---|
| 应收金额 | Invoice 发出时 | 客户手里那张纸不能变 |
| 结清差额 | 财务批准时 | 后来收到零头，现算会变 0，批准记录就糊了 |
| 账龄起点 | 第一版 Invoice 发出 | 改版不能抹掉 |
| 工程利润率 | 交付当天 | 交付后成本还在动 |
| 是否免责维保覆盖 | 结案时 | —— |
| Google 路程预估 | 查询时（连出发时间一起） | 路况会变，不能拿今天审昨天 |
| 每日超时判定 | 提交时 | 后补工时不改历史判定 |
| **日结工资** | **当夜 0 点** | 含制度、发薪时薪、成本时薪、加班倍数 |
| 盘点账面数 | 盘点那一刻 | 否则"当时差多少"永远还原不出 |
| 文档已发放版本 | 发放时 | 电工签的是哪一版 |
| 离职倒休应结 | 离职清算时 | —— |
| 出库/退库单价 | 出库时 | 喂项目成本 |

### 现算不落库（会变的东西）

距今天数 ｜ 已收/还差 ｜ 超期天数与账龄 ｜ 路上时间 ｜ 每人每天在场/首尾/付薪 ｜
项目人工成本 ｜ 人工消耗% ｜ 工程流水账两个维度 ｜ 库存与在途 ｜ 安装剩余统计 ｜
两套维保到期 ｜ 长期利润率 ｜ 停服天数 ｜ 倒休余额 ｜ 外包实付与摊入差额 ｜
状态停留天数 ｜ 订阅状态与年化

---

## 9. 卡点网与断言体系

### 卡点网（DB 触发器强制，回归 130 次拦截全过）

**售前**：第六步没签 → 不能置"已签署报价"、不能进施工/交付/维护 ｜ 乐观锁

**工程**：SM 顺序（需前序完成或标不适用）｜ 出发前清单适用项全勾 ｜ 安装必须 SM4 之后 ｜
安装完工 = 剩余清零 **且** 负责人确认 ｜ 剩余项取消/挂起必须填原因、转变更必须关联变更单 ｜
SM 完成时必签收文档必须已签收

**财务**：S2 需 SM3 完成 ｜ S3 需 SM4 完成 ｜ Invoice 已发出改金额必须升版本 ｜
不足额结清必须填原因 ｜ 应收为空不能开票 ｜ 移位变更不得有金额 ｜ 已发出 S4 后变更金额锁死

**交付**：S4 结清（**硬卡无后门**）+ 已有 ready 确认 + **客户签收** + 必交客户文档已发放

**库管**：S2 未结清不出库 ｜ 提货人须自己人或本项目参建方 ｜ 提货人须有手机号 ｜
出库不超库存 ｜ 到货不超下单 ｜ 退库不超名下未退 ｜ 拒收三件齐全 ｜
一个物料只能做一次期初 ｜ 已划掉物料不再被调用 ｜ 有流水的物料不许彻底删除 ｜
盘亏必须填原因、超审批线须审批且**盘点人不能自批** ｜ RMA 顺序硬卡

**运维**：维护单只受理已交付项目 ｜ 现场三样（归因+服务内容+客户签字）缺一不能完成 ｜
人没去过不许开票 ｜ 归因未填不许结案 ｜ 拒付停服后任何新任务派不出去 ｜
订阅只能建在已交付项目、第5档不得有年费、1~4档必须有年费

**工资**：日结定格后不可改 ｜ 挂起放行须负责人补录+说明 ｜ 入职前离职后不生成日结 ｜
月薪制不填成本时薪拒绝建人 ｜ 外包不填参考时间拒绝建人 ｜ 加班倍数不许低于 1 ｜
离职清算实付与应结不一致须填原因

**账号**：第一个账号必须是核心管理员 ｜ 管理员上限 3 ｜ 核心管理员唯一且不能停用自己 ｜
只有核心管理员能建/停用管理员 ｜ 部门账号只能由管理员建 ｜ 施工人员只能由工程管理建 ｜
施工人员不得有后台权限 ｜ 管理员必须有邮箱 ｜ 管理员与施工人员不得挂部门 ｜
只有管理员可自主找回

**项目归属**：通知/交接/留痕/报销没项目必须显式标 company 或 untraceable ｜
问题上报必须落项目或标公司级 ｜ 报销必须有收据、**不能自己批自己**

**例外的三个正规后门**（都是"手动标记 + 填原因 + 留痕"）：
`na_flag` 标不适用 · 无单派工 · 不足额结清（差额定格）

### 不变量断言（73 条，`assertion_def` 声明式）

```sql
SELECT * FROM fn_assertion_summary();      -- 一句话结论
SELECT * FROM fn_run_assertions();         -- 逐条
SELECT * FROM fn_assertion_detail('代码');  -- 看是哪几行违规
```

三档：`critical` 有违规 → **不可上线/发版**；`high` → 需修复；`medium` → 提醒。
**加一条断言只加一行数据，不改代码。**

分组：库存 7 · 财务 9 · 工资 8 · 权限安全 8 · 流程文档 13 · 订阅状态 10 · 项目归属 11 · UI 5

> **门禁问"该拦的拦住了吗"，断言问"没被拦住的，算对了吗"。**
> 实证过：干净数据全过；故意搞坏五处（到货数改小 / 变更漏估 / 付薪时长被改 /
> 视图关掉 security_invoker / 移位填金额），每处都被抓到并报出是哪几行。

---

## 10. 交付物清单

| 文件 | 内容 |
|---|---|
| **contract_v0_30.sql** | 契约本体：60 表 / 84 视图 / 132 条 RLS 策略 / 73 条断言 |
| **KONNEXT_项目决策记录.md** | 本文件——跨对话记忆 |
| **KONNEXT_全流程总览.md** | 业务视角：从售前到长期运维 |
| **KONNEXT_UI界面清单与字段布局.md** | UI 规格：登录、布局、六部门子页面、字段 |
| **KONNEXT_UI原型.html** | 可点击原型（单文件，右上角可切身份看金额遮罩） |
| **KONNEXT_上线清单.md** | 架构、建库顺序、Twilio/Google、验收标准、Go-No-Go |
| **tests/**（26 个 .sql + run_all.sh） | 回归套件：130 次拦截 + 73 条断言 |
| 导入期初库存.py + 期初库存_填写模板.csv | 上线时把仓库现有存货录进系统 |

---

## 11. ★ 当前进度与下一步

### 已完成
契约全线闭合（售前 · SM1~4 · 变更 · 采购 · 库管 · 安装 · 交付 · 运维 · 订阅 · 财务 · 三种工资 · 文档 · 报销）
+ 账号三级 + RLS + 五类金额遮罩 + 部门交接通知 + 73 条断言 + UI 规格 + 可点击原型 + 上线清单
+ **UI 结构手术（2026-07-30 落地进原型；冒烟测试 6 角色 × 111 页渲染零报错）**
+ **售前 UI 雏形（2026-07-30 六轮用户反馈全落地，规则见 §14，待定稿）** + 售前「设置」页（内生提醒阈值）
+ **应用阶段 1 后端**（app/：登录三步·待办·账号 API + konnext_app 降权 + RLS 冒烟，本地实测全绿；页面待移植）
+ **办事指南四件套已交付用户**（Twilio/Supabase/Vercel 注册指南 + 物料盘点 Excel 表）
+ **契约 v0_31（2026-07-30）**：eng_setting 新增 `write_depts` 列——**按键控制写权限**（比整表 ownership 细）。
  加班倍数四键暂全 1.0、开放给**财务**手改（用户定）；新增 `sub_tier1..4_fee` 四键归**运维**填（第5档=不订阅无键），
  配套视图 `v_subscription_fee_standard`（标准价目不遮罩：运维要维护它，且它是档位报价、非某客户合同额）；
  **补上 eng_setting / eng_staff 的 RLS**（此前只登记未强制，写权限全靠前端藏按钮）；
  write_depts 本身仅决策管理员可改（防部门自我扩权，门禁触发器拦）。
  三步规矩全过：零报错 / 130 拦截逐文件与 v0_30 一致 / 断言 73 全过；权限行为 6 场景实测符合预期。
+ **★售前 UI 样板定稿（2026-08-01 用户「基本满意」）**：七天十九轮反馈全落地（§14 为定稿口径与部门页模板）；
  冒烟 6 角色 × 105 页零报错、界面断言全过（大表/状态表/成交图/立项引导/修改抽屉/搜索/设置/待办三动作）
+ **契约 v0_32（2026-08-01）**：售前定稿对齐——6+1 落库口径 / est_quote_low·high / 双注释已读 /
  pre_stage_remind_days + 角色·工种两键 / v_presales_pipeline + fn_presales_reminders_today() /
  to_contact 删除、「留标」全库更名「已流失」。**60 表 / 86 视图 / 140 策略 / 73 断言**，三步规矩全绿
  （建库零报错；130 拦截且逐条为业务门禁；断言除文档记载的建库常态 INV-SEC-03 外全过，建引导管理员后 73/73）。
+ **全局标准件补齐（2026-08-01 二十轮）**：六部门「操作日志」页（真记录·四要素·只读，见 §14）+
  立项建筑四类选项设置化（v0_32 补 4 键并放开硬 CHECK，三步规矩复跑全绿）
+ **财务全线十一页第一版（2026-08-01 二十一轮，用户定架构，台账见 §15）**：
  收款S1-S3 请收结催全流程真操作 + S4与Var 组合结算 + 催账停服双口径 + 工资/报销/设置；
  「留标」→「已流失」清欠账；冒烟 117 页零报错、财务 25 条断言全过。**待用户上手逐轮反馈至定稿**
+ **回归工具归档进仓库**：docs/tools/ui-smoke.js（UI 全量冒烟）+ docs/tools/发布原型副本.py

### UI 结构手术（已做完，结论与落地方式）

**当时的两个结构性问题（用户指出）：**

1. **左侧总览里很多内容重复** —— 同一批待办出现三遍（全局待办抽屉 / 六个部门总览下半部分 / 决策页悬而未决）；项目列表做成了六份，只有一列不同
   - **根因**：按"部门 × 页面"网格填格子，没问"这个人打开这页要干什么"
2. **页面做成了只读展示，不承载操作** —— 报修受理、项目列表这些页要能建单、派工、上传照片、推进状态，不是列表格看

**手术结论（已全部落进 `docs/KONNEXT_UI原型.html`）：**
- **待办只留一处**（全局抽屉）。总览改成"**本部门此刻卡在哪**"——`STUCK` 卡点表：卡点 · 卡在哪/影响什么 · 卡了多久 · 「去处理→」直跳对应页面；项目编号+地址仍是前两列，公司级/无从追溯明确标出
  - 例：财务总览 = "3 条变更待估价、1 个项目未退料没结算、超期应收 214K"（已按此实装）
- **决策页「悬而未决」改成升级看板**——不再抄全部待办，只收达到**升级线**的：超 SLA ｜ 无从追溯 ｜ 卡满一周(168h)；其余明确写"由各部门待办中心跟进"。管理层要的不是队列，是队列里卡死的那几件
- **项目列表全公司一份**（`projectList` + `DEPT_FILTER`）：部门只决定**默认筛选**和**默认那一列**，状态筛选条随时可切；售前/管理员才见「+新建项目」（`v_my_permissions` 原则：无写权限不显示）
- **每页「本页操作」条（`ops()` 组件）**：操作 · 被什么门禁卡 · 操作完触发谁 —— 已装进 6 个已展开业务页（立项 / 签约 / 派工排班 / 出库 / S4 结算 / 业主订阅）与项目列表

### 下一步（2026-08-01 深夜收工更新，★下次开工照此顺序）

1. ~~售前定稿~~ ✅ · ~~契约 v0_32~~ ✅ · ~~财务十一页第一版~~ ✅（§15）
2. **用户上手玩财务页 → 按反馈逐轮迭代至「财务定稿」**（照售前节奏：一轮反馈 → 改 → 冒烟 →
   截图 → 重发原型同链接 → 提交 → §15 追记）→ 契约 v0_33 清 §15 欠账（催款记录表/GST与停服键/待办留痕）
3. 应用阶段 1 页面移植（登录/全局壳/待办抽屉，浅色办公蓝；后端已就绪）
4. 之后按链路：SM1~4 → 出库 → 安装 → 交付 → S4 结算 → 运维订阅

**每页只定三件事**：①谁进来、要干什么 ②能做哪些操作、被什么门禁卡 ③操作完触发谁
（操作与教学一律走 `ops()`/`helpNote()` 进「？帮助」，主页面保持干净）

**等用户交回**：Twilio/Supabase/Vercel 账号（指南已发）· 物料主表与期初盘点 Excel（表已发）

### UI 七条贯穿规则

1. **门禁报错原样透传**（契约里都是完整中文句），不要包装成"操作失败"
2. 按钮可用性由 `v_my_permissions` + 业务前置条件决定，**悬停要说明为什么禁用**
3. 金额为 NULL 显示 `— —`，**不显示 0**
4. 定格数据只读，**并写明什么时候定的**
5. **只查视图，不查基表**
6. 跨部门动作完成后显示"已通知谁"
7. **★ 待办以项目编号 + 项目地址开头**

### 页面分两类，不能混

| 类型 | 例子 | 顶部显示 |
|---|---|---|
| **项目内页面** | SM · 安装 · 交付 · 维护 · S4结算 · 出库 · 订阅 · 立项签约 | **项目上下文条**：项目下拉 + 状态 + 📍地址 + 业主/Builder/电工 |
| **公司级页面** | 物料主表 · 汇率 · 库存流水 · 盘点 · 人员 · 每日上报 · 账号 · 决策看板 | 灰条注明「公司级页面 · 不属于任何单个项目」 |

> 混在一起做全局项目选择器，会让人误以为**库存也是分项目的**。
> 每日上报尤其要注意：它挂"人+日期"，**一人一天跨多个项目**，本质是公司级。

### 部署阶段（不需要新表）

Twilio 接入（★最急，没它谁都登不进）｜ Google Maps 实接 ｜ 2027 公共假日录入 ｜
加班倍数按 Award 填 ｜ 黄金测试集（20–50 真实案例）

---

## 12. 待确认的开放项

| # | 事项 | 阻塞什么 |
|---|---|---|
| 1 | **Twilio 账号 + 澳洲手机号** | 登录功能（阶段 1 就要） |
| 2 | ~~加班倍数~~ **已解**（2026-07-30：暂全 1.0，财务系统内可改；v0_31 开权限） | — |
| 3 | ~~订阅五档年费标准~~ **已解机制**（v0_31 建 sub_tier1..4_fee 键，运维系统内填数） | — |
| 4 | **物料主表初始数据 + 仓库盘点结果** | 采购与库管全线、期初移库 |
| 5 | Supabase 连接串（生产+预发） | 部署 |
| 6 | **兼任风险**：采购+库管同一人时，到货确认与盘点审批是否要求他人复核 | 内控 |
| 7 | Google API Key（可延后） | 路程预估，不影响开门 |

### 明确不做
❌ 设备台账（维保只查 S2 收款日期）
❌ Variation 争议追溯（交给人，系统只摆事实）
❌ 报价系统 API 对接（保持手填）

---

## 13. 工作方式约定

- **一次一个问题**，用户用大白话答，Claude 分类确认（归到"字段 / 关系 / 卡点 / 报表 / 权限"）
- 先理关系和卡点，再填字段
- **每版 DDL 都真跑 PostgreSQL 验证**（故意违规测门禁）
- **门禁测试 + 不变量断言，两者缺一不可**——门禁问"该拦的拦住了吗"，断言问"**没被拦住的，算对了吗**"
- **回归套件必须覆盖全部门禁**，不只是本版新增的
- **每个测试文件必须自给自足**——套件给每个文件建独立库，测试间不能有隐式依赖
- **能自动扫全库的地方绝不手列清单**（手列一定会漏且不报错）
- **报数字前先看报错内容**，不只数条数（拦截次数会骗人：环境错误也计一次）
- 字段可后补（JSONB），关系和门禁要慎重
- Claude 不在对话外工作、不跨对话记忆（靠本文档）、不能部署/连库

---

## 14. ★售前 UI 样板（已定稿 2026-08-01 · 十九轮用户反馈全落地）

> **定稿记录**：2026-08-01 用户原话「售前的UI样板我基本满意了」→ 本章即定稿口径，
> **是后续所有部门页面的模板**；再改动须用户明确提出并在此追记。

> 本章是后续所有部门页面的**模板**。原型：`docs/KONNEXT_UI原型.html` → 售前大表。

### 结构
- **整个售前 = 一页长大表**；导航（十一轮定名）：**项目列表**（=大表）· **项目状态表** · 设置
- **项目状态表**（十一轮加）：上半左**扇形图**=在途项目（非签约非流标）状态占比（有序阶段=同蓝系浅→深，
  逐片直标+旁表数量兜底，调色板过验证脚本）；上半右**堆叠柱**=所选时段立项去向（一根柱三段：
  绿=已签约 #047857 · 红=已流标 #dc2626 · 蓝=待签约在途 #2563eb，周/月/季/年方块切换，段间白隔+直标数量，
  三色过 CVD 验证）；下半「全部+6+1」状态过滤，过滤出的表格与项目列表**完全同款可操作**（共用 preBigTable）
- **近 12 个月逐月柱 = 只一张「每月立项项目成交图」**（十四轮两张 → 十八轮用户定删掉「新立项数量（市场增长）」单柱图）：
  三段柱（绿签约/红流标/蓝在途），**柱顶数字 = 当月新立项总数**（没有就是 0，空月柱显 0）——一张图同时看增长与转化；
  **去向永远回填到立项月份的柱上**（十七轮定）：26-05 立的项哪怕 9 月才流失，红段也记在 26-05 那根柱。
  项目持续多月也只出现在立项那个月，**不是积累量、不重复计数**（标题与帮助明示）；
  当前月加粗、悬停各段；**柱高按当档最大值自适应等比缩放**（十五轮定：数值再大也不溢出面板）
- **图表联动 = 全部现算**（十三轮定，十七轮改口径）：扇形/去向柱/两张月度图**全部由项目数据现算**
  （立项日期 + 当前状态；模拟历史与「改动算今天」的增量补丁已删）——改 6+1 勾，成交图在该项目**立项月份**
  的柱上换色、别的月不动；立项建档当月柱 +1；周/月/季/年去向柱同样按立项日期取窗口。
  与正式系统同口径：按 `project.created_at` 归月分组、按当前状态拆段，现算不落库
- **默认只显示四大项**：项目 ｜ 售前状态 ｜ 预测报价 ｜ 签约（单号·工时·定价·定金）——
  其余列组（联系人/关系人与佣金/房屋/注释）用**开关按钮**调出；City / State 两个下拉过滤
- 地址列：**黑体 = 客户项目昵称**，灰字地址**带州缩写**；前两列冻结，其余横向滚动；列组间 2px 分隔线
- **实时搜索（逐字刷新）**：编号/昵称/地址（含 City/State）一框全搜，打一个字过滤一次；全公司项目列表同款——大表类页面标准件
- **立项时间列 = 编号后标配**（十六轮定；**二十六轮升级为三件套**：编号 · 立项时间 · **昵称+完整地址**，
  凡带编号+立项时间的表一律跟上昵称与完整地址——操作日志/卡点表/催账/运维支持等五处已补齐）——
  售前大表进冻结区（跟编号一起钉住）、全公司项目列表、总览卡点表已同步；立项引导完成时自动落当天；
  对应 `project.created_at`（契约已有字段，不新增欠账）

### 售前状态 = 6+1 个勾（全部手填，系统不猜）
- 顺序：**接洽中 → 设计中 → 报价中 → 修订中 → 待签约 → 已签约**；第⑦勾（红）= **已流失**（流失=流标同一回事，勾上即终态整行归档变灰；原「留标」并入已流失）
- **勾到哪 = 处于哪**：最后一个勾即当前状态（蓝高亮）；已签约只亮 6 个勾；每勾**定格日期**（悬停可见）；勾下 45° 斜排状态名
- **勾可点选且支持反选**（十轮定）：点下一格=推进；**点当前格=反选回退一步**（人为点错可回退）；点更早的格=直接退回；
  第⑦勾点一下流失、再点取消；已签约不可勾流失（作废走财务流程）；「接洽中」为起始态不可取消。
  **任何改动停留清零、当天重新计时，提醒跟着重算**——点错造成的偶发不做特殊处理（用户定）
- 「停留」列 = 当前状态 pending 天数；**勾「已签约」→ 自动通知财务开 S1 定金 Invoice**（整条链第一条通知），其余勾不惊动别的部门

### 三个填写时点（严格分开）
1. **预测报价（范围，低–高）**：立项引导第 5 步填 —— 立项阶段没有真报价，无版本概念（quote_version 废弃）
2. **报价单号 / 预测人工工时 / 签约定价**：三样在「**待签约**」阶段一并填（预测工时=人工消耗率分母）
3. **定金**：只读列，财务写入后亮绿

### 立项与子表
- **立项完成 = 真建档**（十五轮定，用户实测发现原型只走过场后改）：第 5 步「完成立项」即刻在大表顶部插入新行
  （编号自动取现有最大 +1、状态=接洽中、立项时间=当天），扇形/当月新立项柱同步 +1；正式系统对应写 `project` 表
- **立项 = 大表上的按钮 → 5 步引导**：①基本与地址 ②联系人（每人五项：姓名/昵称/电话/邮箱/角色）与佣金（**佣金永远跟关系人同组**）③家庭成员+参建方 ④房屋（9 选）/施工阶段（含地下室、其他+说明）/楼层×5+屋顶/免责维保 ⑤方案倾向（有无 KNX/地暖）+预测报价范围
- **子表走行内「补全」抽屉**：大表只显示个数角标（参建方 2 · 成员 4 · 方案✓ · 楼层✓），**未填标黄**；
  参建方与方案倾向是**下游硬依赖**（出库提货人/Builder 告知/SM 电工签字/SM2 带线清单）
- **子表内容在大表直读**（2026-07-30 八轮补）：参建方（工种·联系人）→「联系人+参建方」列组末列；
  楼层·屋顶 →「房屋+楼层」列组末列；家庭成员 → 独立列组「成员构成」——格子里「改」进补全抽屉，未填标黄
- **下拉选项不写死**：联系人/关系人**角色**与参建方**工种**的选项列表由售前「设置 → 下拉选项管理」维护，
  加删即时生效（大表与立项引导同步启用）；删除选项不影响已填过它的历史数据 —— 落库对应两个配置键（v0_32）
- **双注释**：行尾财务注释+工程注释（列组里），售前填 → 对应部门必读（已读✓/未读 角标）；
  **多行大框**（小字 textarea，可拖右下角加高），**鼠标悬停弹浮窗显示全文**（九轮定）
- **「修改」抽屉（原「补全」，九轮更名）**：里面**全部可编辑**——联系人五项每格输入框、
  家庭成员/参建方每行可删（红✕）可加行、方案倾向与楼层全下拉；三个联系人角色行固定（内容可改）。
  原则：**凡展示即可改、凡列表可增删**（只读必须显式说明原因，如定金=财务写）

### 内生提醒（售前的提醒不来自外部）
- 只来自前六步流程本身：任一步**停留达到阈值（默认 3 天）→ 每天提醒一次**，直到推进或流失；已签约/已流失终态不提醒
- 阈值在售前「设置」页**自行修改**（改完大表停留橙线即刻联动）；频率固定每天一次；改动留痕
- 待办抽屉里售前条目来源统一显示「售前流程」

### 教学收纳（全系统规则）
- `ops()`（操作·门禁·触发谁）与 `helpNote()` 只登记不上屏；左栏「？帮助」弹层显示**当前页**的操作表+说明+通用规则——**主页面只留干活的东西**

### 待办中心（十九轮 · 全局标准件，六部门通用）
- **每条三动作**：**已知悉**（停止这条通知，正式系统留痕）｜ **处理**（跳到对应页面并**切到该项目**——
  售前跳大表并按编号过滤出该行，其他部门切项目上下文进目标页 p）｜ **搁置**（保留通知，标「已搁置」淡显，之后照常在）
- **已知悉只对当前状态有效**（用户定）：项目状态一改（点勾推进/回退/流失/取消流失），标记即清除，通知按新状态重新启动
- **每天提醒只更新同一条，不累积**；排序**按超期时长降序**——超得越久越置顶，未到期的沉底（抽屉顶部三条规则明示）
- **超期/等待/SLA 时长自动用 年/月/天/小时 表述**（取前两级：月=30 天、年=365 天），不再出现裸小时数
- **售前条目与 SLA 全部现算**（用户纠错：设置改 4 天仍显 72h 是错）：由大表停留天数 + 设置阈值 preRemindDays 生成，
  不写死——阈值一改，条目增减、SLA 展示、超期计算立即同步；点勾推进→停留清零→条目消失，再停留到阈值→重新出现

### 操作日志（二十轮 2026-08-01 · 全局标准件，六部门每部门一页）
- **左栏每个部门都有「操作日志」页**，内容=**本部门相关**：①本部门自己的每一步操作
  （状态勾推进/回退、立项建档、大表行内直改、设置改动、待办三动作）；
  ②**其他部门确认时引起的状态改变**（财务写定金→售前定金列亮绿、对方部门已读注释、开 Invoice 回填、
  SM 完成触发交接……）——凡是改了本部门页面上看得见的东西，日志必有一行
- **每条四要素**：什么时间 · 谁（部门）· 操作了什么 · **改了什么（前值 → 后值）**；最新在最上；
  项目编号后固定跟立项时间列；公司级改动（设置等）标「公司级」
- **只读**：不能改、不能删，管理员也只能看；正式系统数据源 = `audit_log` 表（契约已有，触发器写 before/after）
- 页内：逐字搜索（编号/人/操作/内容一框全搜）+ 按操作人部门 chip 筛选；帮助弹层有本页功能总结（二十轮用户定：帮助=页面功能使用总结）
- **原型即真记录**：点勾/建档/改值/改设置/待办动作立即入日志（演示与正式同口径）；
  售前设置页原「改动留痕（演示）」面板改为指向本页
- 大表行内直改借此轮全部接上保存与留痕（preEdit：预测报价/报价单号/预测工时/签约定价/房屋三下拉/双注释，
  注释一改对方部门自动置回「未读」）

### 立项建筑选项设置化（二十轮补，用户定：立项时建筑相关下拉的增减也进设置）
- 售前设置页「立项 · 建筑选项管理」：**房屋类型 / 施工阶段 / 楼层用途 / 屋顶** 四类列表可增删，
  改完立项引导第④步、大表「房屋+楼层」列组、修改抽屉立即用新列表；增减动作进操作日志
- 契约侧：`pre_house_types / pre_build_stages / pre_floor_uses / pre_roof_types` 四键（write_depts=presales），
  同时**放开 project.house_type / build_stage / roof_type 的硬编码 CHECK**（build_stage 保持必填）——
  选项即配置，不再是 DDL 枚举

### 契约对齐欠账 —— ✅ 已由 v0_32 清账（2026-08-01，三步规矩全过：零报错 / 130 拦截 / 73 断言）
- ✅ 七步 ↔ 6+1：step1..6 = 六个勾的定格日期（列注释对齐），第⑦红勾=已流失=`bid_lost`；
  `to_contact` 从 status 枚举删除；全库中文标签「留标」→「已流失」（21 处，enum 值不动）
- ✅ quote_version / quote_status 删除；新增 `est_quote_low/high`（含 low≤high CHECK）
- ✅ `pre_stage_remind_days` 键（write_depts=presales，默认 3）+ `fn_presales_reminders_today()`
  每日提醒生成函数（一项目一条不累积；状态一变 current_step 变，自然重新出现）
- ✅ 双注释六列：note_finance/note_eng + read_by/read_at 已读回执
- ✅ 下拉选项键共六个（JSON 数组，售前设置页维护）：`pre_contact_roles` / `pre_party_trades`
  + 二十轮补 `pre_house_types` / `pre_build_stages` / `pre_floor_uses` / `pre_roof_types`
  （并放开 house_type/build_stage/roof_type 硬 CHECK，build_stage 保持必填；重跑三步规矩仍全绿）
- ✅ 新视图 `v_presales_pipeline`（85→86）：售前大表数据源——当前勾/停留/提醒全现算，
  金额按 fn_can_see_customer_amount 遮罩；工时按 fn_can_see_margin
- 立项时间 = project.created_at（本来就有，视图暴露为 opened_on）

### 新欠账（后续轮次做，不阻塞财务线开工）
- 待办中心三动作的**留痕落库**（已知悉/搁置状态表 + 状态改变自动失效）→ 做待办后端时一并
  （dept_handoff 已有 acknowledged_at/by 可复用；售前内生提醒的 ack 需新表）
- 全公司项目列表的「留标」筛选chip与状态字样 → 财务线轮改为「已流失」（DB 已统一，UI 演示数据待跟）


---

## 15. ★财务 UI（已定稿 2026-08-02 · 二十一~三十六轮用户反馈全落地）

> **定稿声明**：财务十一页（财务节点 / 项目列表 / 项目收款S1-S3 / 尾款结算S4与Var / 运维财务支持 /
> 催账和停服 / 工资数据 / 项目报销 / 成本与利润率 / 操作日志 / 设置）全部过完用户反馈，
> 2026-08-02 用户宣布进入下一个部门。**本节与 §14 并列为后续部门页面的模板。**
> 终验：UI 冒烟 6 角色 × 117 页 · 0 报错 · 0 断言失败；契约 v0_33 三步规矩（零报错 / 134 拦截 /
> 断言 72/73 过，唯一例外 INV-SEC-03=空库无核心管理员，上线清单 2.3 建号即过）。
> 契约欠账 → v0_34（见本节尾部清单），进下个部门前或并行清账。

> 架构 = 用户原话定的十一页；**定位（用户定，所有文案都要体现）**：
> **真正的财务系统在外部、与本系统完全不关联** —— 这里只做流程管理控制、帮财务获得/管理信息、提高效率。
> 「标记已请款」= 外部系统已开出 Invoice 之后在这里登记，应收此刻定格。

### 左栏十一页（用户定的分法）
财务节点 ｜ 项目列表（全公司标准件）｜ **项目收款S1-S3** ｜ **尾款结算S4与Var** ｜ 运维财务支持 ｜
催账和停服 ｜ 工资数据 ｜ 项目报销 ｜ 成本与利润率 ｜ 操作日志（全局件）｜ 设置

### 项目收款 S1-S3（旗舰页，照 §14 大表模板）
- 一行=一个签约项目：冻结区（编号+立项时间+昵称地址）→ 付款人（姓名+电话）→ 签约价 →
  S1/S2/S3 三组（应收🔒 · 状态 · 已收/还差 · 收款时间 · 催款）→ 当前节点动作 → 明细▸
- **应收=签约价×10/40/40 自动算**；「标记已请款」即定格（🔒 日期·版本，改=v+1 重开）
- **门禁双向**：S2 按钮被 SM3 卡、S3 被 SM4 卡（disabled+悬停原句）；反向 S1 结清→工程可进场+售前定金亮绿、
  S2 结清→采购可下单+库管可放货（结清动作弹「已通知谁」）
- **收款流水**：一笔可多次、现金/走账、GST 单列自动带出（设置里改默认率，报税以会计为准）；
  收了不够系统自动判「部分到账」；**结清只能财务点**，不足额必须填原因（prompt 拦）差额定格
- **催款**：一键发付款人短信，次数+时间+谁催的留痕
- **通知与确认**（明细▸ 展开）：每条通知列出接收人 chips —— 外部付款人=短信回 YES、内部=待办已知悉；
  未确认可手动「标记已确认」，全过程进操作日志
- 顶部双图：饼=在途项目按当前节点分布（含「等 SM3/SM4」）；柱=每项目 已收(绿)/未收(蓝)/超期未收(红)

### 尾款结算 S4 与 Variation
- 变量**一路积累**随时看：计费 N 条 A$X ｜ 待估价 M（红）｜ 不计费 K（灰）；未退料单列+「结算未退料」真操作（转正式变更）
- **S4 组合一眼看清**：尾款(10%) + 变更 + 未退料 = 应收（发出前现算浮动、标记请款即 🔒 定格）
- 付款人信息卡（姓名/电话/邮箱）就在行内 —— 最短时间去外部系统发 Invoice
- Var 明细：**系统算价 vs 财务定价并排**，财务可改但必须填依据（谁/何时/依据留痕四件套）；
  移位=整改不计费（填金额直接拒原句）；S4 发出后全部锁死（改=开 v2）
- 门禁：待估价没清完/未退料没结算 → 「标记 S4 已请款」禁用（悬停原句）

### 其余页要点
- **财务节点**：应收未收/近30天已收(走账·现金拆)/在途项目数/封账日 四卡全现算 + S1~S4 节点分布 chips + 卡点表
- **运维财务支持**：只服务维护阶段项目；待定价队列（归因+时长定价）→ 开票 → 收款结案；免责期内按归因免单
- **催账和停服**：合同款+维护单在收一屏；**本期超期**(最后版+30天,催账用) 与 **账龄**(首版起,改版抹不掉,管理层用) 双口径；
  拒付→confirm→项目级全面停服→90 天提醒停服务器
- **工资数据**：真发工资在外部系统的定位条；人员薪酬(财务写)/日结🔒定格+挂起卡封账/21日汇总单(给外部工资系统用)/倒休现算
- **项目报销**：柱图=各项目已批报销+**占项目成本%**(>1%变橙)；饼图=类别分布；无从追溯红牌不能批；
  不能自己审批自己（原句拦）；无收据拒收
- **设置**：超期天数/GST 默认率/停服倒计时/加班倍数四键(≥1 门禁)，全部改动进操作日志
- 顺手清欠账：全公司项目列表「留标」→「已流失」+ 新增「施工中」筛选（0188/0203/0170 三个演示项目）

### 二十二轮（2026-08-02 追记）
- **付款人机制（用户定）**：候选=售前收集的 联系人1/2 · 干系人 · Builder · 电工（BIGPD 有的项目动态取，
  Builder/电工从项目档案并入）；财务在每项目付款人处下拉选定 → 点保存全线生效（S1-3 表 / S4 页 / 通知抬头）；
  信息全息显示：**姓名 · 电话 · 邮箱 · 公司 · title（有则显示）** + 来源小标；换人留痕（前→后 进操作日志）
- **昵称与地址口径对齐售前定稿**：粗体位置一律=客户项目昵称（林宅/罗宅），地址一律显示完整
  （street, suburb state）——收款表 / S4 表 / 公司项目列表 / 成本利润率页 / 财务节点清单全改
- **财务节点分布框=过滤器**：S1~S4 节点框可点选 + 「全部」，点选后下方清单只显示该阶段项目
  （编号/立项时间/昵称地址/付款人/该节点应收已收/去处理→）
- **售前财务注释进 S1 流程**（用户定）：注释直接显示在收款 S1-S3 项目行内（黄底小块+悬浮全文，
  未读可「标已读」落回执→售前侧角标变已读✓）；财务 S1 待办改为**内生生成**（S1 未结清自动出条目、
  附售前注释、结清即消失）

- **工资数据=数据平台（三十一轮用户定，2026-08-02）**：真正发工资在外部系统，本页做整理与总结。
  核心=**项目视角人力消耗表**：范围＝施工中/项目交付/项目维护中；列＝三件套 + 售前预计人工（签约「预测工时」）·
  交付已消耗人工（超预计标红＝吃利润率不挂客户）· 交付前人工成本 · 维护已消耗人工；每行可展开**每人明细**
  （个人总工时 + 人成本＝逐日工时×当天**阶段时薪**累计——调薪只影响之后的天，已定格日结不回溯）；
  已消耗＝日结定格逐日加总，今天的段未定格明天才计入
- **日结定格与补录（三十一轮提出、三十二轮定稿）**：当夜 0 点定格，定格后**本人不可自改**；
  **补录入口在工程端「每日上报」页**（用户指正：工程负责人打不开财务页面——财务页里放补录按钮是权限死路）：
  挂起（无记录）→ 系统给**工程负责人**发待办提醒任务 + 总览卡点指路 → 负责人在「每日上报」补录，
  弹窗四样缺一不可＝**项目（含公司级选项）+ 付薪时长（0=定案那天没出工）+ 原因类别
  （忘打卡/围栏GPS异常/设备问题/临时外派/其他）+ 详细说明（≥10 字，写清那天实际发生了什么）**；
  补录即定格、金额按当天阶段时薪现算、挂起与补录**永久进 KPI**、通知财务；
  **财务侧「工资数据」只显示状态**（已补录琥珀标，悬停看原因；挂起行指路每日上报）——财务点补录直接门禁原句拦；
  **补录账目进入下一个月的工时计算**（滚下月补发）——**挂起天当月按 0 计、不卡封账**；
  周期网格补录数字带 ° 角标；日结近况**项目列用昵称+完整地址、不用编号**；
  新增「**本周期全员日结**」按钮：上个 21 号 → 今天的全员网格（数字=定格工时/°=补录/挂=红/休/⏳今天未定格），
  每月 21 号自动滚动，历史周期正式系统按月归档
- **「生成汇总单」按钮删除（三十三轮用户定）**：汇总＝日结定格逐日加总，**现算随时最新，不需要「生成」动作**
  ——20 号过完所有日结已定格，表自然定型；**21 号自动滚动即封账，无需手动操作**；
  面板更名「月度汇总 · 外部工资系统发钱参考」——发钱在外部系统操作，本系统不碰钱、不再存一遍汇总
  （与倒休「存流水不存余额」同一原则：可推导的数不落第二份）
- **项目报销闭环（三十四轮用户定）**：只做**上报与审批**，**付款不走本系统**（无「已付」状态，
  怎么付钱线下自行安排）；类别正名只有三个：**材料补购 · 交通递送 · 工具采购**（其余一律外部财务）；
  提交端两个入口＝**施工端 App「报销」屏**（**拍收据必填**——没照片提交不进来；项目下拉默认带出当天派工项目，
  防「忘写哪个工地」；含 公司级 与 ★无从追溯 选项）+ 内勤后台入口；
  收据以**图片链接**入库：财务点「📷 查看」弹层预览审核，**批准/驳回按钮就在图旁边**，
  弹层「⬇ 下载到本地」随时可下、已批准行另有「⬇ 收据」快捷链接（存档/交外部会计）；
  演示收据=SVG 现生成（正式系统=手机拍照原图的存储链接）；原有门禁不变：无照片拒/不能自批/无从追溯先确认
- **成本页项目分布地图（三十五轮用户定）**：成本与利润率页尾新增地址分布图——**色点颜色=利润率四档色**
  （与表格同配色，灰=不显示/参考），**点色点弹窗显示 项目昵称+完整地址+利润率+状态**，再点一次或点空白关闭；
  原型=按真实经纬度等比落位的自绘悉尼上北区示意图（Artifact 沙箱屏蔽外部请求，Google 瓦片进不来），
  **正式系统接 Google 地图**（与「路程预估」共用一个 API Key，部署时配置），色点/弹窗交互同款；
  地图只当导览、数字以上表为准；维护净额有正有负（+390/−180 两例都在演示里），本页其余用户认可
- **操作日志与帮助收尾（三十六轮用户定，定稿轮）**：①操作日志=财务自身操作全留痕（改数据必 opLog，审计过）
  ＋**上下游联动事件**（售前勾已签约→通知开票、SM3 完成→S2 待办、库管未退料计提/出库计入成本、
  运维上门完成→定价待办、工程报销提交——种子+实时都有）；②九个财务页面帮助最前面统一加
  **「这页怎么用」总结**（操作方式+逻辑+完成后触发谁，教学口吻）；③设置页不变；
  ④**全站左栏「？ 帮助」的「？」删除**（用户嫌怪）——六部门统一改「帮助（本页说明）」
- **倒休休假（三十一轮用户定）**：人员表有倒休余额的人亮「休假」按钮 → 弹窗填 从→至（年月日时）→
  确认即给员工发短信；**回 Y 系统自动扣减**倒休余额；**不回复不影响假期本身——财务可强制扣减，红字留痕**；
  倒休不能透支（拦）；流水只增不改、余额现算（与既有倒休口径一致）
- **工资人员管理进设置页（三十一轮用户定）**：财务在「设置」添加/停用发放工资人员——字段：
  姓名/手机（登录+短信确认）/制度（月薪|时薪|外包日薪）/基准/成本时薪（**不填建人直接拒**，门禁）/入职/状态；
  **删=停用留痕**（有日结历史不物理删，历史工资与项目成本照算）；添加后正式系统同步建施工端 App 账号（三级）；
  增删改全进操作日志并通知工程管理
- **录人必带邮箱栏（三十一轮用户定，售前全局件）**：立项引导与修改抽屉里**凡录人员处都有邮箱栏，选填不强制**
  ——家庭成员、参建方（Builder/电工的邮箱进付款人候选与催款邮件）本轮补齐；联系人1/2、关系人原本就有
- **注释展示件（二十三轮定）**：财务侧售前注释=大框小字**固定两行**（截断不破格），
  悬停弹全文浮层（与售前大表 notepop 同款），「标已读/已读✓」固定在框下右侧
- **「全部」=真全部（三十轮修 bug）**：过滤进来后点「全部」类筛选钮，连预填的搜索词一起清
  （收款页 finF / 项目列表 pjFilter 含财务相关 / 售前状态表 stFilter 三处）——不用退出页面重进
- **去处理=直达过滤（二十九轮用户定）**：财务节点（分布清单+卡点表）与各部门总览卡点表的
  「去处理→」一律 goPj 跳转并**预填搜索只显示该项目**（不要全列出来让人找）；
  S4与Var 页与催账和停服页借此补上逐字搜索标准件（原来没有过滤能力）
- **原生弹窗全面替换（二十八轮修根因）**：Artifact 沙箱屏蔽 alert/confirm/prompt → 用户「催款点了没效果」
  的根因；37 处 alert→uiAlert（右下角 toast，4 秒自动消失，门禁原句照透传）、confirm/prompt→uiConfirm/uiPrompt
  （页内弹层回调式：停服确认、不足额结清原因、报销驳回原因）——**原型今后禁用原生弹窗**
- **催款弹窗（二十八轮用户定）**：点催款 → 弹窗选 渠道（只发短信/只发邮件/短信+邮件）· 语言（中文/English）·
  发送人（默认当前登录人可改）· 内容按模版渲染预填**可临时修改**（只影响本次，默认模版在设置改）→ 确认发送；
  每次催款记录 渠道/语言/发送人/级别 进下拉明细；**对外内容一律不含项目昵称**（用完整地址标识）；
  **短信可回 Y 作确认、邮件发出即视为送达无需回复——都只是记录，不回不影响任何进展、随时可再催**
- **催款模版搬进设置页（二十八轮）**：常规/最终 × 中文/English 四套 ×（短信正文/邮件标题/邮件正文），
  英文参考模版内置；变量图例与实时预览随迁；催账页尾改为指路链接
- **催款分级与模版（二十七轮用户定）**：**烂尾项目不再关联催款**（催款按钮换「烂尾·不催款」标，
  finRemind 拦截——走拒付停服与法务）；催款两级：**常规**（收款S1-S3 / S4与Var 页内发）·
  **最终催款**（催账和停服页专属，红边按钮，语气升级+「7 日内付清否则停服」警告）；
  **催款模版编辑器**（催账页尾）：常规/最终两级 tab × 短信正文/邮件标题/邮件正文三框，
  16 个系统变量自动抓取（{付款人}{项目昵称}{项目地址}{项目编号}{节点}{节点名}{应收金额}{已收金额}
  {还差金额}{Invoice号}{Invoice日期}{版本}{超期天数}{催款次数}{公司名}{联系电话}），
  实时预览+发出前弹渲染结果确认；保存全财务生效并留痕；阶段差异先用变量适配，
  需完全独立时正式系统按阶段加模版覆盖行（v0_33 模版键）
- **三件套标准列（二十六轮用户定，全局件）**：凡显示 项目编号+立项时间 的表格，一律跟**昵称+完整地址**
  （粗体昵称 + 📍 street, suburb state）——本轮补齐 操作日志（六部门）/ 两张卡点表 / 运维财务支持 / 催账和停服 五处；
  公司级行显示「不属于单个项目」、无从追溯显示「需人工确认」
- **运维财务支持·成本核算（二十五轮用户定）**：每单显示只读现算成本——**人工**=（在场+路上）×该员工
  成本时薪（工时来自打卡系统）；**物料**=维护出库的更换件从**库管出库台账自动归集**（出库单价已定格、
  关联维护单，能拿到、不手填）；Σ成本+开票毛利参考；待定价输入旁给「成本参考」；免单成本照记入维保成本；
  在保物料 RMA 索赔回冲；月度维护成本卡（人工+物料）。财务可见性补齐 stock（对齐五类表：库存成本含财务）
- **项目报销口径（二十四轮用户定）**：本系统只管**项目管理报销**，类别仅 **材料 / 工具 / 交通递送**
  （餐费办公等其他费用走外部财务）；报销表带**项目昵称+完整地址**列（光编号认不出来）
- **未退料来源讲清（二十四轮用户问后补）**：未退料**不是财务手填**——库管出库/退库台账现算
  （S2 出库单价定格 × 领走未退数量），列头 ⓘ、门禁弹窗、帮助三处都写明来源与操作路径
  （点本行【结算未退料】按出库价转正式变更）；空值显示「未退料（库管未填写）」提醒——
  未填写按 0 处理不挡 S4，但提示开票前与库管核对；用户确认口径无忧
- **财务项目列表口径（用户定）**：默认视角=「财务相关」（施工中+项目交付+项目维护中+已烂尾）——
  接洽中（售前没走完，财务无需参与）与已流失（售前的结论）不进默认视野；**已烂尾必须看**
  （两种来源：拒付之后的结果 ／ 项目做到一半没了也可标烂尾）；「全部」chip 保留（读全部原则不破）

### 契约欠账 —— ✅ 收款线部分已由 v0_33 清账（2026-08-02，三步全绿：零报错 / 134 拦截 / 73 断言）
- ✅ payment_remind_log 催款记录表（渠道 sms/email/both · 语言 zh/en · 级别 normal/final · 发送人 ·
  sms_replied_at 仅记录不做门禁）+ **门禁：已烂尾项目不再关联催款**（P0001 原句；回归 26 号文件盖 4 拦）
- ✅ 催款模版 12 键（常规/最终 × 中/英 × 短信/邮件标题/邮件正文，write_depts=finance，
  默认值=UI 参考文案、不含项目昵称）；按阶段覆盖行留给需要时再加
- ✅ 财务设置键：gst_bank_pct=10 / gst_cash_pct=0 / suspend_server_days=90；
  overdue_days 开放财务共写（UPDATE 置于写权限门禁触发器之前，被自家门禁拦过一次后修正）
- ✅ project 付款人指定六列（payer_source CHECK 五候选 + 姓名/电话/邮箱/公司/title 快照，财务写）
- ✅ 新表三件配套齐：project_scope_registry（经 milestone 追项目）+ table_ownership（finance）+ 4 条 RLS
  （吸取 v0_31「只登记未设防」教训，本次一并补齐）
- **61 表 / 86 视图 / 144 策略 / 73 断言 · 回归 26 个文件合计 134 拦截**

### 剩余欠账 —— ✅ 已由 v0_34 全部清账（2026-08-02，三步全绿：零报错 / 27 文件 150 拦截 / 断言 74/75，
### 唯一例外 INV-SEC-03=空库无核心管理员；63 表 / 86 视图 / 148 策略 / 75 断言）
> 落库对照：工资线（daily_payroll 补录列+门禁、leave_request 休假单、toil 透支门禁）·
> expense 三类 CHECK+删已付 · 三处 email · todo_action_log · variation 工程追加（origin/工时/告知+门禁）·
> 断言 +2（INV-TOIL-01 / INV-EXP-01）。人力消耗视图复用既有 v_project_labor_cost / v_project_staff_effort。
> **★新教训进规矩**：给已有列加枚举 CHECK 前，先查系统函数在写哪些值——origin 差点掐死
> fn_settle_unreturned 的 'unreturned'，是 22 号回归测试抓出来的（多出的拦截必须逐条验明正身）。
> 原清单如下（全部 ✅）：
- 待办三动作留痕落库（十九轮）
- 工资相关键与汇总视图（三十一轮已出 UI 口径，等「财务定稿」一并落）：
  - staff.active 停用留痕 + phone 列（人员不物理删）；人员邮箱一并考虑
  - toil_flow 倒休流水表（只增不改，余额现算）+ leave_request 休假单
    （从/至时刻 · 短信回 Y 自动扣减 · **强制扣减 forced 留痕** · 透支拦截门禁）
  - work_log 日结 0 点定格 + 挂起补录列：backfill_hours(≥0)/reason_cat/reason_note(≥10 字)/by/at +
    门禁（非工程负责人写→拒 · 类别或说明缺失→拒 · 原句透传）；补录计入**下一个工资周期**（滚下月补发）；
    挂起天当月按 0 计、**不卡封账**；提醒任务=工程负责人待办（fn_eng_reminders 并入）
  - v_project_labor 人力消耗视图（按项目按人聚合：交付/维护工时 + 阶段时薪成本；预计工时来自售前 hrs）
  - 阶段时薪：staff_rate_hist（from_date, cost_hr）——日结按当天档取数
- 家庭成员/参建方 邮箱列（选填不强制；参建方邮箱进付款人候选——payer 快照已有 email 列）
- expense 报销（三十四轮）：receipt_url **非空门禁**（无照片拒）+ 类别 CHECK 收窄正名
  ('材料补购','交通递送','工具采购') + **不设「已付」状态列**（付款不进系统，审批与留档为止）
- 工程追加变更（§7.3，**已拍板 2026-08-02：放开+告知短信**）：variation.origin('sm3'|'post_sm3') CHECK +
  labor_hours_est（工程填，只进成本）+ 追加告知记录（notified_at/sms_replied_at 仅记录）+ 交付后不许追加门禁 + 物料传库管出库联动

---

## 16. 工程管理 UI（三十七轮 2026-08-02 起 · Site Meeting 旗舰先行，待用户逐轮反馈）

> 定位：一个部门、两块屏幕——办公室**工程管理后台**（指挥+确认+管理）+ 工地**施工端 Webapp**
> （三级账号只上报，七屏演示已给工程负责人看过）。后台页面按 §14/§15 模板做。
> 已建：每日上报（补录轮）· 操作日志 · 总览/项目列表/派工排班骨架 · **Site Meeting（本轮旗舰）**。

### Site Meeting 页（三十七轮首版，等用户反馈）
- **项目内页面**：顶部项目上下文条（壳标准件：项目下拉+状态+📍地址+业主/Builder/电工）；
  售前阶段项目显示空态「还没进工程线」；页标语＝**S 是钱 · SM 是会**
- **四张卡=四场会**：SM1 进场 / SM2 布线交流 / SM3 检查布线 / SM4 封板核对；
  状态四态＝未排期(灰) / 已排期(青+时间) / 🔒已完成(绿+定格时刻+完成人+照片数) / 不适用(琥珀+原因)
- **出发前清单**：按 SM 模板（设置维护）+ **方案条件项自动增减**（有 KNX 才出 KNX 测试仪、
  有地暖才出地暖清单——罗宅无地暖就只有 3 项）；开会前可点勾，完成后定格只读；
  **清单未勾完只提醒不拦完成**（留给现场判断）
- **完成（定格）**：顺序门禁「SM(n-1) 未完成不能先完成 SMn」（契约同款原句）；SM3 完成需**电工签字**
  （uiPrompt 默认带出项目电工）；完成时刻定格不可改；
  **SM3 完成 → FINPAY.sm3=true → 财务端 S2 按钮当场解锁**（同源联动，两部门看同一事实）+
  通知财务/采购/库管 + todoWake；SM4 同理解锁 S3
- **SM3 现场登记变更**：卡内 mini 表单（类型 加/减/移位 + 描述≥4字）→ 直接 push 进收款线 vars
  （加/减=待估价计费；**移位=整改不计费 amt 自动 0**，财务端填金额同一门禁拒）→
  自动知会 设计/采购/财务；SM3 完成后表单收起，改显示本场变更列表 + 指路「工程追加变更」
- **标不适用**：必填原因留痕（刘宅=老项目四场全 na 的演示）；烂尾项目停在当时场次
- **下游门禁现状条**：S2/S3 被卡（红）/已解锁（绿）+ 变更待估价数——完成 SM 那一刻当场变绿
- 排期→进派工排班与工人 App 今日任务（通知留痕）；改期同入口

### 三十八轮（用户纠正 · 全局口径）：入场离场 = 每人 × 每天 × 每个任务
- **入场/离场打卡是底层通用动作，不属于任何一场会**：SM 会议、安装、交付、维护上门，
  每个任务各打各的（一天两个工地=两组入场离场）；**运维同样适用**；采集永远在工人 App
  （围栏 100m · 拍照兜底带异常标记 · 围栏外需负责人确认 · 定位失败不扣钱但当天挂起）
- 契约本就如此建模（work_log.work_type=sm/execution/handover/maintenance，每条含 checkin/checkout）
  ——是页面没表达出来；SM1 清单「围栏坐标核对」措辞改「围栏圆心首次到场校准」去歧义
- **SM 页每张已完成卡显示「到场打卡（每人 · 本场任务 · App 采集）」**：人名+入离时刻+围栏异常⚠标；
  未完成卡显示采集说明；出勤自动进日结工资与项目人力消耗
- **App 打卡屏改任务维度**：今日任务逐条各有入场/离场（安装在场中/维护未入场两例），
  维护单屏注明「到场先入场、三样齐完成任务后再离场——在场时长=财务定价的工时依据」

### 三十九轮：工程总览重做（用户定五块）
- **在建 / 维护双计数**：两张卡各显示总数 + **其中停留超阈值几个**（红字）；停留＝status_since 现算
- **今日派工按小时**（用户纠正：人次没有意义——一人跑两个工地是两段，小时才算得出人力投入）；
  卡脚注 段数/人数/含外包小时
- **未记录工时**＝挂起待补录天数（点开＝挂起明细 → 去每日上报补录）
- **超期交接**＝交接审批数据：发出超 SLA 仍未确认；明细里**发给工程的可「确认接收」**（留痕通知交出方）、
  **工程发出去的可「催办」**（对方待办置顶）——静默等待等于没交接
- **五块卡片全部可点开明细**（记录都在）；**卡点与异常＝汇总所有要工程去审计/处理的事**：
  停留超阈值 / 交接超期 / 日结挂起 三类**现算派生** + 静态卡点（安装挂起），按卡了多久置顶

### 四十轮：工程设置（阈值统一可调）+ 工程项目列表（用户定）
- **阈值统一进「设置」**：在建停留天数 · **维护停留天数**（契约 status_stall_threshold.maintaining 原为
  NULL 不设阈值 → **v0_35 补默认值 90，工程可改**）· 交接确认 SLA 小时 · 打卡围栏半径米；
  改完总览/卡点/项目列表立即按新阈值现算，改动进操作日志
- **工程项目列表＝工程视角，与全公司共用表分家**（`eng/项目列表` 单独渲染器）：
  - **六态筛选**：全部 / **Site Meeting 阶段** / 施工中 / 项目交付 / 项目维护 / **已停服**；
    每个 chip 带实时计数；范围只收**进了工程线**的项目（售前接洽中/已流失不进来）
  - **「已停服」取代「已流失＋已烂尾」**（用户定）：**由财务端确认款项不到位时触发**
    （财务「催账和停服」标记拒付 → f.susp；含历史烂尾）——该项目任何新任务派不出
  - **工程进度列＝当前阶段**（现算）：SM 阶段带「SM n/4 已完成 · 下一场 SMx（已排期/未排期）」；
    SM1~4 走完＝施工中（安装调试）；交付/维护/停服各自文案
  - **金额一律不显示**（合同额/已收/利润率列全部去掉——工程看进度和人，钱在财务页）
  - **业主 / Builder / 电工 由工程负责人指定**（机制同财务付款人）：候选＝售前采集的联系人与参建方，
    行内「指定」展开三个下拉 → 保存即**定为📌标注**固定在列表（显示指定人+时间），
    留前后痕 + 通知财务（付款人候选）与库管（提货人）

### 四十一~四十二轮：派工排班（用户定，四组需求）
**基础与人员**：人员名单/时薪/倒休**只读引用财务模块**（工资数据+设置维护），排班表不重复建人；
五类任务＝Site Meeting · 安装 · 交付 · 维护 · **倒休**（颜色区分）
**表格与交互**
- **周视图**（周一~周日 × 人）；上一周/下一周/回本周；**周五排下周**
- 同一天同一人可排 **2~4 个 Case**；**任务必须关联项目**（倒休除外——休假不是工作，门禁原句拦）
- 时长**最小 15 分钟**（非整数自动对齐），**≥60 分钟自动按小时显示**（如 90 分 → 1.5 h（1h30m））
- 工时自动算：每天超 8h 部分＝加班 → **月薪转倒休 / 时薪付加班费 / 外包无加班概念**；
  排「倒休」按**倒休余额校验，不足直接拦**
- **内容多用悬停浮窗**看全（项目/内容/剩余工作/合理时长偏差/确认状态）
- **录入一律走浮窗**（用户定）：格子点「＋ 排任务」弹窗填完保存——**不做拖拽**（拖拽不留痕，浮窗每次保存进日志）
**协作与记录**
- **只记「剩余工作」，不记「今日已做」**（单维度锁定人员内容）——今日已做在「每日上报」由工人自己写
- 排到某项目的人**可查该项目此前的工程上报记录**（任务弹层里点进去看历史进度）
**确认与锁定**
- **周五早晨提醒**排下周（页面顶部橙条，每小时一次、**同一条不累积**，锁定整周即停）
- **按天锁定 / 锁定整周**（锁＝已下发，改要先解锁，解锁留痕）
- **只能改此刻之后**：过去格子斜纹禁用，改动报门禁原句
- **通知**：改动短信通知本人；**被取消的人要回 Yes 确认**（取消待确认清单盯着）；被新加的人收日程变更
- **红点 / 绿点（四十二轮）**：短信发出＝红点，本人回 Y＝绿点，一眼看出任务有没有交付到位；
  **★不论回不回，日程都已自动同步到本人 App**（确认只是回执）；全周确认状态清单可重发短信
- **点人名看当天路线（四十二轮）**：按派工顺序标 **A/B/C/D** 站点 + 连线 + 每站路上预估
  （原型自绘示意图，正式接 **Google 地图**，与路程预估共用 API Key）
- **合理时长（四十二轮）**：工程管理在设置里定义 SM/安装/交付/维护/倒休 的参考时长
  （**重点 SM 与交付**，也是跟客户解释工期的依据）；填时长时给偏差提示（超 30%/不足 60%），**只提示不拦**
- **全部增减改动进操作日志**（新增/修改/取消/确认回执/锁定解锁/改阈值改合理时长）

### 四十二轮修 bug（用户报）：工程总览「去处理」未直达
`goPj()` 只预填了旧的 finQ/pjQ/bigQ，工程列表四十轮起改用独立变量 engPjQ/engPjF ——
补上后「去处理」只显示该项目；**点任一状态 chip 都清预填搜索**恢复该筛选完整内容（同财务口径）。
教训：页面换了自己的状态变量，跨页跳转函数要同步；且**跨部门跳转不该污染别部门的搜索词**（已按目标页收窄）。

### 四十三轮：排班「新增任务」浮窗四修（用户报 bug）
1. **倒休只填时长**：倒休＝把余额用掉，不是干活——隐藏「关联项目 / 工作内容 / 剩余工作」三栏，
   保存时也不写这三个字段
2. **关联项目可搜可筛**：上百个项目手选不现实——浮窗内置搜索框（编号/昵称/地址实时过滤，
   列表框式多行显示，**筛到唯一一个自动选中**），下方显示可选数量
3. **工作内容改多行大框**（≥96px，支持分行写清逐条内容），方便自己回看核对
4. **剩余工作改「自动生成」**（★口径变更，不再手填）：来源＝**该项目最近一次每日上报里留下的剩余**，
   只读展示 + 标注来源（谁 · 哪天）。分阶段：
   - **SM1~4**：不存在剩余工作（显示「（SM 阶段不存在剩余工作）」）
   - **施工 / 交付**：自动带上一次剩余；没有上报记录＝「第一次进场」
   - **维护**：默认可空（有时真没剩）；**上一次维护没做完却没填剩余 → 门禁拦下派工**，
     原句提示「请先让上次上门的人在每日上报补填剩余工作」（刘宅演示：上次巡检没填 → 拦）
   - 未选项目时提示「（先选关联项目，系统自动带出上次剩余）」
- **工程师 App 同步**：每日上报屏每段拆成「今天做了什么」+「★还剩什么没做（剩余工作）」两栏，
  并写明规则（SM 无此项；维护没剩写「无」——**上次没做完又没写，下次派工会被系统拦下**）

### 四十四轮：排班改为「开始时间 → 截止时间」（用户定）
- **每段活都有起止时间**：`st` / `et` 落库，**时长＝区间自动算**（不再手填分钟数）
- **开始时间不固定**：上午没活就休息、**10 点才进场**，一直干到晚上——演示里周五小陈
  `10:00–19:45（9.75h）`，超 8h 部分照常算加班（月薪转倒休）
- **截止时间统一 15 分钟一档**（起止都从 05:00~23:45 的 15 分钟档位里选，杜绝 16:20 这种）
- **同一人同一天两段活不许时间重叠**（新门禁：一个人不能同时在两个工地，重叠原句拦）
- 一天多段按开始时间排序显示；格子、悬停浮窗、确认清单、路线表、通知短信、操作日志
  全部带上 `08:00–16:30` 这样的区间；取消/修改留痕也用区间描述

### 四十五轮：Site Meeting 重做（用户定六点）
- **改成「总表 + 详细」，页面升级为公司级**（原来是项目内页面，靠左上角项目下拉切——项目一多没法用）：
  上半部总表＝每个项目一行 × SM1~4 四列，直接写状态（🔒完成带日期/完成人/照片数 · 已排期带时间 ·
  未排期 · 不适用带原因）+ 当前阶段 + **下游门禁列**（S2/S3 解锁或卡住）
- **过滤**：项目下拉一次筛一个 + 逐字搜索（编号/昵称/地址）；「全部」＝所有项目的 SM 状态一览
- **每行「详细」**：下半部展开该项目四张会议卡（出发前清单/现场记录/变更/排期），再点收起
- **现场记录浮窗**（用户要的照片与记录）：已完成场次点「现场记录 →」弹层看
  **现场记录（按模板）+ 现场照片（缩略图，点开大图）+ 到场打卡（每人入场–离场+时长+围栏异常⚠）**
- **排期＝派工**（内容同排班「排任务」）：**新增日期字段**（可排到几周后）+ 起止时间（15 分钟档）+
  **参会人多选** + 会议内容多行 + 出发前清单预览；**项目不用选**（排期本来就在该项目下）；
  保存后**给每个参会人在派工排班表生成一条 Site Meeting 任务**（短信+App 日程，红点等回 Y），
  重叠/过去日期/无参会人都有门禁；SM 侧回写 sched
- **完成定格改由 App 提交**（用户定）：后台不再手点完成——现场工程师在**施工端 App** 填记录+照片
  （SM3 还要电工签字）提交，系统**自动置为已完成并定格**，SM3/SM4 照旧解锁财务 S2/S3；
  本页保留「模拟 App 提交完成」按钮仅供演示（标注正式系统里发生在手机上）。
  施工端 App 新增 **Site Meeting 现场提交屏**（清单确认/记录/照片/现场变更/提交即完成）
- **「标不适用」解释并写进帮助**：系统上线前就在施工的老项目，四场会现实中已开过或不适用，
  但系统里不「完成」的话财务 S2/S3 门禁永远打不开——标不适用 + **必填原因**＝这场会在系统之外发生过，
  留痕可查、门禁放行；**新项目不该用**（刘宅＝老项目四场全不适用的演示）
- 下游「物料款/人工款」提示条按用户意见保持不变

### 四十六轮：安装调试页（用户定四点）
- **只列安装调试阶段**：SM4 已完成、还没交付（engStage==='施工中'）——SM 阶段与已交付/维护中的都不进来
- **一行四个数**（全部现算）：**剩余工作量**（自动取该项目最近一次每日上报留下的剩余 + 来源）·
  **已投入工时**（日结定格逐日加总）· **预计总工时**（售前签约填的预测工时）· **工时占比**＝已投入÷预计
  （+ 剩余工时，可为负并标红）
- **四档配色 + 预警**（本轮设计，阈值可调）：<70% 充裕（绿）· 70~预警线 正常（蓝）·
  **预警线~100% 接近超支（橙，发预警）** · **>100% 已超支·亏损（红，整行浅红底）**；
  预警线默认 **90%**，工程在「设置」→「安装调试 · 工时预警线」自改，改完全页立即重算；
  口径承接 §7.3：**工时超支不挂客户**，吃的是工程利润率
- 顶部四张卡：在装项目数 / 工时超支数（列出是谁多少%）/ 接近超支数 / 合计工时占比
- **资源分配**（交互同 SM 排期）：行末按钮 → 浮窗 日期（可提前几周）+ 起止时间（15 分钟档）+
  **派谁去（多选）** + 工作内容（**按剩余工作预填**）→ 保存**直接写进派工排班表**（每人一条安装任务，
  带上剩余工作），短信+App 日程发出（红点等回 Y）；重叠/过去日期/没选人都有门禁；留痕带当前工时占比
- 演示补两个在装项目：**周宅 247/260＝95%（预警）· 吴宅 214/180＝118.9%（超支，剩余 −34h）**，
  加上原有赵宅 198/300＝66%（充裕），三档一眼可见；页面归为公司级（跨项目列表）

### 四十七轮：安装调试顶部改两张环形图（用户定）
- 四张数字卡 → **两张环形图**（写不下的文字走**悬停明细**：SVG `<title>` + 右侧图例表）：
  ①**项目状态分布**：四档各多少个（充裕/正常/接近超支预警/已超支亏损，配色同表格；
    未填预计工时的单列灰色），中心大字＝在装项目总数
  ②**合计工时对比**：已投入 vs 剩余预算（合计超 100% 时改为 预算内 + **超出部分红色**），
    中心大字＝合计工时占比；下方补三行：已投入合计 / 预计总工时 / 尚余或超出
- 新增通用件 `donut()` + `donutLegend()`，后续各部门图表复用

### 四十八轮：交付页（用户定五块）
- **待交付判定＝两条同时满足**：①施工部分已结束（安装项清零/工程确认完工）②**S4 尾款已结清**
  （以财务收款线现算为准）。只满足①的显示灰行「S4 未结清 —— 钱没到位不能约交付」，可先联系不建议排期；
  **施工已结束的项目自动移出「安装调试」页**（各归各页，不重复出现）
- **客户联络与通知**（机制同财务催款）：显示联系人是谁 + 是否已联系（渠道/时间/发送人）；
  **发交付预约**弹窗＝选客户方联系人（售前采集的联系人1/2/干系人/Builder/电工）· 渠道（短信/邮件/两者）·
  发送人可改 · **短信与邮件正文按模版渲染、可临时改**（变量：称呼/项目地址/交付时间/公司名/发送人）；
  发出即留痕并记「已联系」；**客户回不回都不挡进度**（同催款口径）；选邮件但对方没邮箱→门禁提示去售前补
- **交付日期排班**（做法同安装调试的资源分配）：日期 + 起止（15 分钟档）+ **工程管理指派谁去交付**（多选）+
  当天内容 → **写进派工排班表**并进 App 日程（红点等回 Y）；重叠/过去日期/没指派人都有门禁；
  留痕注明**当天人工自动进项目成本**
- **工时对比**：标准交付工时＝设置「合理时长参考 · 交付」× 指派人数；实际工时来自当天 App 打卡/每日上报；
  **实际 > 标准即标红**并显示超标多少（演示：陈宅 5.5h vs 标准 4h ＝ 137.5%）——超时不挂客户，吃工程利润率
- **运维条款签署确认**：工程管理勾选**置位**（留痕：谁、何时）；**没签不让确认交付**
  （否则运维接管与订阅收费没依据）；置位后通知运维「接管与订阅可启动」
- **确认交付**：人肉判断（系统不判断干得好不好），但门禁两条——运维条款已签 + 当天实际工时已回；
  确认即**工程利润率交付当天定格**（此后只读）、项目转「项目交付」、通知运维（免责维保起算）与财务

### 四十九轮：发送人身份统一（用户 2026-08-03 定 · 全站横切）
> 用户原话：「公司所有的短信发送人都是公司的统一申请到的号码，邮件发送人，财务都为
> accounts@konnext.com.au，信息类都为 info@konnext.com.au，工程管理都为 admin@konnext.com.au。
> 建议可以在总管理员页面的设置里面设置。」

- **短信：全公司只有一个号码**（公司统一申请的澳洲手机号 · Twilio）。催款 / 排班 / SM 排期 /
  交付预约 / 施工与追加告知 —— 全部从这一个号发出；客户回的 **Y**、员工回的 **Yes** 也全回到这个号，
  系统据此把红点变绿、把倒休扣掉。
  ⚠ **不能用字母发送者 ID**（收件人看到 "KONNEXT" 那种）：澳洲的字母 ID **收不到任何回复**，
  回执一条都拿不到，红点永远不变绿 —— 这条早在阻塞项 #1 就写死了，这里再落一次。
- **邮件：三个身份**，谁经办都用同一个地址，客户永远认得出是 KONNEXT：

| 身份 | 发件地址 | 用在哪些事 |
|---|---|---|
| 财务 | `accounts@konnext.com.au` | 催款 · Invoice 告知 · 收款确认 · 对账 · 报销结果 |
| 信息类 | `info@konnext.com.au` | 售前联络 · 通知与回执 · 订阅到期提醒 · 系统提醒 |
| 工程管理 | `admin@konnext.com.au` | 交付预约 · 排期告知 · 施工告知 · 工程追加告知 |

- **部门 → 身份映射写死在系统里**（`MAIL_OF`）：财务→财务身份 · 工程管理→工程管理身份 ·
  售前/采购/库管/运维→信息类。经办人**不用选**，系统按当前部门自动带；以后新部门默认走信息类。
- **★ 身份是公司的，署名是人的**：发件地址永远是上面三个之一；经办人姓名只出现在正文末尾的署名里
  —— 催款弹窗与交付通知里原来那个「发送人」框**更名为「署名（经办人）」**，语义分清。
  客户回信一律回到公司邮箱，经办人休假、离职都不会把信丢掉。
- **配置位置＝决策页「全局参数」**（只有决策管理员能改，二级账号调用直接门禁拦）：
  一个短信号码输入框 + 三行邮箱身份 + 部门映射一览；改动进操作日志（rel 六部门全带，谁都查得到）。
  **两条格式门禁**（宁可报错不要静默算错）：号码必须 `+61`/`0` 开头，邮箱必须 `名字@域名`
  —— 填错了不会报错，只是**所有短信发不出去 / 客户回信全丢**，所以在入口拦死。
- **发件身份横幅**（`fromBar`）挂在每个发信弹窗上：催款（财务身份）· 交付预约（工程管理身份）·
  派工排班（只有短信）。只发短信时**不显示邮箱**，只发邮件时不显示号码 —— 显示的就是这次真会用的。
- **通知记录定格发件身份**（`notifAdd` 自动按 `to[].how` 判断走短信还是邮件，写进 `NOTIF.from`）：
  改了身份**只影响之后发的，历史记录仍是当时那个身份** —— 同「定格 vs 现算」口径。
- 财务设置页的催款模版旁补一句指路：**发件身份不在这里改**，在决策页「全局参数」（只有决策管理员能改）。

**契约欠账 → v0_35**：`global_setting` 三键 `sms_sender_number` / `mail_from_finance` /
`mail_from_info` / `mail_from_eng`（写权限限决策管理员 tier=1）；发信留痕表补 `sender_identity` 列
（定格当时身份）。部署时把号码填成 Twilio 真号、三个邮箱在域名商处建好并做 SPF/DKIM。

### 五十轮：工程流水账 + 每日工时管理 + 删「人员与外包」（用户 2026-08-03 定）

**工程流水账**（用户原话：「工程负责人可以选择某一个项目，在下面就会列出这个项目以入场天为行，
发生的从第一次 SM 到交付的每一天次发生的人力投放、事件和回执记录等细节内容」）：
- **一个项目一本账**：项目在顶部项目条选（项目内页面）；**一行＝一个入场天**，从第一次 SM 到交付；
  **没人到场的日子不占行**（否则一屏全是空白）；最近的在最上
- 每行四样：**人力投放**（每人 计划起止 → **实际打卡入退场** + 时长 + 围栏异常标）·
  **当天事件**（SM 完成定格 / 电工签字 / SM3 现场变更 / ↳解锁财务 S2·S3 / 交付各步）·
  **回执记录**（排班短信回没回 Y 的红绿点 / 电工签字 / 运维条款置位 / 客户通知）·
  **每日上报原文与剩余工作**（原样引用）
- 头部三算：入场天数（首次→最近 + 日历跨度）· 累计人工工时（按打卡现算 + 人次）· 当前阶段 ·
  **派了工没打卡**（红）；过滤：阶段 chips + 「只看异常」
- **★计划 ≠ 事实**：过去的日子派了工却没打卡＝**红字「没打卡」**（工时算不出来、日结挂起，
  要去每日工时管理补录）；**未来的排班不算异常**，标「已排期 · 还没到」
- **本页只读**（流水账不许改，要改去源头改）；数据一个字都不自己造：
  排班 SCH · App 打卡 · 每日上报 · SMD · SM3 变更 · DELIV 交付线全部联表现算
- 演示：陈宅 KX-2026-0180 走完 SM1(2025/10/06) → 交付(2026/07/28) 共 306 天 / 11 个入场天；
  罗宅 KX-2026-0203 小陈 07/29–30 派了工没打卡 —— **就是「每日工时管理」那两天挂起的由来**（同一件事两处看得见）

**每日上报 → 更名「每日工时管理」**（用户定）：
- 页面名改，**App 端「每日上报」这个动作名不改**（员工在 App 上报，后台在这页管理与补录）
- 页头写明：**人员名单与薪酬由财务建立与维护，工程这边不重复建人**
- **补录选项目加搜索**（同派工排班：搜编号/昵称/地址，筛到唯一自动选中；上百个项目手选不现实）
- 新门禁：**没选项目不让提交**（不允许没有项目归属的工时；真不属于任何单个项目就选「公司级」）
- **原因类别改成工程设置里可增删**：重复拦 · **已被用过的删不掉**（删了历史记录的类别成孤儿）·
  「其他」是兜底项不可删；增删都留痕

**「人员与外包」整页删除**（用户定：工程人员由财务部门建立，工程这边不重复维护）。

### 五十一轮：文档库（用户 2026-08-03 定）
用途两类，一个库装：**①协作** —— SM1~4 要递交给配合团队的（布线图 / 线材清单 / 变更签字单 →
Builder · 电工 · 设计师）；**②技术** —— 我方工程人员在**施工端 App 现场查阅**的（接线规范 / 面板手册 / 调试步骤）。
- **树状归档**：工程管理自己新建 / 重命名 / 删除文件夹（同层重名拦 · **非空文件夹删不得**，
  否则文档连同链接一起消失，现场 App 打开就是 404）
- **上传**：名称必填 · 同文件夹重名拦（改版请用「传新版本」，不要新建同名的）· 说明 ≥5 字 → 存为 v1
- **版本**：「传新版本」存成 v+1，**旧版自动保留**可随时下载回看；**必须写「这一版改了什么」（≥5 字）**
  —— 不写的话现场拿到两版图纸不知道差在哪，照旧版施工就是返工
- **链接**：每份一个固定链接，**永远指向最新版** —— 现场用同一个链接看到的永远是最新的，不用重发
- **邮件转发**：发件身份＝**工程管理 admin@**（四十九轮统一）· 收件人来自配合团队候选或手填（邮箱格式拦）·
  发出即在版本历史里定格**已发放**（谁 / 何时 / 发给谁）
- **★发放过的版本删不得**（整份也删不得）：发给 Builder / 电工的那一版是日后「按谁给的图施工」
  唯一说得清的依据。不想让人再看到就取消「App 可查阅」，**记录留着**
- **App 可查阅**开关：勾了才出现在施工端 App「技术文档」屏
- 全库搜索（不限当前文件夹，命中显示所在路径）；每一次动作都进操作日志

### 五十二轮：日志查阅增强（全系统）· 设置补齐 · 部门总说明（用户 2026-08-03 定）
- **全系统六个部门的「操作日志」页统一增强**（共用 opLogPage）：在原有「部门 chip + 逐字搜索」上加
  **时间范围**（全部 / 今天 / 近 7 天 / 近 30 天 / **自定义起止**）· **操作人**下拉 ·
  **项目**下拉（含「公司级」一档）· 搜索扩到昵称与完整地址 · **命中 N / 共 M** · 「清空筛选」·
  **导出 CSV**（按当前筛选导出到本地，**导出这个动作本身也留痕**）
- **工程操作日志补上下游种子**：工程自身（排班 / SM 完成定格 / 交付通知 / 运维条款置位 / 文档新版与发放）
  ＋别人动作引起工程这边变化（**财务标记拒付停服 → 工程侧派不出任务**、**库管出库到场 → 可排安装**）
- **工程设置补齐**（用户：把各种阈值和必选下拉都做成可设置的）：原有六项之外新增
  **排班任务类型**（五类内建不可删 —— 与工时/倒休算法绑死；自建类型默认计工时 + 合理时长 4 时；
  **已被排班用过的删不掉**）与 **SM 模板与出发前清单**（SM1~4 各自条目增删，带 ◇ 的是**条件项**，
  只有方案里有那一项才出现；**改动只影响之后的场次，已完成的定格不动**）
- **帮助加「部门总说明」页签**：本页说明 / 部门总说明 两个页签。**工程管理写全文** ——
  这条线在干什么 · 一个项目的完整走法（总览→项目列表→SM→安装→交付，全程排班/工时/流水账/文档/日志）·
  十页各自干什么 · **最容易搞错的三件事**（S 是钱 SM 是会 / 派工是计划打卡是事实 / 定格的东西改不了）；
  售前与财务给要点版，未定稿部门给占位提示

**契约欠账 → v0_35**（与四十九轮的发送人身份键一并清）：
`attendance`（每人×每天×每任务的入退场，流水账与工时的事实源）· `eng_document` + `eng_document_version`
（树状路径 / 版本 / **sent_at 发放定格** / app_visible / 固定链接）· `daily_payroll.backfill_reason_cat`
改为引用可维护的 `setting` 选项表 · `sch_task_type` 与 `sm_checklist_item` 两张选项表 ·
`global_setting` 发送人身份四键。

---

## §17 并发与多设备安全（用户 2026-08-03 晚提出 · 五十三轮落地）

### 用户提的场景（原话）
> A 部门管理员在电脑 a 上打开了页面但没关，后来 A 在家里电脑 b 上做了修改操作。
> 这时候电脑 a 被同事动了一下 —— A 在 b 上的操作会不会被同事在 a 上的阅读操作影响产生混乱？

### 结论（先说清楚）
**纯"阅读"不会把 b 上的操作搞乱。** 每个请求是独立事务，读不写库，b 上改完就是库里的事实，
a 上那台只是拿着一份旧快照在显示。

**真正危险的不是"读"，是"点"。** 那台电脑还挂着 A 的登录态，同事碰一下、误点一个按钮，
系统会**当作 A 做的**，而且用的是屏幕上那份旧数据。三种后果：
1. **陈旧覆盖**：a 上是 21:00 的快照，同事 21:30 点保存，把 A 21:20 在 b 上改的内容盖回去，**全程不报错**
   —— 正是本项目最怕的「不报错、只是数字悄悄错了」
2. **谁做的分不清**：审计日志如实写「A 在 21:30 改了 X」，A 第二天看日志查不出所以然
3. **定格类回不来**：SM 完成定格 / 确认交付（利润率定格）/ 文档发放版本 / 标记请款 —— 误点一下不可逆

### 现状盘点（查代码得出，非印象）
- ✅ 身份在服务端定（`SET app.account_id` 取自 HttpOnly Cookie），前端改不了 —— 不会记成同事，**但也就是会记成 A**
- ✅ 「已读」是点按钮才标的（不是打开即已读）—— 同事单纯浏览**不会**把未读注释标掉，这条当初设计对了
- ✅ 契约本来就有乐观锁（`version` + `app.expected_version` + 触发器），但**只装在 `project` 一张表**
- ❌ 会话是无状态加密 Cookie（7 天滑动），**没有会话表 → 踢不掉某一台设备**，只能停用整个账号
- ❌ 没有空闲自动登出

### 五条措施（用户拍板做 ①②④⑤，② 定 30 分钟；③ 留待后端阶段）

| # | 措施 | 状态 | 落在哪 |
|---|---|---|---|
| ① | **乐观锁推广到所有可改业务表** | ✅ 已落 | 契约 v0.35 |
| ② | **空闲 30 分钟自动锁屏** + 解锁要短信码 | ✅ 已落 | app 会话层 + 原型 |
| ③ | 会话表 + 设备列表 + 一键注销 | ⏳ 欠账 | 需把无状态会话改成可吊销（jti + 撤销表） |
| ④ | **不可逆操作二次验证**（短信码） | ✅ 已落 | app `requireStepUp` + 原型 |
| ⑤ | **页面过期提示 + 冲突原句** | ✅ 已落 | 原型（后端由 ① 兜底） |

**一句话记法：读不会乱，点会乱。** ① 防"覆盖"，②③④ 防"别人碰"，两类都得有。

### ① 契约 v0.35（并发控制专版，三步全绿）
- 通用件 `trg_row_touch`（刷 updated_at + version 自增）与 `trg_row_optlock`（比对 `app.expected_version`）
  取代原 project 专用两只（专用函数已删）
- **自动扫全库挂锁**：凡是带 UPDATE / ALL 策略的表 = 有人能改它 = 必须挡住陈旧覆盖 → **42 张全挂上**
  （不手列清单，手列一定会漏且不报错）；只读表不挂，省得白加列
- 冲突原句：「并发冲突：这条记录在你打开页面之后已经被人改过（当前是第 X 版，你手上这份是第 Y 版）——
  请刷新页面看最新的内容再改，不要直接覆盖别人的修改」
- 版本号乱填也报中文句（不是 Postgres 的 invalid input syntax）
- **不设 `app.expected_version` 时放行** —— 系统函数、触发器内部更新、后台脚本照常走
- 新断言 **INV-CC-01**：可改的表必须挂乐观锁（以后新增表忘了挂，断言直接报违规，不靠人记）
- 新回归 `28_并发与陈旧覆盖.sql`（3 次拦截）：陈旧覆盖 ×2（project + material）· 版本号乱填 ×1；
  另验：刷新后拿新版本能改 · 不设 GUC 放行 · 手填 version=99 被触发器按 OLD+1 覆盖回去 · 42/42 覆盖率

### ②④ app 层（Next.js）
- `session.ts`：`IDLE_MS = 30 分钟`、`STEPUP_MS = 5 分钟`；`requireSession()` 空闲超时 →
  **stage 退回 sms_sent（锁屏，不销毁会话）**，解锁后回原页、没存的内容不丢；seen 超过 1 分钟才写 Cookie
- `requireStepUp()`：不可逆操作要求 5 分钟内验过短信码
- 新路由 `/api/auth/unlock`（解锁：request 补发码 / verify）与 `/api/auth/step-up`（二次验证：同结构）
- `otp.ts` 新增 `resendSmsOtp(accountId)` —— 两处都已经知道你是谁，不必再输手机号
- `db.ts`：`withAccount(id, fn, {expectedVersion})` → `set_config('app.expected_version', …, true)`；
  P0001 原样 409 透传（`toResponse` 早就这么做）

### ②④⑤ 原型（可点击演示）
- **锁屏层**：空闲 30 分钟真的会锁（`setInterval` 每 30 秒查一次）；决策页有「演示：立即锁屏」按钮；
  错码不解锁；解锁回原页
- **二次验证**已挂五处不可逆操作：**SM 完成定格 · 确认交付 · 文档邮件发放 · 标记请款(S1-3/S4) · 拒付停服**；
  验过 5 分钟内同类免再验；二次验证本身也进操作日志
- **页面过期橙条**：停留超 10 分钟顶部出现「这页数据是 hh:mm 取的…建议先刷新」+ 一键刷新
- **并发冲突演示**：决策页勾「模拟另一台已经改过这条」→ 去售前大表改一格 / 工程项目列表存负责人，
  就撞上冲突原句，且**你填的内容还在**
- 三个阈值（30 / 5 / 10 分钟）在决策页「全局参数」可改，**只有决策管理员**能改，填 0 或超 720 被拦、改动留痕

**欠账 → v0.36**：③ 可吊销会话（会话表 + 设备列表 + 一键注销）· 工程线表（attendance / eng_document(+version) /
三张选项表）· 发送人身份四键。

---

## §18 运维线（2026-08-04 五十四轮 · 首版）

### 这条线在干什么
**工程把房子交出去，运维管房子交出去以后的每一次麻烦。**
主线一条：**客户报修 → 派人上门 → 现场把事实说清楚 → 财务定价开票 → 结案**。
旁边守两件事：**维保钟**（这单该不该收钱）· **订阅**（每年该不该续费）。

### ★分工（最容易搞错的四条）
| 事 | 归谁 | 运维这边看到什么 |
|---|---|---|
| **定价 / 开票 / 收款** | **财务**（「运维财务支持」页） | 运维只负责把**事实**说清楚：谁去的、多久、什么原因坏的、换了什么件、客户签没签字 |
| **人员名单与时薪** | **财务**建立 | 只引用，不重复建人（同工程线） |
| **排班** | **共用工程那张派工排班表** | 上门＝排一条「维护」任务；**不另做一套排班** |
| **换件成本** | **库管出库**自动归集 | 出库那刻单价已定格，运维不手填成本 |

一句话：**运维管事，不管钱。**

### 八页（**「项目列表」不做** —— 五十四轮用户定：运维的入口是<b>维护单</b>不是项目；
项目信息每张单上都带着，而「维保状态」本身就是运维视角的项目清单）
**① 总览**（重做）——五块可点开 + 卡点表全现算：未结案维护单 · 已受理未派工（SLA 在跑）·
**上门完三样不齐**（最要命：人去了活干了，但缺一样，财务定不了价，这单卡死）· 免责维保将到期 · 订阅要处理。
每行「去处理 →」直达并只显示那一条。

**② 报修受理** —— 建维护单，记四样：哪个项目 · 客户什么时候报的 · 怎么报的 · 客户原话。
- 门禁：**项目必选且只能是已交付的**（没交付的问题走工程线整改，不是维护）· 客户原话必填 ≥5 字
- **★报修时间允许留空**：客户记不清就空着，显示【未知】，该单**不参与响应时长统计**。
  **绝不拿建单时间顶替** —— 那样每单响应时长都是漂亮的 0 小时，报表好看但是**假的**，
  等于把真实的响应慢永远藏起来（契约里早写死的口径，本轮 UI 照做）
- 口头预估金额：选填，**只作参考不参与计费**，实际以财务发票为准

**③ 运维派工状态**（五十六轮用户定：原名「派工与上门」，且**排人不在这一页**）——
**维护上门的排班一律在工程「派工排班」里排**：工作类型选「**维护**」，第二个框就**直接列出现有维护单**
（未派工、未结案的）；点中哪张单，**项目地址与工作内容自动带出**（取自运维建单时的现象与客户原话，
不用再抄一遍也不会抄错），剩余工作照旧按该单的项目自动带。门禁：必须选一张维护单
（「维护活儿的源头永远是客户报修，没有单就没有归因、没有客户签字，财务也定不了价」）·
过去的日期不能派 · 同一人当天不能重叠 · 上次维护没留剩余工作记录就拦。
运维这一页**只看状态**：派到谁、哪天、回没回 Y、上门完了没、三样齐没齐；能做两件事——
**作废** 与 **点现场三样齐**。
- ★**作废会连带删排班**：运维作废维护单 → 工程那边对应的维护排班**一起删掉**，并**短信 + App 通知本人**
  （进「取消待确认」等回 Yes）。活儿没了却还挂在人日程上、白跑一趟，这种错不许发生。
- ★**三样齐 ≠ 关单**（五十六轮用户强调）：三样齐只是**运维这边的活儿干完了**，单子转「待定价」
  **交给财务定价开票**；**等财务收到钱点「结案」，这张单才算真的关掉**。免单的单也一样走财务
  （金额 0 也要开出来记账，维保到底贴了多少钱要看得见）。
- **★三样齐是这条线的命门**：**①归因**（四类统一，现场当着客户面确认）· **②当日服务内容**
  （客户签字签的就是这段）· **③客户签字**。缺一样，财务**定不了价**，状态显示红色的「上门完 · 三样不齐」，
  并进总览卡点**说清缺哪样**。正式系统三样由**上门的人在 App 现场提交**，后台不能替他填
- 归因四类（human 人为 / product_defect 产品缺陷 / wear_out 自然老化 / force_majeure 不可抗力）
  是**全公司统一词表，内建不可改**：现场填一次，**免责判定**与 **RMA 向供应商索赔**两处都用它

**④ 维护单跟踪** —— 一张单走到哪一步：已受理 → 已派工 → 上门完（三样齐）→ 待定价 → 已开票 → 已结案 ｜ 已作废。
**后三步是财务在推**，运维只读显示进度。明细展开看：报修原话 · 现场三样 · **免责判定** ·
**成本构成现算**（人工＝(在场+路上)×成本时薪，物料＝出库台账归集，两样都不手填）· 钱（只读）。
作废要填原因（≥4 字），已开票/已结案的不能作废。

**⑤ 维保状态** —— ★**两套钟，不是一套**：

| | 起点 | 长度 | 决定什么 |
|---|---|---|---|
| **免责维保** | **交付那天** | 售前签约时定 3~12 个月 | 这单**收不收客户钱**（期内 + 非人为 = 全免） |
| **物料保修** | **S2 物料款结清那天** | 各供应商月数 | 能不能**找供应商赔**（走 RMA） |

常见的错是拿一套当两套用：要么白赔客户钱，要么错过向供应商索赔的窗口。
起点都是**定格的历史事实**，只读；剩余天数天天现算。**不做设备台账**（早就定了）。

**⑥ 业主订阅** —— 从写死假数据改成真算：五档（第 5 档＝不订阅，改到第 5 档**年费与期间一并清空**，
且不能续费）· 本期起止与剩余天数 · 自动续费开关 · 续费（**不可逆操作要二次验证**）· 退订（原因必填，
历史已收仍计累计但不再计年化）· **已交付未签订阅**单列一块（免责期到期前是谈订阅最好的时机）。
**订阅收支不计入项目利润率**（契约定死）。

**⑦ 操作日志** —— 通用件（时间/操作人/项目/搜索/导出），本轮所有动作都进日志。

**⑧ 设置**（新增）—— 阈值与下拉都可改：响应 SLA 小时 · 免责维保/物料保修/订阅三个提前提醒天数 ·
**订阅五档年费标准**（契约键 sub_tier1..4_fee 已建，**等用户填数**）· 报修渠道 · 常见故障现象
（重复拦 · 已被维护单用过的删不掉 · 「其他」不可删）· 故障归因四类**只读展示，内建不可改**。
订阅**改价只影响以后**：新签与下一次续费按新价，**已生效的那一期不动**。

**契约欠账 → v0.36**：maintenance_case 补报修四要素已在契约里（reported_at 可空 / report_channel /
taken_by / report_note）· 需补 `fault_symptom` 选项表与 `mt_setting` 键（slaH / 三个提醒天数）·
`subscription` 的 tier 年费键接 eng_setting。

### 本轮修的一个既有脆弱点
冒烟里安装调试「资源分配」用 `今天+3 天` 作演示日期 —— 今天从周一滚到周二后，
+3 撞上周五种子里 **老李的倒休 13:00–17:00**，被重叠门禁正确拦下，测试却当成 bug。
已改成固定「下周四」（种子里没人排班）。**教训：演示日期不要用相对偏移，会随星期几漂移。**

---

## §19 采购线（2026-08-04 五十七轮 · 首版）

### 定位（用户原话口径）
- 采购分两块：**集中采购**（公司备货）+ **项目采购**（某个项目专用）
- 采购要回答的就几个问题：**什么时候、什么原因、采购什么货、什么汇率、多少钱、多少货**，
  以及 **货到了没、什么时候能到**
- **补货建议都来自库管** —— **采购更像是库管需求指令的执行者**
- **真正的采购在线下进行**；这个平台是让采购**方便决策、执行正确的采购指令**
- 采购页面更多是管理 **价格 · 供应商 · 运输费用和时间 · 部门之间的门禁逻辑**
- ★**要不要采购，指令一定由库管下达，不能由采购决定**

### 十一页
**① 总览** —— 待下单需求（分项目/集中）· 在途未到 · **超期未到**（要催的）· 汇率新不新 · RMA 索赔中；卡点表现算。

**② 采购需求** —— 两个来源都是别人给的指令：**库管补货建议**（C1 低于红线，**系统现算**，不用人维护，
补够了自动消失）+ **项目需求**（S2 结清后库管按方案 BOM 提料）。勾选 → 生成采购单
（**一张单只能开给一个供应商**，勾了两家会拦）。
- ★**这页没有「新建需求」按钮，也不能删需求**——要买什么由库管定。采购觉得不合理只能
  **「退回库管」**并写清为什么（≥4 字），由库管改数量或撤销。**责任始终清楚：库管定要什么，采购定找谁买、什么价、什么时候到。**
- 生成采购单时**改了库管要的数量必须写原因**（最小起订量、整箱规格是正当理由，但不能悄悄改——
  到货点数对不上就成了谁也说不清的账），原因进日志并告知库管。

**③ 采购单** —— 指令留痕 + 到货跟踪。每单记：什么时候 · 谁 · **为什么买**（原因必选）· 买什么多少 ·
**币种与单价** · **运费** · **汇率（下单即定格 🔒）** · 落地 AUD · **预计到货 / 实际到货**。
- **汇率定格**：以后汇率再涨再跌这单不动，否则上月买的东西这月成本自己变，账永远对不上
- **落地成本 ＝（单价 + 运费）× 定格汇率** —— 海运便宜但慢、空运快但贵三倍，**运费和时间是一体两面**
- **超期未到要催，但别乱改 ETA**：对方给了新日期才改，原日期留在日志里。随手改 ETA＝超期天数永远归零，
  再也看不出哪家总拖 —— **供应商准时率就是这么算出来的**
- 已到货的不能取消（要退走库管退货流程）；取消时相关需求**退回「待下单」**

**④ 供应商**（用户强调：采购里非常重要的是供应商信息）—— 联系人/电话/邮箱 · 结算币种 · **标准交期**
（下单时按它带出 ETA）· 结算条件 · **历史准时率（现算：到货日 ≤ ETA）** · 在供物料数 · 在途单数 ·
**备注（经验）**——「春节前后要提前两周」这种经验**不写下来就只在一个人脑子里**，他一休假就断了。
邮箱格式与交期正整数两条门禁。

**⑤ 项目采购状态**（原「项目列表」改名，**只读**）—— 把**部门门禁链**画出来：
**①财务 S2 结清 → ②库管提料 → ③采购下单盯到货 → ④库管入库点数 → ⑤库管出库给提货人 → ⑥工程安装**。
采购只能推第③环，其余都在别的部门手里 —— 所以这页只读，免得"卡在谁那儿"要问三个人。

**⑥ 物料主表** · **⑦ 汇率** · **⑧ 调价历史** · **⑨ RMA 退换** · **⑩ 操作日志** · **⑪ 设置**
- **C1/C2 是两种买法**：C1 常备件设红线，低于红线自动出补货建议；C2 项目专用不备货。
  分错了：C2 当 C1＝钱压仓库；C1 当 C2＝每次急着买赶不上交期
- 调价**必须写原因**（≥4 字），进调价历史，**只增不改**——采购价直接决定毛利率
- 汇率页改的是"以后"，**已下单的汇率只读**；超过设置天数没更新进总览卡点
- RMA 来自运维现场归因＝**产品缺陷**且物料在**物料保修期**内（与客户的**免责维保**是两套钟，别混）

### 顺带（用户同轮定）
**库管「项目列表」→「项目出库状态」**：采购的项目采购状态一到货就是办理入库；项目真正需要的是**出库**——
把物料交到**指定提货人**手里。★**物料既跟着项目走，也跟着提货人走**。（库管线整体重设计见下一轮。）

**契约欠账 → v0.36**：supplier 表（联系人/币种/交期/结算/备注）· purchase_order 补 fx 定格与 eta/arrived_at
与 chase 记录 · purchase_req（需求来源 库管补货/项目需求 + 退回原因）· material 补 freight 与 reorder ·
proc_setting 三键 + 采购原因选项表。


---

## §19.1 采购线补完（2026-08-04 五十八~六十轮 · 用户逐条追加）

### 五十八轮 —— 供应商可增删改（用户：「采购应该能够建立修改删除供应商信息」）
建立 / 修改 / **停用** / **删除**四个动作，门禁全在"别把历史搞成孤儿"这一条上：
- **名称必填不重名** · **邮箱格式** · **标准交期正整数**（交期填 0 → ETA 全算成当天，采购单从此没有"超期"）
- **有在途未到采购单的不许停用** —— 停了就没人盯着催货了
- ★**被物料挂过 / 开过采购单 / 报过退换的删不掉**，只能**停用**：
  停用的不再出现在新下单与新建物料的选项里，**历史照旧查得到**。
  真删了：物料查不到供应商、采购单查不到是谁家的货 —— 而且**不会报错**，只是名字变成空白

### 五十九轮 —— 物料主表由采购管理，重点是调价（用户：「重要的是调价，操作调价时能看到最近 10 次调价历史」）
- 增 / 改 / **停用** / 删，门禁：编码格式 + **不能重码**（重码＝出入库全对不上）· 内部名称 ≥2 字 ·
  **C1 必须填补货红线**（不填就永远不出补货建议，缺货了也没人知道）· **C2 不许有红线** ·
  建档必须填采购单价（没价算不出落地成本）· 保修月数 ≥0 · 运费 ≥0
- **有库存的物料不许停用** —— 停了它从补货建议与出库选项里消失，剩下的货变成没人管的死账
- **被采购单/需求/退换引用过、或库存 >0 的删不掉**，改用停用
- ★**调价弹窗把这个料自己的最近 10 次历史摆在眼前**（时间 / 调前 / 调后 / 幅度 / 谁 / 为什么），
  下面才是新价输入框；**同价拦**（没改就别记一笔，历史要干净）· **原因必填 ≥4 字** ·
  实时预览"调完是多少"（百分比 + 落地 AUD 变化）· 一年内涨过多次会提示**该考虑换供应商或谈年度协议价**

### 六十轮 A —— 调价历史改成两张表（用户：「能够看到所有设备最近的一次调价，了解整个趋势，与物料主表中每一个单品的调价历史不冲突」）
一页三个角度，各看各的，**互不冲突**：

| 在哪 | 一行是什么 | 回答的问题 |
|---|---|---|
| 调价历史 · 上表 | **一个物料** | 整个目录在往哪走？谁最近涨了？**哪些从没核过价**？ |
| 调价历史 · 下表 | **一次调价** | 哪天动了哪个料？完整流水，只增不改 |
| 物料主表 · 调价弹窗 | **这一个料的一次调价** | 这次该不该接受？（改价当场对照它自己的历史） |

- 上表**每个物料都有一行**（含从没调过的，排最后并标灰）：现价 · 最近一次时间 · 调前→调后 ·
  **幅度（▲红/▼绿）** · **距今多少天** · 累计调过几次 · 为什么 · 行尾「调价」直达
- 四张趋势卡：**最近一次在涨的 N 项（平均 +x%）** / **在降的 N 项** / **★从没调过价的 N 项** / 最近一次调价是哪天
- ★**「距今」越大越危险**：供应商早涨了而我们还按老价算成本，**毛利率是假的，而且不会报错**。
  超过半年标黄 —— 不是让你去改数，是让你**去核一遍**
- 采购价照旧受 `fn_can_see_purchase_price` 遮罩：没有采购视野的人看到的是 `— —`

### 六十轮 B —— RMA「索赔」正名为「退换」，做成四步跟踪（用户原话）
> 「RMA 改索赔为退换，主要用于跟踪退换货过程：已退回，对方已收（定期短信催确认是否已接收），
> 已发货（定期短信确认是否已发货），已接收（采购结束跟踪并完成入库）」

**这页不是要钱，是跟货。** 四步一步都不能少：

| 步 | 谁动的 | 卡在这一步意味着 |
|---|---|---|
| **已退回** | 我们寄出坏件 | 在等对方确认收到 → **定期短信催** |
| **对方已收** | 对方签收 | 在等对方发换新件 → **定期短信催** |
| **已发货** | 对方发出 | 在路上，到了就收货 |
| **已接收** | 我们收到 | **采购跟踪结束 · 自动通知库管办入库** |

- **状态只能一步一步往前**，不许跳级、不许回退；**每一步必须填凭据 ≥4 字**（对方邮件 / 快递签收 / 单号）——
  口头的"对方说收到了"三个月后翻不出来
- **催确认**：三个在等的状态各问不同的话（是否收到 / 是否已发 / 单号与到期），短信走**公司统一号码**
  + 邮件走 `info@`，留痕第几次催、问的是什么；一个状态停超过 **退换催确认天数**（设置里可改，默认 5 天）
  → 这单跳进**采购卡点表**提醒去催。★不催，货就真在海上飘着没人问
- **已接收**要填**实收数量 + 收货凭据**：收得比寄回去的多 → 拦（数错了还是串单了）；
  **少收要二次确认并留痕**（差额别自己抹平）
- ★**采购只负责把货追回来**；真正点数、上架、记成本是**库管的动作** —— 标已接收即 `notifAdd` 通知库管
- 全站正名：17 处「索赔」→「退换」；口径也跟着改 —— 不再说"赔回冲减成本"，而是
  **"换回来的新件入库后，这次维护用掉的那件不用公司再自己掏钱补"**

**冒烟同步补 34 条断言**（五十九轮 18 条 + 六十轮 16 条），全部通过：**119 页 · 0 报错 · 0 断言失败**。

**契约欠账 → v0.36（在 §19 尾那串上再加）**：material 补 active（停用）· material_price_log（调价流水：
物料/币种/调前/调后/谁/为什么，只增不改）· rma 四态与 track/chase 两张子表 · proc_setting 补 `rma_chase_days`。


---

## §20 库管线重做（2026-08-04 六十一轮 · 用户点名"重新设计"）

用户三句话定了这一轮：
> 「库管的项目出库状态，**只有施工中才会有项目物料的出库**。不要显示无用内容」
> 「出库和退库与拒收，**顶上的当前项目有什么意义**？请帮我再把库管的内容仔细走一遍，删掉多余的」
> 「里面**有几项都没有内容**」

三个问题分别是：**列了不该列的**、**页面分类错了**、**七页是空的**。

### 一、库管全部是公司级页面（改分类）
原来 出库 / 退库与拒收 / 物料在谁手里 是「项目内页面」，顶上挂项目上下文条。**这是错的**：
库管一天要给好几个项目发货，页顶挂一个"当前项目"不但没用，还容易发错货；
到货**拒收**更是多数属于集中采购——**根本不属于任何项目**，挂个项目只会误导。

★**项目在单据里选，不在页顶挂** —— 一张单据一个项目，单据存下的那一刻这批货才和项目绑上。
从「项目出库状态」点「去出库」会把项目带过去（只做高亮预选，仍然可改）。

### 二、页面从 11 页改成 10 页（删 2 加 1，且每页都有真内容）
| 动作 | 页面 | 为什么 |
|---|---|---|
| 删 | **期初移库** | 上线时**一次性**动作，做完这页就永远没用了 → 并进「盘点」页底部的「期初导入」说明块 |
| 删 | **RMA 登记** | 退换单是**运维现场归因自动生成**、由**采购**跟四步流程；库管在这件事里的动作只有一个——**把换回来的新件入库**，那就是「入库」页 |
| 加 | **设置** | 两个阈值（提货回执超时 / 盘点差异提醒）要有地方改；另附一张「这些不在库管这儿改」对照表（红线在采购、S2 在财务、人员在财务）——**少走冤枉路** |

原来只有 4 页有内容（总览/项目出库状态/出库/操作日志），其余 7 页是占位符。这一轮**全部做实**。

### 三、★库存改成现算（这一轮最要紧的一条）
原来 `STOCKQ` 是写死的字典。**写死一个库存数最危险**：出库忘了扣、退库忘了加，
那个数会**永远错下去，而且不会报错**——正是 CLAUDE.md 开头说的"不报错、只是数字悄悄错了"。

改成：**当前库存 ＝ 期初 + 全部流水**（`WH_OPEN` + `WHMOVE`），永远现算，不落库
（符合「定格 vs 现算」里库存属现算）。改完之后：
- 数不对**一定是某一笔流水记错了**，翻库存流水就能找出是哪一笔
- 现算出来的 7 个物料库存与原来写死的数**完全一致**（12/4/3/22/9/3/14），冒烟里有断言钉住
- 「库存流水」页顶部直接把期初摆出来，账怎么算出来的一眼可见

### 四、项目出库状态：只列施工中 + 一张"尾巴表"
主表**只列施工中**——链条是 **财务 S2 结清 → 采购到货 → 库管入库 → 库管出库给提货人 → 工程安装**，
接洽中/设计中没有物料可发，交付后与维护期的领料走**维护单**不走项目出库。

★**唯一的例外**：施工期已经过去、货却还在提货人手里的项目（演示里就是王宅 0142，13 件 A$2,013.83
还在 Tony 手上）。它们不该混在"在施工的项目"里，但**更不能不显示**——那批料要按出库单价结进 S4，
**不列出来就等于把钱悄悄丢了，而且不会报错**。所以做成下面一张小表，**空了才是正常**。

### 五、每页的门禁（全部会拦、报错原句照抄）
| 页 | 门禁 |
|---|---|
| 入库 | 实收必须一件件点；**点数有差异时备注必填 ≥4 字**；★**按实收入库，差额自动开「到货差异」交采购**——库管不许把数字改成"刚好"，抹平了这批货就永远查不出去哪了 |
| 出库 | **S2 物料款没结清不能出库放货**（卡在财务，不在库管）· **出库数超库存直接拒**（不许先出后补，出成负库存账再也对不上）· 提货人只能是我方人员或本项目参建方 · **手机号与出库单价此刻定格 🔒** |
| 退库 | **退回物料状态必填 ≥4 字**（好料坏料混着退＝拿坏件当好货再发一次）· 退回数不能超过还欠的 |
| 盘点 | **有差异时原因必填 ≥4 字**；差异按盘盈/盘亏进流水；超过「盘点差异提醒 %」在日志里单独标出来（这种量级多半是**有一整笔出入库没记**）；**盘点账面数提交即定格** |

### 六、两个视角必须对得上
物料**既跟着项目走，也跟着提货人走**（用户五十七轮定的口径）：
- 「项目出库状态」按**项目**看 —— 这个工地还欠多少料、S4 能不能结
- 「物料在谁手里」按**提货人**看 —— **这批货现在到底在谁手上**（一个人可能同时在三个工地干活，只按项目看永远拼不出他手里压了多少）

冒烟有一条断言专门钉这个：两个视角加起来必须**相等**（演示态 44 件），
且库管现算的未退料金额必须与财务 `f.unret` **一致**（A$2,013.83）。

### 七、跨部门联动（顺手补上的一个真 bug）
财务点「结算未退料」时，原来只把 `f.unret` 归零，**库管台账不动** —— 两页从此各说各话，**而且不报错**。
这一轮补上同步：结算掉的行标 `stl`，与 `back`（真退回来了）**分开记** ——
**结算掉不等于货退回来了，两回事别混**。冒烟断言 `settleSync` 钉住。

### 八、验证
冒烟新增 **24 条库管断言**；全套 **6 角色 × 117 页 · 0 报错 · 0 断言失败**
（页数 119→117：删两页、加一页，wh 对 2 个角色可见）。

**契约欠账 → v0.36（再加）**：`stock_move` 流水表（一切进出，库存现算不落库）·
`stock_open` 期初 · `outbound`/`outbound_line`（提货人+手机号定格+出库单价定格+back/stl 两个字段）·
`goods_receipt_diff` 到货差异 · `stock_count` 盘点（账面数定格）· `wh_setting` 两键。


---

## §21 全面回归（2026-08-04 收工 · 用户点名「从售前到今天做的，测试全面」）

六条线（售前 · 财务 · 工程 · 运维 · 采购 · 库管）今天全部完成，做了一次端到端回归。

### 跑了什么

| 项 | 结果 |
|---|---|
| **UI 全量冒烟** | 6 角色 × **117 页** · **513 条断言** · 运行时报错 **0** · 断言失败 **0** |
| **页面内容审计**（新增） | 62 个去重页面，**占位符页 0 个** |
| **重复定义扫描**（新增） | 66 个页面键，**重复 0** |
| **契约 ① 干净建库** | 零报错 |
| **契约 ② 回归套件** | 28 个文件 · **153 次拦截** |
| **契约 ③ 断言** | **77 条**：建号后 76 通过 · **1 条高危违规（INV-SEC-09，新加的那条）** |
| **对象数复核** | 63 表 / 86 视图 / 148 策略 / 42 乐观锁触发器 / **非 security_invoker 视图 0** |

### 查出来的三件事

**① ★真问题：20 张表没开 RLS，而降权角色对它们有完整 DML**

跑 `db/deploy/10_app_role.sql` 建出 `konnext_app` 之后一查：它对这 20 张表有
`SELECT/INSERT/UPDATE/DELETE`，而这些表**一条 RLS 策略都没有**。最要紧的三张：

- `auth_otp` —— **能读到别人的短信验证码**（无密码登录，等于可被盗号）
- `audit_log` —— **留痕可被改、可被删**（留痕的意义就没了）
- `account_department` —— **改自己的部门归属＝提权**

其余：`procurement`（采购单业务表）· `notification` · `fx_rate` · `staff_daily_commitment` ·
`travel_estimate` · `job_checklist` · `dept_handoff` / `handoff_log` · `account_reset_log` ·
`assertion_def`（**能把自检删掉**）· 以及 6 张配置/元数据表（写侧同样该管住）。

**今天不可利用**：后端只查视图（`只查视图不查基表`），前端不直连数据库。但这是纵深防御的洞，
一旦哪天有人图省事在 API 里裸查一次基表，就直接漏。**→ v0.36 必修。**

**② 为什么以前一路全绿：断言在手列清单**

`INV-SEC-07` 叫「关键表必须开启 RLS」，但它 `WHERE relname IN (...)` **只列了 10 张表**。
20 张漏网的一次都没被提过。这正是 CLAUDE.md 早就写下的教训——
> 「能自动扫全库的地方绝不手列清单 —— 手列一定会漏且不报错（曾漏 13 个视图、漏售前身份）」

视图有 `security_invoker` 自动补齐的 DO 块兜底，**表没有**。所以补了一条自动扫全库的断言：

```sql
('INV-SEC-09','所有表必须开启 RLS（自动扫全库，不许手列清单）','high',
 $q$SELECT relname FROM pg_class
     WHERE relkind='r' AND relnamespace='public'::regnamespace AND NOT relrowsecurity$q$, …)
```

它现在报 20 处违规，**并且会一直报到修完为止** —— 这就是要的效果：把一个静默的洞变成一个吵闹的洞。

**③ 原型里查出一个真回归：`proc/总览` 被定义了两次**

五十七轮写了真算版的采购总览（现算需求/在途/超期/汇率/退换 + 卡点表），
但旧的写死版（`overview('采购',[...])`，硬编码「低于红线 5 项 / 待到货 12 项」）**没删掉，
而且排在后面** —— JS 对象字面量后者覆盖前者，所以用户这几天看到的一直是**旧的假数据版**。
已删旧版；同时加了「重复页面键扫描」，以后再出这种事当场报出来。

### 顺手补掉的

- **`decision/审计日志`** 原来是占位符（全系统最后一个空页）。做成**决策管理员看全公司**的总账：
  不按部门过滤（部门「操作日志」只给看和本部门有关的），并把**敏感操作**
  （改安全参数 / 改发件身份 / 删除停用 / 定格 / 拒付停服 / 标记请款 / 文档发放 / 账号权限 / 导出）
  单独挑一栏摆最上面 —— 这些出问题最难挽回，不该等人先搜再找。
- **冒烟新增「六部门跨线链路」一整段**（15 条断言），专测**部门之间对不对得上**：
  引用完整性（没有悬空项目号）· SM3/SM4 与财务 S2/S3 同源 · S2 结清 ⇄ 库管能否出库 ·
  库管未退料 ⇄ 财务 S4 未退料金额 · 库存低于红线 ⇄ 采购补货建议 · 采购到货/退换换回 ⇄ 库管待入库 ·
  库存唯一真相 · 维护派工 ⇄ 工程排班 · 项目归属三态 · 跨部门双写日志 ·
  **金额遮罩不漏**（用 `see:[]` 的运维身份扫五个页面，出现具体金额就算漏）。

### 两条演示数据层面的记录（不是缺陷）

- **售前大表与全公司项目表基本是两套**：7 个在建/交付/维护项目在售前大表里没有来源行。
  但**引用完整性是干净的**（各部门引用的项目号全部真实存在）。正式系统只有一张 `project` 表，不会出现。
- **昵称重名**：周宅＝0195/0186 · 吴宅＝0199/0210。现实中确实会重名，UI 一直按「编号 + 地址」区分，不影响判断。


---

## §22 契约 v0.36（2026-08-04 收工 · 用户「那就开始吧」）

全面回归查出的欠账，这一轮清完。分两步走。

### ① 补 RLS：20 张表一条策略都没有

跑 `db/deploy/10_app_role.sql` 建出降权角色之后一查：`konnext_app` 对这 20 张表有完整
`SELECT/INSERT/UPDATE/DELETE`，而它们**一条 RLS 策略都没有**。补法分四类：

| 类 | 表 | 怎么管 |
|---|---|---|
| **谁都不许碰** | `auth_otp` | 开 RLS 且**不建任何策略**（行级全拒）＋ `REVOKE ALL`（表级收权）。只留 `/api/auth/*` 的 owner 连接。两道都关才叫纵深 |
| **只增不改的留痕** | `audit_log` · `handoff_log` · `account_reset_log` | 只建 SELECT 与 INSERT 策略，**故意不建 UPDATE/DELETE** —— 能改的留痕不叫留痕，决策管理员也只能看 |
| **只有决策管理员能写** | `account_department` · `assertion_def` · `money_visibility` · `project_scope_registry` · `table_ownership` · `status_stall_threshold` · `dept_handoff_rule` | 能改 `account_department` 就能给自己加部门＝**提权**；能删 `assertion_def` 就能把**系统自检悄悄关掉** |
| **读全部、写本部门** | `fx_rate` · `job_checklist` · `public_holiday` · `sm_template` · `procurement` · `travel_estimate` · `dept_handoff` · `notification` · `staff_daily_commitment` | 套通用模板，并补齐 `table_ownership` 登记；`staff_daily_commitment` 带日薪覆盖价，读按薪酬口径限 |

**为什么以前一路全绿**：`INV-SEC-07` 叫「关键表必须开启 RLS」，但它 `WHERE relname IN (…)` **只列了 10 张**。
已用自动扫全库的 `INV-SEC-09` 兜底，INV-SEC-07 保留但不再是唯一防线。

**29 号回归**把手工验证过的七刀固化：以「售前负责人」身份试探——读别人的验证码（表级 permission denied）·
删审计日志（DELETE 0）· 改自己的部门（UPDATE 0）· 删改系统自检（0 行）· 塞后门断言 ·
越权写汇率 / 表归属 / 金额可见口径 / 停留阈值（全部 RLS 拒）。同时验证**该放行的照常放行**（写留痕 INSERT 1）。

### ② 采购与库管落库

契约里 `stock_out` / `stock_return` / `stocktake` / `v_material_stock` / `v_material_custody` /
`v_stock_ledger` 早就有了，**不重复造**。真正缺的是七件：

1. **`supplier`** —— 原来供应商只是一串 text，删不掉也管不住。现在：名称唯一 · **交期不许填 0**
   （填 0 → ETA 全算成下单当天 → 从此没有任何一单超期，准时率永远 100% 而且不报错）·
   **被引用的删不掉**（触发器给中文原句，指路"改用停用"）· **有在途未到单不许停用**
2. **`material_price_log`** —— 调价「为什么」原来没地方存（`v_material_price_history` 是从
   `audit_log` 推的，推不出原因）。现在 **原因必填 ≥4 字**、**同价不许记**、**只增不改**
3. **采购单定格汇率 + 采购原因 + `purchase_order_chase`** —— 汇率一旦定格改不了（触发器）；
   **已到货不许再改预计到货**（准时率靠它算）
4. **`purchase_req`** —— ★用户定的核心口径落库：`source` 只有 `warehouse_restock` /
   `project_demand` 两档，**故意没有 'procurement'**，采购只能「退回库管」且**必须写原因 ≥4 字**
5. **`rma_case` 四态 + `rma_track` + `rma_chase`** —— 已退回 → 对方已收 → 已发货 → 已接收；
   **只能往前推、不许跳级、每步必填凭据 ≥4 字**；标已接收**必须填实收数量且不能多于寄回数**
6. **`goods_receipt_diff`** —— 到货点数对不上：原因必填、没差异不许开单、了结必须写处理结果
7. **阈值与选项并入既有的 `eng_setting`**（不新建设置表 —— 架构不要过载）：
   `proc_eta_warn_days` / `proc_fx_stale_days` / `proc_req_sla_hours` / `proc_rma_chase_days` /
   `wh_ack_hours` / `wh_count_diff_pct` / `proc_reasons`

配套四条断言 `INV-PC-01/02/03` 与 `INV-WH-01`，四个新视图
（`v_supplier_perf` 准时率现算 · `v_material_price_trend` 每料最近一次 · `v_rma_tracking` · `v_purchase_req_open`）。

### ③ 这一轮撞出来的两个真问题

**★真 bug：集中采购根本标不了到货。**
`trg_notify_po` 在采购单转「已到货」时发部门交接。集中采购**天然没有项目**（物料是在**出库**那一刻
才落到项目上的），而 `dept_handoff_rule.po_arrived` 的 `default_scope` 是 `'project'` ——
于是交接行 `project_id IS NULL` 且 `scope='project'`，被项目归属三态门禁直接拒掉。
**整条集中采购流程走不通**，而集中采购是采购的大头。已把 `po_arrived` 标成 `company`，
并在 `fn_notify_dept` 里加了一句说得清的报错（规则要求挂项目却没传项目时，直接告诉你去改哪儿）。

**★更要紧的：这个 bug 一直在把假拦截混进回归计数。**
到货被拒 → 库存永远是 0 → 后面的出库报「库存 0」、退库报「Tony 名下只有…」——
**每一条都被 `run_all.sh` 记成"拦截成功"**。18 号、22 号、15 号三个文件实际上什么都没测到，
测试却一直是绿的。这正是 CLAUDE.md 开头写的那句话在测试台上的翻版：
> 「报数字前先看报错内容，不只数条数 —— 拦截次数会骗人」

**已把测试台修好**：`run_all.sh` 现在按 **门禁（中文原句）/ 约束权限 / ★其他** 三类分开数，
**「★其他」不是 0 就把原文逐条打出来**。修完这一刀又顺手查出三个测试文件在引用
**早就不存在的列**（`maintenance_case.quote_amount` / `cost_bucket`）——那 6 次也是假拦截。

### ④ 三步规矩（v0.36 终值）

| 步 | 结果 |
|---|---|
| ① 干净建库 | 零报错 |
| ② 回归套件 | 31 个文件 · **155 次拦截 ＝ 门禁 110 ＋ 约束/权限 45 ＋ ★其他 0** |
| ③ 断言 | **81 条全过**（建号后） |

对象数：**70 表 / 90 视图 / 226 策略 / 63 乐观锁触发器**；
**没开 RLS 的表 0 张 · 非 security_invoker 视图 0 个**。

**新增的一条硬规矩**：新增视图之后调 `SELECT fn_apply_view_conventions();` ——
一次做完 `security_invoker` 与 `pj_label` 两件事。这一轮我自己就漏了 4 个视图，
被 `INV-SEC-01` 与 `INV-UI-01` 当场抓出来；靠"记得回去重跑中段那两个 DO 块"迟早还会漏。


---

## §23 项目物料账（2026-08-05 六十二轮 · 采购负责人试用后提，老板拍板）

### 一、口径变更：项目料的提单人【从库管改成工程人员】

v0.36 刚落库的规矩是「要不要采购的指令一定由库管下达」。采购负责人试用后指出：
**项目采购应该由项目工程人员提单（SM2 后），Variation 也由项目负责人提单，工程审完再到采购。**

老板拍板：**采纳**。理由——方案和现场情况工程人员最清楚，库管管的是仓库不是项目。
（`purchase_req.source` 的枚举、断言 `INV-PC-02`、30 号回归都要跟着改 → 契约 v0.37）

**库管补货那一档不变**：C1 常备件低于红线仍是库管（系统现算）下的指令。

### 二、完整链条（四段）

```
外部报价系统（设计+报价，不在本系统）
   │ 客户接受报价 → 出物料清单 Excel → 带外交给采购
   ▼
①【报价原始单】采购在「采购需求」页上传 Excel → 基线 · 只读 · 版本化
   │  ★只认物料编码；认不上的挂起，卡住工程人员那一步
   ▼  SM2 之后
②【SM2 变更】工程人员在 App 改：不能删行、只能把数量调 0 · Additional 加新料 · 暂存 · 一键恢复
   ▼
   工程管理负责人审（★只看变量，不看整表）
   ▼
③【施工新增】SM2 之后只能增不能减
   ▼
④【库管先看现货】★有货直接出库，不够的差额才交采购
   ▼
   采购：勾选下单 → 已采购归清单，未采购留流水账（分期采购）
```

### 三、三个由老板拍板的决定

| 问题 | 定了什么 | 为什么 |
|---|---|---|
| 库管在项目料这条线上还管什么 | **工程审完先过库管看现货，再给采购** | 不会把仓库里已有的料又买一遍；库管也提前知道要备哪些货。代价是多一道手 |
| Excel 里的编码认不上怎么办 | **能认的先导入，认不上的挂起并卡住下一步** | 不用为一个编码重传整张表；也不会漏掉——工程人员那边显示"还缺 N 个料没建档"，不让开始改 |
| 谁审变更 | **工程管理负责人一个人审全部** | 口子收紧、标准统一；项目量还不大，还没到瓶颈 |

### 四、几条不能松的规矩

- **原始单的行永远不删，只能把数量调 0。** 删了行，"报价里本来有、后来不要了"这件事就消失了，
  客户回头问"我付的钱里那 10 个面板呢"没人答得上来。所以物料账里会出现「原始 10 → 现在 0」的灰行，那是**正常的**。
- **SM2 阶段可增可减；SM2 之后只能增不能减。** 施工都开始了还往下减，已经发到工地的料就对不上账。
- **待审的变更不算进应采。** 审完才算数 —— 否则采购会照着还没批的数去下单。
- ★**「买多了」必须当场标红**（这一轮做界面时当场撞出来的）：
  采购已经下了 18 件，工程后来把应采改成 0 —— 原来那行显示绿色「齐了」，
  **那 18 件的钱就悄悄留在项目成本里，谁也不会发现**。现在整行标红、单独统计，
  处理办法写在页面上：退供应商，或转成公司备货。
- **没勾的不会消失**：勾选下单之后，没勾的留在物料账里，什么时候要买什么时候再勾 —— 这就是分期采购。

### 五、这一轮做了什么（采购侧）

- 「采购需求」页重做：四个来源、**上传 Excel 弹窗**（版本号必填、不覆盖旧版、二次确认）、
  **★挂起区**（认不上的编码单独列出 + 「去建档」直达物料主表）
- 「项目采购状态」→ **「项目物料与采购」**：一个项目一本账，三段合成一张表 ——
  原始 / 变更 / 应采 / 已下单 / **库房现货（可顶多少）** / **★还要买** / **★买多了**；勾选下单
- 冒烟新增 **19 条断言**，全套 **117 页 · 532 条断言 · 0 报错 · 0 断言失败**

### 六、六十三轮：两页合一（用户 2026-08-05 当天追加）

> 「采购需求和项目物料与采购这两个页面，能不能合二为一？
>  1. 先展示项目的总览表，呈现哪些项目目前涉及采购内容
>  2. 点进去之后，能看到该项目物料采购的完整清单
>  3. 采购人员可以在清单内进行分批采购的操作」

**合了**，而且是两层结构（不是并排两块）：

| 层 | 看到什么 |
|---|---|
| **落地：项目总览** | 哪些项目现在有采购的事 —— 报价原始单版本 / 工程变更（含待审）/ 应采 / 已下单 / **★还要买** / **★买多了** / **「现在卡在哪」一句话** / 「进入 →」 |
| **点进去：项目明细** | 那个项目的完整物料清单（原始→变更→应采→已下单→库房现货→还要买）· **勾选分批下单** · 变更记录 · 「← 返回项目总览」 |

★**公司级的库管补货单独一块摆在总览层** —— 它不属于任何项目，进不了任何一本项目账，
所以总览这一层必须留着它的位置。

**顺带并掉一个隐患（这才是合并最大的收获）**：
项目料的需求原来**存了两份** —— `PCREQ` 里有库管当初提的需求行（`RQ-0188-01 C-Bus ×18`），
物料账那边又按「应采 − 已下单」现算一遍。**同一件事两个数字，迟早对不上，而且不会报错。**
现在只留一份：**项目料的需求全部由物料账现算**（`pcReqAll` 把 `bomRows().gap` 摊平），
`PCREQ` 空着。冒烟里有一条断言专门钉住：改一张变更单的审批状态，待下单需求必须**立刻**跟着变。

**采购不能删项目料需求**（它是工程审批过、现算出来的），只能**「提请工程复核」**并写清哪里不合理 ≥4 字，
由工程去改或撤销那条变更。

采购线页面数 11 → **10**；冒烟 **115 页 · 540 条断言 · 0 报错 · 0 断言失败**。

### 七、六十四轮：链条剩下的三段做实（2026-08-05 · 工程侧 + 库管侧）

六十二~六十三轮只做了**采购这一头**（报价单进来、三段合成一张账、勾选下单）。
这一轮把上游两段和中间那段补齐 —— 至此**四段链条全部可点可操作**：

> ①报价原始单（采购上传）→ ②现场在 App 改 → ③**工程管理审「变量」** →
> ④**库管先看现货**（能出的直接拨过去）→ 采购只买差额

#### 7.1 工程管理「物料变更审批」（新页 · 工程线第 11 页 · 公司级）

**★这页只摆变量，不摆整表** —— 这是全页最重要的一条口径：
报价单几十上百行，让负责人逐行核对，**实际结果一定是直接点批准**，那这道审批就等于不存在。
所以中间那一栏就是全部内容：**哪个料从几改成几 · 加了什么 ×几 · 为什么加 · 提单理由**。
完整那张表在采购的「项目物料与采购」里，随时能查。

- 四张卡：待审（含变量总数）· 涉及项目 · **★等最久几天**（≥2 天标红 —— 现场在等这个才敢继续干）·
  **★会被门禁拦几张**
- 筛选：待审（默认）/ 已批 / 已退回 / 全部 ＋ 逐字搜索（单号 / 项目 / 提单人 / 物料 / 原因）
- **批准** → 变量立刻进项目物料账 ＋ 通知**库管**（先看现货）· **采购**（买差额）· **提单人 App**
- **退回** → 原因必填 **≥4 字**（只写"不行"，现场不知道该怎么改）→ 提单人 App 上看得到原句
- ★**门禁：SM2 之后带减量的单批不了**，按钮直接禁用并说明为什么，只能退回让现场重提一张只增的

#### 7.2 App 物料屏（做进审批页里的**可操作**手机模拟器 ＋ 施工端 App 演示新增一屏）

老板在审批页上就能拿"手机"改几笔然后提交，上面那张待审表当场多一条 —— **改完就能看到审批长什么样**。
六条规矩（采购负责人 2026-08-05 提，用户拍板）：

| # | 规矩 | 为什么 |
|---|---|---|
| ① | **只能调 0，不能删行** | 删了行，"这个料本来报价里有、后来不要了"就消失了。客户回头问"我付的钱里那 10 个面板呢"，谁也答不上来 |
| ② | **每行摆原始采购数量** | 现场是在**原始数**的基础上加减，不是凭空填一个数 |
| ③ | **一键恢复** | 点错了随手退回原始数，不用记原来是几 |
| ④ | **换货＝老的减到 0 ＋ Additional 加新的** | 两个动作都留痕，比"直接换成另一个料"清楚得多 |
| ⑤ | **SM2 可增可减；施工中只能增** | 料可能已经发到工地甚至装上去了，这时候往下减，出库单和物料账立刻对不上，**而且不会报错**。点减号当场拦 |
| ⑥ | **暂存** | 改到一半锁屏、明天再打开都还在；**确认无误再提交**，半成品不会跑到审批那儿 |

另外四条门禁：Additional **必须写为什么**（工程管理只看变量，没理由没法判断）·
**同一个料不许重复加**（提示直接改数量）· **报价单里本来就有的料不许在 Additional 再加一遍**
（同一个料摆成两行，采购看到的应采数就错了）· ★**这张报价单还有物料没建档就不许提交**
（不然改了半天，最后发现少了几个料，前面的活白干）。

★**加料只能搜，不能翻**（用户 2026-08-10 当天提：「几百个物料工人根本找不到」）——
下拉列表在几百条物料面前等于没有。名字 / 编码 / 型号都能搜，已经在单上的**照样列出来但标灰**
（搜不到会让工人以为"库里没有"，转头自己写一个名字）；**搜不到就是库里没有 → 找采购建档**，
自己写名字对不上编码，出库入库全乱。施工端 App 演示那一屏同步改成搜索（演示物料库 24 条）。

多人一起改：每张变更单都记**谁提的 · 什么时候 · 改了什么 · 为什么**；
正式系统里同一条记录还挂着乐观锁版本号（§17），后提交的会被拦下并提示"这条已被别人改过"，
**不会悄悄把别人的改动覆盖掉**。

#### 7.3 库管「项目备料」（做进 wh/项目出库状态，不新开页）

**★这一步的全部意义是：让采购别再去买。** 仓库明明有货还下单，这笔钱就白花了，而且没人会发现。

- 表上一行一个施工中项目：应采 / 已下单 / **我已备** / **还要买** / **仓库能顶哪些**（逐个料写清"可顶几、还要几、库存几"）
- 弹窗逐行填"这次拨多少"，默认填**能顶的最大数**；备注选填
- 三条门禁：非负整数 · **拨的不能比还要的多** · ★**现货不够不许先占后补**
  （占了采购就不买，到时候两头都没有）
- ★**备料只是占住，不扣库存** —— 真正扣是在**出库**那一刻（要 S2 结清、要提货人签收）。
  冒烟有断言专门钉住这条：备料之后 `stockOf` 一动不动

于是**「还要买」＝ 应采 − 已下单 − 库管已备**；
**「买多了」＝ 已下单 + 已备 − 应采**（工程后来减了量，这批货已经花了钱 —— 标红，见 §23.五）。

#### 7.4 本轮口径小结（进契约 v0.37）

| 谁 | 能做什么 | 被什么卡 |
|---|---|---|
| 现场工程人员（App） | 调数量（不删行）· Additional 加料 · 暂存 · 提交 | 施工中只能增 · 加料要写理由 · 有料没建档不许提交 |
| 工程管理负责人 | 批准 / 退回（只看变量） | SM2 之后带减量的批不了 · 退回原因 ≥4 字 |
| 库管 | 从现货里拨给项目（备料） | 不能多拨 · **现货不够不许先占后补** · 备料不扣库存 |
| 采购 | 勾选下单（只买差额） | 需求不能删，只能提请工程复核 |

冒烟 **117 页 · 567 条断言 · 0 报错 · 0 断言失败**（本轮新增 27 条）。

### 八、六十五轮：提货人两类 · 通知来提货 · SM2 预提（2026-08-10）

#### 8.1 提货人分两类（用户定）

| 类 | 是谁 | 通知怎么发 |
|---|---|---|
| **第三方 · 工地** | Builder / 电工 / **其他工种**（泥水、木工、防水…），货交给他们拉到工地 | **中英双语**短信 ＋ 仓库地址 ＋ 营业时间 —— 他们多半不看中文，发中文等于没发 |
| **我方 · 办公室提货** | 自己的工程人员：施工中从办公室提货、**SM2 那天提线材** | App 推送 ＋ 短信（中文） |

Builder 与电工立项时就录了；**其他工种谁来拉货就现加一个**（库管在「项目出库状态」里加）。
★**没有手机号的提货人：不许发通知、不许开出库单** —— 短信发不出去等于没发，
而系统不会报错，货就一直堆在仓库没人来拉。缺号码的单独列一张表，一键补。

#### 8.2 「通知来提货」≠「催提货确认」（两条都在项目出库状态页，别混）

- **通知来提货**在**出库之前**：货备齐了，叫人来拉。**库管主动、手工发**
- **催提货确认**在**出库之后**：清单已发出，等提货人回 YES 认领
- 中间隔着的就是「开出库单」那一下 —— **那一刻才真正扣库存**

★**为什么不做自动定时催**（用户明确要手工）：什么时候叫人来拉，取决于工地进度和车什么时候有空，
系统不知道，库管知道。自动发只会变成没人看的短信。
通知过、**还没给他开出库单**的进「已通知、人还没来提」表，超过 `callDays`（默认 3 天）标红，可「再催」。
仓库地址与营业时间在库管设置里改，**原样写进短信**，改了只影响之后发出的。

#### 8.3 SM2 预提线材（老板拍板的例外通道）

发现的冲突：SM2 那天我方人员要从办公室带线材去现场，可那时 **S2 物料款根本还没开**
（S2 要 SM3 完成才解锁），照原门禁一定被拦下，工地就停在那儿。

开一条**窄通道**，四道锁死：①只能**我方人员**提 ②只能提**线材**（面板模块这些钱的大头照旧卡 S2）
③**用途必填 ≥4 字** ④**照样定格单价、照样进未退料台账、照样按 S4 结算** —— 一分钱不会丢。
第三方一律不走这条通道。冒烟有一条断言专门钉住：**开了这个口子之后，正常出库的 S2 门禁必须照旧卡死**。

### 九、六十六轮：把提料做进排班表（2026-08-10 用户提）

> 「工程管理人员在排某一个项目的人员时，可以根据不同情况进行出库提醒和申请。」

**为什么入口在排班**：谁去提、什么时候提，只有**排班这一刻**才知道。
排完人再单独跑一趟别的页面提料，十次有八次会忘 —— 然后人到了工地才发现没货，
白跑一趟，**而且系统不会报错**。

排班浮窗按工作类型出三种提料：

| 工作类型 | 提料方式 | 特别处理 |
|---|---|---|
| **安装** | 勾「提醒他去仓库办提货」 | **项目还没出过库＝首次进场**，当场标出来；施工出库通常**一次性出完** |
| **Site Meeting** | 勾「带线材去现场」 | 走 §8.3 那条窄通道：只我方、只线材 |
| **维护** | 填「这次上门要带什么」（选填） | 换件成本按**维护单**归集 |

勾上就把该项目**库管已备的料自动带出来**（库管早就拨好了，工程不用再抄一遍），可加可减；
加料**只能搜不能翻**。保存排班 → 生成一张**出货单**交库管。

**库存核对**（库管「项目出库状态」新增一块）：
- **够** → 通知工程与提货人 →「安排出货」开出库单（**那一刻才真正扣库存**）→ 出货单标已出库
- **不够** → **同时提醒采购（去买）和工程（别派人白跑）**，缺货明细写清「要多少 / 库存多少 /
  有没有在途采购单、ETA 几号」→ 工程去问采购拿 **lead time** 填回来（必填 ≥4 字），
  库管和现场同一句话都看得到

★**核对这一步不能省**：工程看不到库存（也不该看），库管不知道工地什么时候要人到场。
不核对就让人来拉，白跑一趟没人会报错。

**采购侧**：**只有运维缺的料单独进采购需求**（维护料不在报价物料账里）；
施工缺的**不重复加** —— 那部分本来就在项目物料账里算过了，同一件事两个数字迟早对不上而且不报错
（六十三轮刚踩过这个坑）。

工程线新增第 12 页「**出货单**」：走到哪一步、缺不缺货、交期填了没，一张表看完。

冒烟 **119 页 · 609 条断言 · 0 报错 · 0 断言失败**（六十五轮 +18、六十六轮 +20）。

### 九·补、六十七轮：项目出库状态改成「点状态看那一类」（2026-08-10 用户当场提）

> 「你现在是把所有状态的全部都从头到尾罗列出来，这个不应该是点选哪个状态，我就能够看到哪个状态的东西吗？
>  你这样子从头到尾罗列，并且还有重复的，就很乱。」

**用户说得对，是我做错了。** 六十四~六十六轮我一轮加一块表，堆到最后这页有六块表从头排到尾，
而且**同一个项目在好几块里重复出现**（林宅同时出现在"出库进度""待备料""已通知没来提"三处）。

改法：**八个状态卡＝八个页签，点一个只出一张表**。
等 S2 · 可以出库 · 待备料 · 工程提的出货单 · 叫了没来提 · 提货没回执 · 提货人缺号码 · 货还没回来。
每张表的列按那一类该看的东西定，动作按钮也只留那一类要用的。
新增一条断言钉死：**同一个项目不许在两类里同时出现** —— 不然过两轮又会堆回去。

### 十、下一轮

1. **契约 v0.37**：`quote_bom` / `quote_bom_line`（版本化）· `bom_change` / `bom_change_line` ·
   `bom_reserve`（库管备料）· `purchase_req.source` 枚举改
   （quote / sm2_change / build_add / warehouse_restock，**推翻 v0.36 的"项目料由库管下达"**）·
   门禁落库：不许删行只能调 0 · SM2 后只能增 · 待审不计入应采 · 已下单不能再往下减 ·
   备料不超现货；同时改 `INV-PC-02` 与 30 号回归
2. 财务与工程试用反馈（用户已带回，等逐条过）
