# CLAUDE.md — KONNEXT 办公管理平台

> Claude Code 每次会话自动读这份文件。**先读完这里，再动任何代码。**
> 详细决策见 `docs/KONNEXT_项目决策记录.md`（50KB，跨对话记忆，**遇到任何"为什么这么定"的疑问先查它**）。

---

## 这是什么项目

澳大利亚（悉尼/NSW）智能家居集成公司 KONNEXT 的多部门协作办公管理平台。
六个部门：售前 · 工程管理 · 采购 · 库管 · 财务 · 工程运维。

**数据库契约已完成并验证**：`db/contract_v0_39.sql` —— **84 表 / 103 视图 / 278 条 RLS 策略 / 98 条断言**。
（v0_39 ＝**部门授权**（用户 2026-08-10 提）：★**最高管理者默认对别的部门只读** ——
他是唯一能写所有部门的角色，也就是唯一可能和别人撞车的人；乐观锁是「撞了才报错」，这一版「根本不让它撞」。
`dept_delegation`：休假授权（负责人本人点 · **到期日必填** · 上限 90 天）/ 强制接管（联系不上时的出口 ·
**原因 ≥10 字 + 短信通知本人**）· 一个部门同时只能有一条生效 · 记录只增不删。
★写权限一处改全库生效：`fn_can_write` 加 `fn_in_dept`（**`fn_has_dept` 对管理员永远 true，
只堵一条等于没改**）+ `fn_admin_may_write`；删除策略自动扫全库改 55 条。
★「本职 vs 代部门」的划分是**天然的**：表在 `table_ownership` 有部门归属＝代部门要授权，没有＝本职照旧。
（原 v0_38 头注如下）
＝**派工排班表落库**（欠账表第 1 条）：`sched_task_type` 六类内建不可删 + `eng_schedule` 门禁八条
（15 分钟一档 · **同人同天不许重叠** · 非倒休必须有项目 · **倒休余额不足直接拦** ·
**不许给过去的日期排班** · 过去的不许改删 · 锁定要先解锁 · **维护上次没填剩余就派不了工**）
+ `daily_report_line` 每日上报分段（★没有它「剩余工作自动带出」落不了）+ 排班→出货单 + 断言 +5。
★踩到两个真 bug，见决策记录 §25.三/四：**BEFORE 触发器里 GENERATED 列是 NULL → 门禁静默放行**；
**门禁查带金额遮罩的视图 → 判断取决于「谁在看」**
（原 v0_37 头注如下）
＝清 §23 全部欠账：①**★推翻 v0.36 的「项目料由库管下达」** —— `purchase_req.source`
改五档（quote / sm2_change / build_add ＝工程人员提 · warehouse_restock 库管补货 · maintenance 运维缺料），
`INV-PC-02` 与 30 号回归同改；②项目物料账四表（`quote_bom` 只读版本化 · `bom_change` 暂存→提交→审批 ·
`bom_reserve` **占住不扣库存** · `mat_req` 出货单）+ **需求只存一份**的现算视图 `v_project_bom`；
③提货（`party_pickup` · `wh_call` · 澳洲手机号归一 · **没号码不许出库** · **SM2 预提线材四道锁**）；
④部门试用反馈落库（M1 报修时间必填+默认现在 / M3 P0~P3 SLA / M4 上门前要排查结论 /
M5 完结交财务 / E5 项目备忘 / E6 项目分级）；⑤`fn_apply_optlock()` 与 `fn_apply_view_conventions()` 配成一对
（原 v0_36 头注如下）
①＝补 RLS：20 张表一条策略都没有，`auth_otp` 连表级权限也收回，**没开 RLS 的表 0 张**；
②＝采购与库管落库：supplier / material_price_log / purchase_req / rma 四态 / goods_receipt_diff 等七件；
★同时修掉一个真 bug：**集中采购到货标不了**（交接没标 scope=company），它还一直在把假拦截混进回归计数）
（v0_34 变更＝财务定稿清欠账+工程追加拍板：daily_payroll 补录收紧（类别+详说≥10字门禁、归属改 eng）、
leave_request 休假单（回Y自动扣/强制留痕）+倒休透支门禁、expense 三类正名+删已付、三处 email、
todo_action_log、variation 工程追加（origin/labor_hours_est/告知短信+交付前门禁）——
回归新增 27 号文件，拦截 134→150；断言 73→75）
（v0_32 变更＝售前定稿对齐：6+1 落库口径、预测报价范围 est_quote_low/high、双注释+已读回执、
pre_stage_remind_days 与 六个下拉选项键（角色/工种/房屋/施工阶段/楼层/屋顶，对应硬 CHECK 放开）、
v_presales_pipeline + 每日提醒函数、to_contact 删除、「留标」全库更名「已流失」；
二十轮追加：六部门「操作日志」页 = 全局标准件，正式数据源 audit_log）
**现在的任务是写前后端**，不是重新设计数据库。

---

## 用户的两个核心担心（所有决策的驱动力）

1. **架构过载** —— 太复杂以后改不动
2. **逻辑错误长期不被发现** —— 这条更重要

> 实践中真正危险的 bug 全是"**不报错、只是数字悄悄错了**"：
> 退库不进库存、未退料不进发票、视图绕过 RLS、SECURITY DEFINER 提权。
> **门禁测试一个都没抓到**，所以有了 98 条不变量断言。
> 写代码时同样适用：**宁可报错，不要静默算错。**

---

## 绝对不能做的事

1. ❌ **前端直连 Supabase** —— RLS 靠 `SET app.account_id` 判断身份，前端直连等于让浏览器自己说是谁，改一行 JS 就能声称是核心管理员，278 条策略瞬间失效
2. ❌ **Service Key / 数据库连接串下发到浏览器**
3. ❌ **前端直接查基表**（`material` / `project` / `payment_milestone` …）—— 基表没有列级遮罩，一查就把金额全带出来。**只查视图**
4. ❌ **改动 `db/contract_*.sql` 却不跑回归** —— 见下方"改数据库的规矩"
5. ❌ **新增视图不设 `security_invoker=true`** —— PostgreSQL 视图默认按**视图所有者**执行，RLS 整个被绕过
6. ❌ **在 `SECURITY DEFINER` 函数里用 `pg_has_role(current_user, ...)`** —— 那里的 `current_user` 是函数所有者（超级用户），结果人人都被判成管理员
7. ❌ 把门禁报错包装成「操作失败，请重试」—— 契约里的提示都是完整中文句，**原样透传**

---

## 架构

```
浏览器（React + TypeScript + Vite）
    │  只调 API，不知道数据库存在
    ▼
后端 API（Next.js API Routes）
    │  ★ 在这里 SET app.account_id ← 取自服务器验证过的会话（HttpOnly Cookie）
    │    前端请求里带的任何身份信息一律忽略
    ▼
Supabase / PostgreSQL 15+
    278 条 RLS 策略 · 98 条断言 · 全部业务门禁
```

**必须 PostgreSQL 15+**（视图 `security_invoker` 选项要它）。

**业务逻辑全在数据库里**，后端只做两件事：认证 + 转发。**不要在应用层重写一遍业务规则。**

---

## 仓库结构

```
CLAUDE.md                       ← 本文件
db/
  contract_v0_39.sql            ← 契约本体，唯一权威（v0_38/v0_37/v0_36/v0_35/v0_34 保留作历史）
  tests/                        ← 34 个回归测试 + run_all.sh（按门禁/约束/★其他 分类计数）
  tools/
    导入期初库存.py
    期初库存_填写模板.csv
docs/
  KONNEXT_项目决策记录.md        ← ★跨对话记忆，最重要
  KONNEXT_全流程总览.md          ← 业务视角
  KONNEXT_UI界面清单与字段布局.md ← UI 规格
  KONNEXT_上线清单.md            ← 部署步骤与验收标准
  KONNEXT_UI原型.html            ← 可点击原型（单文件）
app/                            ← Next.js 15 应用（阶段1 API 层已就绪）
  src/lib/db.ts                 ← ★数据访问唯一入口：withAccount =
                                   BEGIN → SET LOCAL ROLE konnext_app →
                                   set_config(app.account_id) → COMMIT
                                   （owner 连接会绕过 RLS，业务绝不裸查）
  src/lib/{session,otp,errors}  ← 无状态加密会话 / OTP三通道 / P0001原句透传
  src/app/api/                  ← auth三步·me·todos·dev/otp·health
  src/app/(web)/                ← 页面：待浅色皮肤过审后按原型移植
  scripts/rls-smoke.mjs         ← 部署门禁：RLS 真咬冒烟（npm run rls-smoke）
db/deploy/10_app_role.sql       ← konnext_app 降权角色（每个库都要跑）
```

---

## 改数据库的规矩

任何 DDL 改动之后，**这三步一步都不能少**：

```bash
# 1. 干净建库，必须零报错
createdb konnext_test
psql -d konnext_test -v ON_ERROR_STOP=1 -f db/contract_v0_39.sql

# 2. 回归套件：期望 220 次拦截（34 个文件）
#    ★脚本现在按类型分开数：门禁（中文原句）151 ＋ 约束/权限 69 ＋ ★其他 0
#    「★其他」必须是 0 —— 不是 0 就说明有环境错误/真 bug 混在拦截数里冒充成功
cd db/tests && bash run_all.sh ../contract_v0_39.sql

# 3. 断言：期望「✓ 全部通过」
psql -d konnext_test -c "SELECT * FROM fn_assertion_summary();"
```
**期望**：98 条断言，空库上只有 INV-SEC-03（核心管理员）会报违规，**建号即过**。

**★新增表/视图之后各调一次**（v0.37 起配成一对）：`SELECT fn_apply_optlock();`（乐观锁自动扫全库挂上）
与 `SELECT fn_apply_view_conventions();` ——
它一次做两件事：`security_invoker=true`（不设＝RLS 被整个绕过）＋ `pj_label` 项目标识。
以前靠"记得回去重跑 DDL 中段那两个 DO 块"，这一轮我自己就漏了 4 个视图，被断言当场抓出来。

**★写操作必须带版本号**（v0.35 起）：`withAccount(id, fn, {expectedVersion})` —— 前端提交时把
打开这条记录时拿到的 `version` 原样带回来。对不上触发器直接拒并给中文原句（见 §17）。
不带 = 系统内部更新，放行。**76 张可改业务表全挂了锁**（v0.35 是 42 张 · v0.36 到 63 · v0.37 到 73 · v0.38 到 76），新增表忘了挂 → 断言 `INV-CC-01` 报违规。

**另外两条硬要求**：
- 新增视图 → 跑 DDL 末尾那个 `security_invoker` 自动补齐的 `DO` 块
- 新增表 → 在 `project_scope_registry` 里登记它的项目归属，否则断言 `INV-PJ-10` 报违规

**报数字前先看报错内容，不只数条数** —— 拦截次数会骗人，环境错误也计一次。
**★2026-08-04 实证**：v0.35 那 153 次里混着一串假拦截 —— 集中采购到货被一个真 bug 拒掉，
库存永远是 0，后面的出库/退库跟着连环报错，每一条都被记成"拦截成功"。
三个文件（18/22/15 号）实际上什么都没测到，测试却一直是绿的。
现在 `run_all.sh` 把 **门禁（中文原句）/ 约束权限 / ★其他** 分开数，
**「★其他」不是 0 就必须逐条看清楚**。

---

## 五条不可违背的产品规则

### ① S1~S4 与 SM1~SM4 是两套东西

| 编号 | 是什么 |
|---|---|
| **S1~S4** | **收款阶段**：S1 定金 10% ｜ S2 物料款 40% ｜ S3 人工款 40% ｜ S4 尾款 10% |
| **SM1~SM4** | **工程现场会议**：SM1 进场 ｜ SM2 布线交流 ｜ SM3 检查布线 ｜ SM4 封板核对 |

**S 是钱，SM 是会。** 命名、变量、UI 文案里都不许混。

### ② 项目归属三态

所有**流水/报告/报表/通知/任务/成本/收款/付款/报销/收入/支出/利润**必须有具体项目。
例外只有两种，且**必须明确标记，绝不许留空**：

- `company` 公司级
- `untraceable` 无从追溯（+ 提示人工确认，系统不猜）

**待办类界面：项目编号 + 项目地址必须放最前两列。**
无项目时显示「公司级」或「★无从追溯」，不显示空白。

### ③ 金额五类可见性

| 类别 | 谁能看 | 守卫函数 |
|---|---|---|
| 薪酬 | 财务＋管理层 | `fn_can_see_salary` |
| 成本与利润率 | 财务＋管理层 | `fn_can_see_margin` |
| 采购价与毛利率 | 采购＋管理层 | `fn_can_see_purchase_price` |
| 客户合同金额 | 财务＋售前＋管理层 | `fn_can_see_customer_amount` |
| 库存成本单价 | 库管＋采购＋财务＋管理层 | `fn_can_see_stock_cost` |

**业务流水互相可见 ≠ 钱的数字互相可见。**
遮罩后是 `NULL` → 前端显示 `— —`，**绝不显示 0**（显示 0 会被误读成"真的是零"）。

### ④ 账号三级 · 无密码登录

| 级 | 谁 | 能力 |
|---|---|---|
| 1 | 决策管理员（≤3，其中 1 个核心） | 本职全部；★**别的部门默认只读** —— 要动得等那个部门「休假授权」，或走「强制接管」（原因 ≥10 字 + 短信通知本人）。v0.39 起 |
| 2 | 部门负责人 | 读全部，**只写本部门归属的表** |
| 3 | 施工人员（含外包） | **只有 App，不能进后台，只有上报权限** |

**全系统无密码** —— 手机短信验证码（Twilio + **澳洲手机号**，不能用字母发送者 ID，那个收不到 YES 回复）。
管理员额外邮箱双验证。**只有管理员可自主找回**，其余一律管理员重置且留痕。

### ⑤ 定格 vs 现算

**定格落库**（历史事实，之后不可改）：应收金额（Invoice 发出时）· 结清差额 · 账龄起点 ·
工程利润率（交付当天）· 日结工资（当夜 0 点）· 盘点账面数 · Google 路程预估 · 文档已发放版本 · 出库单价

**现算不落库**：库存 · 已收/还差 · 超期天数 · 路上时间 · 付薪时长 · 项目成本 · 利润率 · 倒休余额 · 状态停留天数

**定格的数据在 UI 上必须只读，并写明什么时候定的。**

---

## UI 七条贯穿规则

1. **门禁报错原样透传**（都是完整中文句，如「SM3 未完成，不能发起 S2 物料款」）
2. 按钮可用性查 `v_my_permissions` + 业务前置条件决定，**悬停必须说明为什么禁用**
3. 金额 NULL → `— —`，不显示 0
4. 定格数据只读 + 写明定格时间
5. **只查视图，不查基表**
6. 跨部门动作完成后显示「已通知谁」（静默发送等于没发）
7. **待办以项目编号 + 项目地址开头**

**页面分两类，不能混：**

| 类型 | 例子 | 顶部 |
|---|---|---|
| **项目内** | SM · 安装 · 交付 · 维护 · S4结算 · 订阅 · 立项签约 | 项目上下文条（项目下拉 + 状态 + 📍地址 + 业主/Builder/电工） |
| **公司级** | 物料主表 · 汇率 · **库管全线（含出库/退库）** · 人员 · 每日工时 · 账号 · 决策看板 | 灰条注明「公司级页面 · 不属于任何单个项目」 |

> 混在一起做全局项目选择器，会让人误以为**库存也是分项目的**。

**★六十一轮用户纠正：出库/退库不是项目内页面** —— 库管一天要给好几个项目发货，
页顶挂"当前项目"没用还容易发错；到货**拒收**多属集中采购，根本不属于任何项目。
**项目在单据里选，不在页顶挂**：一张单据一个项目，单据存下的那一刻这批货才和项目绑上。

---

## 当前状态（2026-08-10 收工 · 六部门 UI 全部过完一轮部门试用）

**六部门 UI 已全部做完并逐条过了部门试用反馈**，冒烟 **121 页 · 670 条断言 · 0 报错 · 0 断言失败**。

**✅ 契约 v0.39 已出（2026-08-10）**：**部门授权 —— ★最高管理者默认对别的部门只读**（决策记录 §26）。
用户当天提的：他是唯一能写所有部门的角色，也就是唯一可能和别人撞车的人；乐观锁是「撞了才报错」，
这一版「根本不让它撞」。三步规矩全绿：**34 文件 220 拦截 = 门禁 151 + 约束 69 + ★其他 0** / 98 断言仅 INV-SEC-03。
逐文件 diff v0.38：**除新增的 33 号外其余 33 个文件一字未动**（220 = 210 + 10）。
★同一轮判定**不做设备列表+一键注销**（§26.二）：一个部门就一个负责人，30 分钟锁屏 + 停用账号已覆盖。
★**UI 还欠四件**（§26.八）：部门页顶横幅+休假授权按钮 · 管理员看别人部门时按钮置灰并悬停说明 ·
决策页「部门授权总览」· 强制接管弹窗。

**✅ 契约 v0.38（2026-08-10）**：**派工排班表落库，欠账表第 1 条已清**（规则台账见**决策记录 §25**）。
三步规矩全绿：建库零报错 / **33 文件 210 拦截 = 门禁 147 + 约束 63 + ★其他 0** /
95 断言仅 INV-SEC-03。逐文件 diff v0.37：**除新增的 32 号外其余 32 个文件一个数字都没变**（210 = 191 + 19）。

**★v0.38 踩到的两个真 bug，都是「门禁静默失效」（§25.三/四）**：
1. `minutes` 是 `GENERATED` 列 —— **BEFORE 触发器里 `NEW.minutes` 是 NULL**，
   `NULL > 0` 求值为 NULL，`IF` 不成立 → **倒休门禁一声不吭地放行**（8 小时倒休在余额 6 小时时进了库）。
   抓到它的是断言 `INV-SCH-04`（现算全库）—— **门禁自己查不出自己没生效**。
   → **硬规矩：BEFORE 触发器里绝不能读本表的 GENERATED 列，要用就自己算一遍。**
2. 门禁原本查 `v_toil_balance.balance_min`，那一列裹着**薪酬遮罩** —— 排班的人是工程管理，
   他没有薪酬可见性 → 余额永远 NULL → 门禁从「防透支」变成「全拦死」。断言第一版也踩了同一个坑。
   → **硬规矩：门禁与断言里不许查带金额遮罩的视图。** 遮罩是给人看的，
   门禁是系统级判断 —— 让它取决于「谁在看」，两个方向都会错，而且都不报错。

**✅ 契约 v0.37（2026-08-10）**：§23 全部欠账已清（决策记录 §24）。

**★v0.37 最该记住的一件事（§24.六）**：第一次跑完回归，数字 155 → 168 看着像「新门禁生效了」，
**逐条查下去 13 次没有一次是新门禁测到该测的东西** —— 报修渠道改必填 + 上门前排查结论门禁，
把旧测试里的**造数据**全拦了，后面的内容一条没跑（18 号从 0 拦截变 3 拦截，实际整个文件空转）。
`run_all.sh` 的分类计数只抓出其中 2 条，**另外 11 条是靠逐文件 diff 上一版才发现的**。
→ **改口径之后不要只看总数和「★其他」，要把每个文件的拦截数与上一版逐行 diff，
数字变了的每一个都必须打开看清楚。**

### 项目物料账四段链条（六十二~六十六轮，§23）

> ①报价原始单（外部报价系统出 Excel → 采购上传，版本化只读）→ ②现场在 App 改（**不能删行只能调 0**）
> → ③**工程管理审「变量」**（不看整表）→ ④**库管先看现货**（备料＝占住不扣库存）→ 采购只买差额

- **提单人＝工程人员**（推翻 v0.36 的「项目料由库管下达」）；库管补货那一档不变
- 采购「采购需求」两层：项目总览 →「进入」看该项目完整清单 · 勾选分批下单 · 公司级补货单独一块
- ★**加料只能搜不能翻**（几百个物料工人找不到）；搜不到＝库里没有 → 找采购建档
- ★**买多了标红**（已下单 18、工程改成 0，原来绿油油写着"齐了"，那笔钱就悄悄没了）
- ★项目料需求**只存一份**（全部由物料账现算）—— 同一件事两个数字迟早对不上且不报错

### 提货与出库（六十五~六十七轮，§23.八~九）

- **提货人两类**：第三方 · 工地（Builder/电工/其他工种）发**中英双语**短信＋仓库地址＋营业时间；
  我方 · 办公室提货发 App 推送＋短信。★**没手机号不许通知也不许出库**
- **「通知来提货」（出库前，库管主动手工）≠「催提货确认」（出库后，等回 YES）**，
  中间隔着「开出库单」那一下 —— **那一刻才真正扣库存**
- ★**SM2 预提线材**：SM2 那天 S2 还没开，四道锁（只我方 · 只线材 · 用途必填 · 照样定格单价+进未退料台账+按 S4 结）
- ★**把提料做进排班表**：安装＝勾「去仓库办提货」（首次进场当场标出）· SM＝勾「带线材」· 维护＝填「这次带什么」
  → 生成**出货单**给库管 → 核对库存：够→安排出货；**不够→同时提醒采购与工程**，工程问到 lead time 回填
- **项目出库状态＝八个状态页签，点一个只出一张表**（六十七轮用户纠正：别从头堆到尾还重复）

### 全站横切（六十八轮）

★**手机号格式全站统一**（`phoneChk`）：要收短信的必须是澳洲手机号（**座机收不到短信**）·
供应商可境外号 · **一律归一成 `+61 4xx xxx xxx`**（同一个人三种写法，去重永远对不上）·
**同一个号不许两个人共用**。顺带修：31 处 `\n` 显示成字面量；**提示框里的 `<b>` 原样显示成标签**
（全站 23 条）→ 转义后只放行 `<b>` 与 `<br>`。

### 部门试用反馈 14 条（六十九~七十四轮，§九·补三~补五）

运维 **M1** 报修时间拆两个（堵住"说不清就退出 SLA 统计"的洞）· **M2** 跨部门跳转回得来 ·
**M3** P0~P3 分级 + 各档 SLA + 超了自动进待办 · **M4** 研发排查（远程排查工时进成本 + 状态 + 上门前门禁）·
**M5** 默认只看未关闭 + 完结交财务（运维不跟踪钱）· **M6** 订阅只在续费时改档 ·
**M7** 故障分析（★按型号：同一型号不同项目都坏 → 停用它、找供应商）
工程 **E2** 新建项目详情页（含历史排班）· **E3** 安装调试并进详情 + 列表行资源用量超支标红 ·
**E4** 流水账搜索 · **E5** 项目备忘（多条取最近的当跟进时间，到期进待办）· **E6** 项目分级 A/B/C + 原因必填。
**E1（负责人不建档直接填联系人）用户决定先搁置。**

---

## 库管线重做（2026-08-04 六十一轮）· 采购补完 · 运维首版 · 售前 ✅ 财务 ✅ 工程 ✅

**★库管 UI 重做**（§20，十页：总览 / 项目出库状态 / 入库 / 出库 / 退库与拒收 / 盘点 /
物料在谁手里 / 库存流水 / 操作日志 / 设置）—— 用户三句话定的：
- **★库存改成现算**：当前库存 ＝ `WH_OPEN` 期初 + `WHMOVE` 流水，**不落库**。
  写死一个库存数最危险 —— 出库忘扣、退库忘加，数字**永远错下去且不报错**。
  现算结果与原写死的数完全一致（12/4/3/22/9/3/14），冒烟断言钉住
- **库管全部是公司级页面**：★**项目在单据里选，不在页顶挂**（用户问「出库/退库顶上的当前项目有什么意义」）——
  库管一天给好几个项目发货，挂"当前项目"容易发错；到货拒收多属集中采购，根本没有项目
- **删两页加一页**：期初移库（上线一次性 → 并进盘点页）· RMA登记（库管在退换里只做"把换回来的新件入库"＝入库页）；
  加**设置**（提货回执超时 / 盘点差异提醒 + 「这些不在库管这儿改」对照表）。原来 7 页是空的，现已全部做实
- **项目出库状态只列施工中** + 一张**尾巴表**（施工期已过、货还在提货人手里的）——
  ★不列出来就等于把 S4 的钱悄悄丢了
- **两个视角必须相等**：按项目看（S4 能不能结）vs 按提货人看（★货现在到底在谁手上），冒烟断言钉住
- 门禁：入库**差异必填备注 + 按实收入库自动开到货差异交采购**（不许抹平）· 出库 **S2 未结清不放货 · 超库存直接拒 ·
  手机号与出库单价定格🔒** · 退库**状态必填**（好坏料混退＝拿坏件当好货再发）· 盘点**差异必填原因 + 账面数定格**
- 补真 bug：财务「结算未退料」原来不动库管台账 → 两页各说各话且不报错；现同步标 `stl`，
  **与 `back`（真退回来了）分开记 —— 结算掉不等于货退回来了**
- 冒烟 **6 角色 × 117 页 · 0 报错 · 0 断言失败**（新增 24 条库管断言）

## 采购线补完（2026-08-04 五十七~六十轮）

**★五十八~六十轮追加**（§19.1）：
- **供应商增删改**：被物料/采购单/退换引用过的**删不掉**，只能停用（历史照旧可查）；
  有在途未到单的不许停用；交期填 0 拦（ETA 全算当天＝从此没有超期）
- **物料主表由采购管理**：增删改停用；**不能重码**（重码＝出入库全对不上）· C1 必须有红线 ·
  有库存不许停用；★**调价弹窗把这个料自己的最近 10 次历史摆在眼前**，同价拦、原因必填 ≥4 字
- **调价历史两张表**：上表**每个物料一行**（最近一次调的是什么 · 幅度 · **距今多久** · ★从没调过的标出来）
  看趋势；下表按时间是流水。与物料主表里的单品历史**三处各看各的角度，不冲突**。
  ★「距今」越大越危险 —— 供应商早涨了我们还按老价算成本，**毛利率是假的且不报错**
- **RMA「索赔」全站正名「退换」**（17 处）：这页不是要钱，是**跟货**。四步：
  **已退回 → 对方已收 → 已发货 → 已接收**；只能往前推、每步必填凭据 ≥4 字；
  在等对方的状态停超 `rmaChaseDays`（默认 5 天）→ 进卡点表提醒**定期短信催确认**；
  **已接收＝采购跟踪结束 + 自动通知库管办入库**（点数上架记成本是库管的动作）；
  实收多于寄回拦、少收二次确认并留痕
- 冒烟补 **34 条断言**；总计 **6 角色 × 119 页 · 0 报错 · 0 断言失败**

**★采购 UI 首版**（§19，十一页：总览 / 采购需求 / 采购单 / 供应商 / 项目采购状态(只读) /
物料主表 / 汇率 / 调价历史 / RMA退换 / 操作日志 / 设置）。定位（用户定）：
**采购是库管需求指令的执行者**——★**要不要采购的指令一定由库管下达，采购不能自己决定**
（需求页没有「新建」按钮、不能删，只能**退回库管**写明原因；改库管要的数量必须写原因）；
**真正的采购在线下做**，这里做**指令留痕 + 到货跟踪**；页面管的是
**价格 · 供应商 · 运输费用和时间 · 部门之间的门禁逻辑**。
**汇率下单即定格**（落地成本＝(单价+运费)×定格汇率）· **超期不许乱改 ETA**（准时率靠它算）·
供应商备注存经验 · C1/C2 两种买法 · 门禁链六环画在「项目采购状态」上。
**库管「项目列表」→「项目出库状态」**（物料既跟项目走，也跟提货人走）。
冒烟 **6 角色 × 119 页 · 0 报错 · 0 断言失败**。
**下一轮：库管管理页面重新设计**（用户已点名）。

## 运维线首版（2026-08-04 五十四~五十六轮）· 售前 ✅ 财务 ✅ 工程 ✅ 三线定稿

**★运维 UI 首版**（§18，八页：总览 / 报修受理 / 运维派工状态 / 维护单跟踪 / 维保状态 /
业主订阅 / 操作日志 / 设置；**运维不做「项目列表」**——入口是维护单不是项目）—— 等用户逐页过目提意见。要点：
**维护派人在工程「派工排班」里排**（工作类型选维护 → 第二个框列维护单 → 地址与内容自动带出）；
**运维作废维护单会连带删掉工程排班并短信+App 通知本人**；**三样齐≠关单**——交财务定价开票收款才算真结案；
**运维管事不管钱**（定价开票收款全在财务）· 人由财务建 · 排班共用工程那张表 · 换件成本走库管出库归集；
**报修时间允许【未知】**（绝不拿建单时间顶替，否则响应时长永远漂亮的 0）·
**三样齐是命门**（归因/服务内容/客户签字，缺一样财务定不了价，红色卡点说清缺哪样）·
**维保两套钟**（免责维保＝交付起，决定收不收客户钱；物料保修＝S2 结清起，决定能不能找供应商换）·
订阅五档（第 5 档改档即清空年费与期间、不能续费；**年费标准等用户填数**）。
冒烟 **6 角色 × 115 页 · 0 报错 · 0 断言失败**（新增 24 条运维断言）。

**★工程管理 UI 已定稿**（§16，三十七~五十二轮全落地；十页：总览 / 项目列表 / 派工排班 /
Site Meeting / 安装调试 / 交付 / 工程流水账 / 每日工时管理 / 文档库 / 操作日志 / 设置）。
**2026-08-03 全面终验**（售前+财务+工程三线一起跑）：
UI 冒烟 **6 角色 × 115 页 · 0 报错 · 0 断言失败**（人员与外包已删故 117→115）；
契约 v0_34 三步规矩 **零报错 / 27 文件 150 拦截 / 断言 74-75**（唯一例外 INV-SEC-03＝空库无核心管理员，
上线时按上线清单 2.3 建号即过）；对象数复核 63 表 / 86 视图 / 148 策略 / 75 断言，与文档一致。

**★财务 UI 已定稿**（§15，二十一~三十六轮全落地；与 §14 售前并列为部门页面模板）。

**2026-08-01 收工状态**（售前七天十九轮反馈全落地，用户口头「基本满意」定稿；
冒烟 6 角色 × 105 页零报错，界面断言全过）：

- ✅ **售前 UI 样板定稿** —— 全部规则见 **决策记录 §14（定稿口径，后续所有部门页面的模板）**：
  一页长大表（编号+立项时间冻结区）· 6+1 状态勾可点选反选 · 默认四大项+列组开关 · 三个填写时点 ·
  5 步立项引导（完成即真建档落当天）· 修改抽屉（凡展示即可改）· 双注释大框悬浮 · 逐字搜索 ·
  项目状态表（扇形 + 周月季年去向柱 + 每月立项项目成交图，全部按数据现算）·
  内生提醒（阈值售前自改，SLA/条目现算联动）· 待办中心三动作（已知悉/处理/搁置 + 超期最久置顶 +
  年月日小时自动单位 + 不累积 + 状态一变重新提醒）· 教学全收「？帮助」
- ✅ **契约 v0_32 已清售前欠账**（三步规矩全绿：零报错 / 130 拦截 / 73 断言）；
  60 表 / 86 视图 / 140 策略 / 73 断言；新视图 v_presales_pipeline = 售前大表数据源
- ✅ 应用阶段 1 后端已实测全绿（`app/`：登录三步/待办/账号 API + RLS 冒烟）；页面待移植
- ✅ 原型已发布为 claude.ai Artifact 可点链接（同文件重发即同链接更新）

**财务线进展（2026-08-01 二十一轮）**：用户定十一页架构已全部落进原型（规则台账＝决策记录 **§15**）——
财务节点 · 项目收款S1-S3（应收自动算/请收结催/门禁双向/通知确认/双图）· 尾款结算S4与Var（变量积累+组合+财务改价留痕）·
运维财务支持 · 催账和停服（超期/账龄双口径+拒付停服）· 工资数据 · 项目报销（图表）· 成本与利润率 · 设置；
「留标」chip 已更名「已流失」，项目列表新增「施工中」筛选与三个演示项目。
**定位（用户定）：真账在外部财务系统，本系统只管流程控制与信息效率。**

**回归工具已归档进仓库**（云容器回收也不丢）：
- `docs/tools/ui-smoke.js` —— UI 全量冒烟（**6 角色 × 117 页 · 513 条断言**，含六部门跨线链路；首次 `npm i playwright --no-save`）
- `docs/tools/发布原型副本.py` —— 生成 Artifact 发布副本（同文件重发＝同链接更新）

**2026-08-02 收工补充**（财务二十二~三十轮全落地，见 §15）：付款人机制（候选=售前采集，全息+下拉保存留痕）·
三件套标准列（编号·立项时间·昵称+完整地址）· 催款体系（分级/双语无昵称模版/弹窗渠道选择/烂尾不催款/回执非必要）·
原生弹窗全面替换（Artifact 沙箱屏蔽 alert/confirm——自制 toast+弹层，原型今后禁用原生弹窗）·
运维支持成本核算（人工现算+物料出库归集）· 报销三类口径 · 去处理直达过滤 · 「全部」清搜索。
**契约 v0_33 已出**：财务收款线已定口径落库，三步全绿（零报错 / 134 拦截 / 73 断言）。

**2026-08-02 下午追加**：
- **插叙**（工程负责人）：工程蓝图一页 + 施工端 App 七屏演示（两个新 Artifact）+
  「工程追加变更」提案做进原型（SM3 后可追加 · 工时工程填只进成本 · 物料传库管 · 并入 S4——§7.3，**等老板拍板**→v0_34）
- **三十一轮＝工资数据改成数据平台**（§15）：项目视角人力消耗表（预计/交付耗/交付前人工成本/维护耗+每人阶段时薪明细）·
  日结 0 点定格后本人不可自改、挂起由**工程负责人补录**（三十二轮定：入口=工程端「每日上报」页+待办提醒；
  项目+时长+原因类别+详说≥10字必填、滚下月补发进 KPI、财务只读显示、挂起不卡封账）·
  日结近况用昵称+地址 · 本周期全员日结网格（21 号滚动）· 「生成汇总单」按钮删（三十三轮：汇总现算即最新，21 号自动滚动即封账）·
  倒休休假（短信回 Y 自动扣/强制扣减红字）· 设置页工资人员管理（成本时薪门禁+停用留痕）·
  售前录人处全部补邮箱栏（选填）；冒烟 117 页全绿
- **三十四轮＝项目报销闭环**（§15）：只做上报与审批、**付款不走本系统**（无已付状态）· 类别正名
  材料补购/交通递送/工具采购 · 收据=图片链接（弹层预览+图旁批准驳回+下载到本地）·
  施工端 App 新增「报销」屏（拍收据必填+项目默认当天派工）
- **三十五轮＝成本页项目分布地图**（§15）：色点=利润率四档色、点选弹窗昵称+完整地址；
  原型自绘经纬度示意图，正式系统接 Google 地图（与路程预估共用 API Key，部署时配置）

**2026-08-02 晚间定稿补充（三十四~三十六轮）**：报销闭环（§15：收据链接预览/图旁审批/下载本地、
付款不走本系统、App 报销屏）· 成本页项目分布地图（利润率四档色点+点选弹窗）·
操作日志上下游种子 + 九页「这页怎么用」帮助 · **全站左栏「？」删除**。

**✅ 契约 v0_34 已出（2026-08-02 晚）**：§15 欠账全清 + 工程追加变更落库（拍板：放开+告知短信）；
三步全绿（零报错 / 27 文件 150 拦截 / 断言 74/75——例外仍只 INV-SEC-03 空库无管理员）。
**★新教训**：给已有列加枚举 CHECK 前先查系统函数在写哪些值（origin 差点掐死 'unreturned'，22 号回归抓出）。

**2026-08-02 深夜 · 工程线开工（用户点名工程优先，功能图已过目认可）**：
- **三十七轮＝Site Meeting 旗舰页首版**（§16）：四卡四场会 · 出发前清单按方案条件项自动增减 ·
  顺序门禁 + SM3 电工签字 + 完成定格 · **SM3/SM4 完成即解锁财务 S2/S3（同源联动）** ·
  SM3 现场变更 mini 表单（移位不计费）· 标不适用留痕 · 下游门禁现状条；冒烟 10 断言全绿
- **三十八轮＝入场离场全局口径（用户纠正）**：每人×每天×**每个任务**（SM/安装/交付/维护全一样，运维同样）——
  SM 卡显示本场到场打卡（App 采集+围栏异常标）、App 打卡屏改任务维度；采集永远在 App，后台只展示

**2026-08-03 工程线**（§16）：
- **三十九轮＝总览重做**：在建/维护双计数+各自停留超阈值 · 今日派工**按小时**（人次无意义）·
  未记录工时 · 超期交接（可确认接收/催办）· 五卡全可点开明细 · 卡点表＝停留/交接/挂起现算派生汇总
- **四十轮＝设置阈值统一可调 + 工程项目列表**：六态（含 **Site Meeting 阶段**、**已停服**取代流失/烂尾，
  财务确认款项不到位触发）· 工程进度列显示当前阶段 · **金额全隐藏** ·
  业主/Builder/电工**由工程负责人指定**（候选=售前采集，保存即📌标注+通知下游）

- **四十一~四十二轮＝派工排班**（§16）：周视图五类任务 · 15 分钟粒度/≥60 自动小时 · 项目必填门禁 ·
  倒休余额校验 · 只记剩余工作 · 可查项目历史上报 · 周五提醒+按天/整周锁定 · 只能改此刻之后 ·
  取消要回 Yes · **红绿点确认（日程无论如何已同步 App）** · **点人名看当天 A/B/C/D 路线** ·
  合理时长（设置里定义，重点 SM/交付，只提示不拦）· **全浮窗录入不做拖拽**
- 修 bug：工程总览「去处理」未直达（goPj 未预填 engPjQ）——已修 + 收窄跨部门污染
- **五十三轮＝并发与多设备安全**（**§17**，用户提的真实场景：电脑没关被同事碰到）：
  **读不会乱，点会乱** —— ①**乐观锁推广到 42 张可改表**（契约 v0.35 · 冲突给中文原句 · 断言 INV-CC-01
  自动扫全库）②**空闲 30 分钟锁屏**（解锁要短信码 · 不销毁会话不丢内容）④**不可逆操作二次验证**
  （SM 定格/确认交付/文档发放/标记请款/拒付停服，验过 5 分钟内免再验）⑤**页面过期橙条 + 冲突原句**；
  三个阈值在决策页可改（只有决策管理员）。**③ 设备列表+一键注销欠着**（要先把会话改成可吊销的）
- **五十二轮＝日志查阅增强（全系统）+ 设置补齐 + 部门总说明**（§16）：六部门操作日志统一加
  **时间范围（含自定义起止）/ 操作人 / 项目（含公司级）/ 搜索扩到昵称地址 / 命中计数 / 清空 / 导出 CSV（导出也留痕）**；
  工程日志补上下游种子（财务停服→工程派不出任务 · 库管出库到场→可排安装）；
  工程设置补 **排班任务类型**（内建五类不可删 · 用过的删不掉）与 **SM 出发前清单模板**（◇＝条件项 · 只影响之后场次）；
  帮助加「**部门总说明**」页签（工程管理写全文，含「最容易搞错的三件事」）
- **五十一轮＝文档库**（§16）：两类文档一个库（**协作**＝SM 递交配合团队 · **技术**＝App 现场查阅）·
  树状归档（非空文件夹删不得）· 传新版必须写「改了什么」· **旧版自动保留、链接永远指向最新版** ·
  邮件转发用工程管理 admin@ 身份 · **发放过的版本删不得**（扯皮唯一依据）· App 可查阅开关
- **五十轮＝工程流水账 + 每日工时管理 + 删人员与外包**（§16）：流水账＝一个项目一本账，
  **一行＝一个入场天**（首次 SM → 交付），人力投放（**计划 vs 实际打卡**）· 事件 · 回执 · 上报原文；
  **只读**，过去派了工没打卡＝红字（未来排班不算异常）；「每日上报」更名「**每日工时管理**」
  （补录选项目可搜 · 无项目门禁 · **原因类别设置里可增删**）；「人员与外包」整页删（工程人员由财务建立）
- **四十九轮＝发送人身份统一**（§16，全站横切）：短信**全公司只有一个号码**（统一申请的澳洲号，
  Twilio；**不能用字母发送者 ID**，那种收不到回的 Y）；邮件三身份 —— 财务 `accounts@` ·
  信息类 `info@` · 工程管理 `admin@konnext.com.au`，部门→身份系统自动带；
  **身份是公司的、署名是人的**（原「发送人」框更名「署名（经办人）」）；
  配置在**决策页「全局参数」**（只有决策管理员能改 + 号码/邮箱格式两条门禁 + 留痕）；
  发信弹窗挂「发件身份」横幅，通知记录**定格当时身份**（改了只影响以后）
- **四十七~四十八轮**（§16）：安装调试顶部改**两张环形图**（悬停出明细，新增通用件 donut）·
  **交付页**（待交付＝施工结束+S4结清 · 客户联络通知弹窗 · 交付排班进排班表 · 工时对比超标标红 ·
  运维条款置位 · 确认交付＝利润率定格+移交运维，两条门禁）
- **四十六轮＝安装调试页**（§16）：只列 SM4 后~交付前 · 剩余工作量（自动带上次上报）+ 已投入/预计总工时 +
  **工时占比四档**（充裕/正常/接近超支预警/已超支亏损，预警线设置可改）· **资源分配→直接进派工排班表**
- **四十五轮＝Site Meeting 重做**（§16）：总表（项目×SM1~4 状态+下游门禁）+ 过滤/搜索 + 「详细」展开四卡 ·
  **现场记录浮窗**（记录/照片/到场打卡）· **排期=派工带日期**（参会人多选→自动写进排班表）·
  **完成定格改由工程师 App 提交**（App 新增 SM 现场提交屏）· 「标不适用」＝老项目补录出口（已写进帮助）
- **四十四轮＝排班加起止时间**（§16）：每段活填 开始→截止（15 分钟档），时长自动算；
  开始时间灵活（演示：上午休息 10:00 进场干到 19:45）；同人当天两段不许重叠（新门禁）
- **四十三轮＝排班浮窗四修**（§16）：倒休只填时长 · 关联项目可搜（筛到唯一自动选中）· 工作内容多行 ·
  **剩余工作改自动生成**（取该项目最近一次每日上报的剩余；SM 无 / 施工交付带上次 /
  维护可空但上次没填就拦派工）· App 每日上报拆「今天做了什么 + 还剩什么」两栏

**下次开工顺序**（2026-08-03 晚：工程线十页已全部做完并终验）：
1. **下一个部门**（用户点名：明天开新的一条线）—— 采购 / 库管 / 运维 三选一，页面清单见 §16 与 UI 清单
2. ~~契约 v0_35~~ / ~~v0_36~~ / ~~v0_37~~ **已出**（v0.37 见决策记录 §24）
3. 阶段 1 页面移植（登录/壳/待办抽屉）→ Vercel 部署等账号

**每页只定三件事**：①谁进来、要干什么 ②能做哪些操作、被什么门禁卡 ③操作完触发谁
**等用户交回**：Twilio/Supabase/Vercel 账号（指南已发）· 物料主表与期初盘点 Excel（表已发）

---

## 已知欠账（2026-08-10 更新）

| # | 事项 | 严重度 | 说明 |
|---|---|---|---|
| 1 | 历史排班导入通道 | 低 | **用户 2026-08-10 确认「不能给过去的日期排班」这条门禁保留**，日常不存在事后补排班。只剩「上线时要不要把过去几个月的排班导进来」这一种可能 —— ★**留到上线前再问一次，现在不要做**（做了没人用的通道，等于给门禁开一个长期敞着的口子）。真要做就照期初库存移库那样：一次性、留痕、只能做一遍，绝不是让人去 `DISABLE TRIGGER` |
| 2 | E1 负责人不建档、直接填联系人+手机号 | 低 | **用户 2026-08-10 决定先搁置**，不是遗漏 |
| 3 | ~~五十三轮③ 设备列表 + 一键注销~~ | — | **用户 2026-08-10 判定不做**：一个部门就一个负责人，不存在「精挑一台设备踢掉」的需求。30 分钟锁屏（全账号统一）+ 停用账号兜底已覆盖 |
| 4 | 原型演示数据：售前大表与全公司项目表基本是两套 | 低 | 只是演示种子割裂，引用完整性干净；正式系统只有一张 `project` 表 |
| 5 | 演示数据昵称重名（周宅 0195/0186 · 吴宅 0199/0210） | 低 | 现实中确实会重名；UI 已按「编号 + 地址」区分 |

**v0.36 已修的两条历史欠账**：20 张表没开 RLS（已补齐，`auth_otp` 更收回表级权限）·
原 `INV-SEC-07` 手列 10 张表（已用自动扫全库的 `INV-SEC-09` 兜底）。

## 阻塞项（要用户提供）

| # | 事项 | 阻塞 |
|---|---|---|
| 1 | **Twilio 账号 + 澳洲手机号**（全公司统一一个号；**不能用字母发送者 ID**——收不到回的 Y）<br>＋三个邮箱建好并配 SPF/DKIM：`accounts@` / `info@` / `admin@konnext.com.au` | 登录功能（第一阶段就要）；全站短信邮件发件身份 |
| 2 | ~~加班倍数~~ **已解**（2026-07-30 定：暂全 1.0，财务在系统里可改，v0_31 已开权限） | — |
| 3 | ~~订阅五档年费标准~~ **已解机制**（v0_31 建了 sub_tier1..4_fee 键，运维在系统里填数即可） | — |
| 4 | 物料主表初始数据 + 仓库盘点结果 | 采购库管全线、期初移库 |
| 5 | Supabase 连接串（生产+预发） | 部署 |

**密钥一律放 `.env.local`，永不进 git。** 仓库里要有 `.env.example` 说明需要哪些变量。

---

## 明确不做

❌ 设备台账（维保只查该项目 S2 收款日期）
❌ Variation 争议追溯（交给人，系统只摆事实）
❌ 报价系统 API 对接（保持手填，`quote_source='manual'`）

---

## 工作方式

- **一次一个问题**问用户，用大白话，不要一次抛一堆
- 用户答完，**分类确认**（这属于字段 / 关系 / 卡点 / 报表 / 权限）
- **能自动扫全库的地方绝不手列清单** —— 手列一定会漏且不报错（曾漏 13 个视图、漏售前身份）
- 发现自己做错了就直说，不要悄悄绕过
- 全程中文（UI 文案、代码注释、提交信息）
